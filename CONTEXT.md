# Mondrian Editing Context

Mondrian edits time-based audiovisual projects while preserving deterministic timeline semantics and explicit runtime capability evidence.

## Language

**Playback Session**:
One contiguous transport run with a stable epoch, timeline mapping, rate, direction, and clock policy.
_Avoid_: Player instance, preview session

**Transport State**:
The user-visible stopped, paused, priming, playing, recovering, or ended condition of a Playback Session.
_Avoid_: Playback boolean

**Clock Master**:
The single authoritative elapsed-media-time source for a running Playback Session.
_Avoid_: Current video frame, UI timer

**Monotonic Runtime Clock**:
A process-local nondecreasing elapsed-time source used for lifecycle ages, expiration, and execution evidence. It is never authored/media time and never advances a Playback Session.
_Avoid_: Clock Master, Timeline Time, wall-clock timestamp, UI event-loop timer

**Source Sample Target**:
One exact source-local `TimelineTime` plus a half-open sampling boundary. `Covering` selects the grid cell containing the target; `StrictPredecessor` selects the cell immediately before an exclusive reverse boundary. The target remains intact through Timeline, prepared visual/audio schedules, Preview, Export, cache identity, and nesting until the declared physical or child Evaluation Grid consumes the boundary exactly once. A zero-rate hold retains the boundary of the picture it captured.
_Avoid_: Naked source time, nearest frame, epsilon before boundary, decoder-specific reverse flag, App-inferred source frame rate

**Realtime Audio Output Negotiation**:
The Media-owned, generation-local selection of one concrete physical monitoring contract from the selected device's enumerated CPAL output candidates and, where required, a platform stream contract. It requires the exact requested sample rate and channel extent, chooses one executable scalar format deterministically, retains host/device/configuration evidence and typed rejection codes, and never treats channel count as named speaker positions. Portable CPAL Mono/Stereo use explicit versioned conventions and Discrete remains ordinal. On Windows, representable named multichannel layouts use a dedicated event-driven WASAPI stream whose successful `WAVEFORMATEXTENSIBLE` initialization binds the exact derived speaker mask; CPAL's `DIRECTOUT` stream is never used as named-layout evidence.
_Avoid_: Default sample format plus arbitrary requested config, channel-count speaker inference, backend error string as the only evidence, device negotiation mutating a Sequence Program

**Frame Work Deadline**:
An opaque Adapter deadline paired with its remaining duration at admission, then lowered once into the Frame Work Broker's Monotonic Runtime Clock for queue, cancellation, and completion decisions.
_Avoid_: UI comparison closure, decode-completion `Instant`, renewed timeout after queueing

**Frame Demand**:
A versioned request for the best frame needed at a timeline position before a presentation deadline. Priming uses its independent bounded limit; a running demand expires at the target's next exact media-frame boundary in the active Clock Master phase, while Paused and Ended demands are untimed.
_Avoid_: Render request, preview refresh, `now + one frame` deadline renewal

**Frame Delivery**:
The timestamp-bound terminal outcome of one Frame Demand, including readiness, execution path, degradation, and blocker evidence. The Playback Engine is the only authority that can apply it and produce the corresponding accepted/rejected state-and-clock-phase evidence.
_Avoid_: Preview result, terminal outcome without completion time, Adapter-composed acceptance/evidence

**Frame Presentation Ticket**:
Opaque authority binding one Frame Demand identity, its final presentation deadline, and the only allowed on-time quality outcome for a CPU or GPU Presentation Adapter.
_Avoid_: Preclassified ready result, UI-ready flag

**Frame Request Binding**:
The latest Frame Demand identity, Adapter deadline, and typed physical-resource scope atomically attached to one opaque frame-work key; a reusable completion may satisfy this binding only when the resource scope still matches, while an obsolete cancellation cannot consume it.
_Avoid_: Worker-captured demand identity as final authority, semantic key alone as physical-ownership identity, separate pending and completion ownership

**Frame Work Broker**:
The Playback Module that atomically owns frame-work admission, payload-preserving rebinding, queued transport, worker-lane-bound in-flight execution leases, generation invalidation, active Playback Demand supersession, preemption, lowered deadlines, cancellation, worker-completion stamps, and completion binding while treating media payloads and absolute Adapter clock values as opaque data. A payload-free bind may update already-owned work only for the exact semantic key and typed physical-resource scope; otherwise the Adapter must acquire a new move-only payload before admission. One active Playback Demand may bind multiple media keys, but synchronizing its successor removes every older unstarted `Current/Playback` payload; an older in-flight execution may finish only for locality and cannot regain publication authority.
_Avoid_: Independent scheduler and worker queue, Adapter-owned worker-activity counters, key-only worker completion, UI-owned priority rollback, Adapter-composed freshness/preemption queries

**Frame Cancellation Evidence**:
Playback-owned, all-run evidence from an authoritative cancellation request through the first cooperative execution checkpoint to worker return, partitioned by Playback, Interactive, and Still frame-work class.
_Avoid_: UI counter families, full worker lifetime as cancellation latency, one cleanup budget for unlike work classes

**Preview Decode Cancellation Fact**:
The media Adapter's immutable first-observation fact for one canceled decode: one typed execution checkpoint plus an ordinary cooperative observation, FFmpeg blocking-I/O interruption, or parent-enforced isolated-demux termination. It complements Broker-owned cause and timing evidence; it does not own cancellation authority or infer timing.
_Avoid_: unit `Canceled`, parsing FFmpeg error text, reporting process termination as an FFmpeg callback return, last checkpoint instead of first observation, media-owned cancellation reason

**Decoded Presentation Extent**:
The proven half-open stream-PTS interval `[start, end)` during which one decoded video frame is authoritative. A positive decoded duration supplies a provisional end, an observed successor PTS may shorten it, and unknown extent proves only the exact start. Evidence interpretation is independent of access mode: Playback and deterministic Still require containment, while Scrub alone may publish a non-covering candidate as Degraded.
_Avoid_: Half-frame tolerance, nearest PTS presented as exact, selected-start equality as the only VFR correctness rule

**Execution Cancellation Token**:
Monotonic, cloneable generation authority shared by schedulers, runtimes, and concrete media Adapters. Cancellation never resets a token; a new generation receives a new token. Canceled execution may not populate success caches or terminal failure memory.
_Avoid_: Resettable flags, Adapter-owned generation truth, caching canceled results

**Execution Work Intent**:
The minimal cross-domain value language for priority, an optional Adapter-lowered deadline, generation cancellation, and terminal disposition. It is not a scheduler: Preview frames, waveform analysis, thumbnails, proxies, and Export retain domain-owned admission, capacity, worker, and resource policy.
_Avoid_: Universal media worker pool, cross-domain job enum, one global capacity or eviction policy

**Platform Execution Contract**:
The shared production semantics and typed capability/evidence Interface implemented by Windows, macOS, Linux, and Headless Adapters. Project, Timeline, Playback, Audio, Effects, Color, Viewer, and Export Modules consume this contract without acquiring an OS graphics API, native handle, or platform-specific synchronization rule. D3D12, Vulkan, Metal, VideoToolbox, VA-API, window-system, audio-device, and display-management details remain inside their concrete Adapters. A platform may lack current real-device qualification evidence, but it cannot substitute a Windows-only Implementation, an untyped success, or semantically incorrect pixels/time as support.
_Avoid_: Cross-platform placeholder, Windows semantics behind a generic name, D3D12 handle in a shared render Interface, missing hardware evidence treated as missing architecture, backend-dependent author model

**Execution Resource Decision**:
A versioned immutable product-policy snapshot that projects machine memory class, real domain-owned worker phase, fixed-cadence Product Process Tree memory, and whole-system pressure into a distinct typed decision for each execution Module. A projection contains only fields that domain consumes; each Module applies it idempotently behind its existing queue and lifecycle Interface.
_Avoid_: One padded background-decision shape, presentation-rate memory probing, pending-minus-deferred presented as Running, universal worker pool, cross-domain job authority, canceling running work by policy, changing source/color/export semantics, inferring success or failure

**Execution Terminal Evidence**:
One immutable terminal classification for a Module-local execution attempt: generation, cross-domain priority, completed/failed/canceled/superseded/rejected disposition, and deadline status. Domain diagnostics retain the detailed reason and resource evidence.
_Avoid_: Boolean success, tracing log as completion truth, UI-inferred cancellation, shared type owning domain failure taxonomy

**Project Session Handoff**:
The persistence-owned transition that closes one exact Authoring Session's admission, places a FIFO barrier after every earlier save/autosave request, applies queued scalar completions while that Session is still authoritative, and then either resumes a new Persistence Generation or retires the Session. Window close/quit drives it through one move-only non-blocking pause ticket; Headless callers may wait on the same protocol. Save-before-close also binds the exact required manual request and may retire only after it becomes the applied durable baseline. While quiescing, author Actions and Project-scoped result publication are frozen, but UI projection and event processing remain responsive. Protocol failure retains the Project fail-closed; explicit user Discard may retire admission without claiming quiescence, while request-owned payload/lease lifetime continues to actual worker return and every eventual completion stays stale.
_Avoid_: UI-thread save wait, pending-count quiescence, empty-queue authority, barrier success substituted for required-save success, dropping a close ticket as success, editing against poisoned persistence admission, silent force-close after a protocol fault

**Thumbnail Execution Service**:
The UI-independent App Module that resolves exact source/color identity, admits and cancels deterministic still work, owns bounded raster/failure residency, and publishes terminal execution evidence. It retains its actual native worker and closes shared admission, bounded transports and publication identities before consuming that worker under one original deadline. Concurrent consuming closure is explicitly unavailable, never an invented empty inventory; later closure retains the first immutable receipt. Its product output is a validated encoded RGBA raster; Widget payloads exist only in the Window Adapter.
_Avoid_: Widget-owned FFmpeg worker, unbounded completion channel, cache keyed only by AssetId, stale publication after relink or color-generation change, UI raster as execution payload

**Proxy Generation Service**:
The AppState-owned, lazily started deep Module that unifies import, user, and Playback-recovery proxy demand under one exact artifact key, bounded project generation, fair domain queues, cache-root execution capacity, recoverable failure memory, and terminal evidence. Media owns freshness, encoding, and cancellable FFmpeg artifact production; Window and Preview callers own no second dedupe registry.
_Avoid_: Process-global app dispatcher, unbounded FIFO, worker blocked on a limiter after falsely entering Running, permanent Preview request set, project-close orphan transcode, automatic retry storm

**Timeline Export Snapshot**:
One immutable execution capture containing a root Sequence, only the nested Sequence closure selected by the prepared visual schedule and encoded Audio Program Output, the exact selected `PreparedVisualProgram` set from one stable Effect Definition Registry Revision, exact range-selected root/nested `CompiledAudioProgram` occurrences keyed by Sequence, public Output, and projected dependency window, one internally consistent media-dependency record per selected file-backed Media Asset, the selected range, and the exact Project Color Environment. The same frozen Programs produce the visual media/Transition/nesting evidence and routed audio Component demands stored beside them. Queue admission recomputes author-derived evidence, rejects mismatched Program content even when IDs/revisions were reused, validates physical media/Component completeness and selected Transition picture handles, and byte-freezes every selected Basic Title face under a separate grant. A deserialized or manually assembled snapshot has no executable attachment and must prepare and validate one before admission. Workers consume the captured visual and audio Programs, frozen font bytes, and frozen Asset identities; they never recompile against the live Effect registry, rescan author Tracks/Clips for audibility, or query the live system font catalog. Generated Asset Library identities do not become file dependencies. Capture rejects missing/cyclic selected internal references and offline selected media before admission; off-range, visually hidden, post-mute-gated, disabled, or non-encoded-audio sources cannot poison the job. A selected pre-mute Route remains executable, and Audio Track visibility is presentation state that never excludes PCM. The snapshot then executes independently of later Project edits, Effect-definition replacement, font installation/removal, or closure.
_Avoid_: Whole-Project clone, four parallel asset maps, live Asset Library/Effect Registry/font-catalog lookup from the worker, path without source revision, Sequence-ID-only audio Program cache, revision number without bound Program objects, media-presence audibility heuristic, snapshot presented as persisted author state

**Smart Render Video Reuse**:
The conservative Export optimization that replaces pixel rendering and video encoding only when the frozen Prepared Visual Schedule proves one continuous, full-range, 1x file-backed Media Clip with identity Transform/Opacity/Blend, no overlap/Transition/generated/nested content, no enabled Effect or Mask, and no placement interpretation override; the frozen media probe then proves exact codec/profile/raster/cadence/picture/signal/color compatibility with the resolved Delivery Contract. The current route reuses only the complete selected source video stream, so it never invents arbitrary GOP-trim safety. Media independently hashes the ordered encoded packet payload before and after remux, ordinary output validation still proves codec/profile/GOP/signal/audio/duration, and source revision is rechecked before durable publication. Any missing or contradictory evidence silently selects the complete render/encode path; it never weakens correctness or publication.
_Avoid_: Asset-count passthrough guess, Track rescan in an Export worker, source-copy across a color View, arbitrary `-ss` packet trim, keyframe flag treated as closed-GOP proof, remux success treated as packet identity, Smart Render failure treated as job failure when the complete render path remains valid

**Project Color Environment**:
The one persisted, exact Mondrian Standard package, ACES preset, or Custom OCIO engine identity shared by the future-Sequence template and every Sequence in one Project. A Sequence has no engine, override, or inheritance state.
_Avoid_: Sequence color engine, active-Sequence engine, implicit process-global OCIO default

**Sequence Color Settings**:
The Sequence-owned working domain, media-input policy, and Program Output policy, represented as `working_color_space`, `input`, and `program_output`. Encoded bit depth/range and authored HDR payloads are separate Sequence Delivery Defaults.
_Avoid_: One color-management bag mixing engine, input, Program Output, monitor, and codec representation

**Scene-linear Gamut Repair**:
Definition-backed point grades that execute before Program Output in the exact Sequence working domain. Gamut Compression is the fixed ACES 1.3 Reference Gamut Compression evaluated in AP1 with explicit working/AP1 matrices and an authored amount. Highlight Recovery preserves working-space CIE Y while reducing above-threshold clipped-channel chroma; it does not reconstruct missing RAW or spatial detail. Both retain alpha and extended Float32 values and share one compiled Preview/Export/GPU semantic path.
_Avoid_: Output-gamut clamp presented as ACES compression, monitor-dependent grade math, RGB8-only recovery, hidden working-space inference, claiming point RGB math restores lost RAW detail

**HDR Zone Grade**:
The Definition-backed `builtin.hdr_grading` point grade evaluated in scene-linear stops relative to 18% grey. Global controls plus Blacks, Dark, Shadows, Light, Highlights, and Specular each author exposure, saturation, and chroma balance; zone center and width remain explicit. Effects compiles one immutable 512-sample, two-row Float32 table per evaluated author state and working space. CPU and GPU sample that same table, preserve straight Alpha and extended values, and never interpret the grade in monitor/output code space.
_Avoid_: Display-nit sliders inside working-space math, six per-pixel zone branches, backend-specific zone formulas, clipping negative or HDR values, serializing the derived sample table

**Grade Graph Author State**:
The Core-owned, persistable, bounded creative-grading DAG containing one Input,
one selected Output, ordinary Effect nodes, and explicit Parallel or Layer
composition. Each graph belongs to one named Grade Version inside a
Sequence-owned Grade Definition. Creating a Version copies values once while
forking node, Effect, and automation identities; activating a Version changes
only the active strong reference. Effects is the sole Module that lowers this
author state into a `CompiledEffectGraph`.
_Avoid_: Persisted execution schedule, renderer-owned grading graph, copied graph on version switch, cross-version node identity aliasing

**Grade Hierarchy**:
The exact Sequence picture order `Group Pre -> Clip Effects/Masks -> Clip Grade
-> Group Post -> Track Composite -> Timeline Grade`. Clip, Group Pre/Post, and
Timeline scopes retain typed references to Sequence-owned Grade Definitions;
referencing one Definition from several scopes is Shared Grade. Timeline Grade
is one explicit full-composite element evaluated after all Track compositing,
never a synthetic Clip.
_Avoid_: UI-defined ordering, Timeline Grade per Clip, copied Shared Grade state, output/display transform treated as creative grade

**Project Gallery Still**:
A portable Project-owned frozen comparison reference containing one bounded sRGB PNG, source Sequence/time, active Grade Version bindings, the capture presentation fingerprint, and bounded working-linear distribution statistics. The raster is authored reference media inside canonical `project.json`, not a machine-local Preview cache; archive admission decodes it and proves the declared extent. Deleting the still does not invalidate Shot Match evidence already embedded in a Grade Version.
_Avoid_: Viewer-current pixels as persistent authority, unvalidated PNG blobs, cache paths in `.mdp`, live Grade references presented as a frozen version

**Shot Match**:
The deterministic renderer analysis and App authoring workflow that samples a bounded straight-alpha working-linear frame, derives per-channel 5/50/95 percentiles, solves a clamped Gain/Offset recipe, and commits a fresh Grade Version with complete evidence and fresh graph identities. It is an auditable statistical match, not an AI, semantic, or perceptual-equivalence claim.
_Avoid_: Matching encoded Viewer pixels, mutating the active Version in place, shared cross-Version node identities, nondeterministic full-frame analysis

**Gallery Comparison Session**:
The transient App/Viewer state selecting one frozen Gallery still plus None, Wipe, or Split layout. The reference PNG is decoded once for painting over either a CPU raster or current external GPU texture; capture requests an exact CPU Viewer result when no evidence-bearing CPU output exists. Comparison never mutates Timeline pixels, Scopes inputs, Program Output, or Project state beyond explicit Gallery author actions.
_Avoid_: Persisted divider position, UI-thread full-frame compositing, GPU surface availability treated as working-linear capture evidence, comparison altering render/cache identity

**Custom OCIO Dynamic Properties**:
The canonical, persisted Project-engine overrides for OCIO Exposure, Contrast, Gamma, Grading Primary, Grading Tone, Grading RGB Curve, and all eight Grading Hue Curve families. Values are strictly typed and validated against the selected executable route. Static config/processor/shader/LUT identity excludes only the changing payload; CPU applies it on the processor owner thread and GPU updates the resident uniform buffer without rebuilding static backend objects. Result/cache identities still include the complete evaluated values.
_Avoid_: Free-form vendor properties, duplicate property kinds, dynamic values folded into config reload identity, stale resident uniforms, route-missing properties silently ignored

**OCIO Engine Artifact Registry**:
The bounded process-lifetime Module behind all Preview, Export, Thumbnail, and historical-snapshot color owners. It keys immutable parent Processor graphs by the exact static Project engine and route, builds each cold key once under OCIO's process-global config lease, and keys device-independent GPU shader/LUT/binding/uniform plans by the complete request including dynamic values. Per-key single-flight cells never retain failures. CPU execution handles, Dynamic Properties, concrete wgpu objects, mutable uniform buffers, frame tables, and failure state remain owner/device-local. Every loaded Config enables graph caching while explicitly disabling OCIO dynamic-property sharing.
_Avoid_: Per-frame current-config selection, cache revision bolted onto complete engine identity, shared CPU DynamicProperty state, process-global wgpu objects, failed single-flight poison, unbounded cross-Project artifact retention

**Viewer Display Policy**:
The validated machine-local user preference applied only after the engine-owned Program Output: one monitor-output intent, optional final ICC device calibration plus rendering intent, and an SDR/PQ/HLG presentation policy. An exact OCIO display/view is admitted only when the active Project Color Environment maps it to a standardized monitor target. The policy is stored in user preferences, never in `.mdp`; actual monitor and surface capability remain Window/platform evidence.
_Avoid_: Second creative output View authority, monitor policy in Sequence settings, ICC profile as source interpretation, user-requested HDR treated as swapchain proof, arbitrary relative ICC id/path

**Reference Output Module**:
The machine-local deep execution Module that pairs one canonical full-raster
Program Output with exact 48 kHz Audio Program intervals and one canonical
Ancillary Frame, admits one closed
physical signal, and owns bounded scheduled playout through a DeckLink/AJA
Adapter Seam. It binds device authority to exact Sequence revision and Project
author generation. Viewer scaling, comparison, monitor/ICC, Scopes, warnings,
Monitor Path PCM, Export publication, device routing preferences, and vendor
ABI handles are outside the clean-feed semantic payload. A simulated Adapter
can prove Module behavior only and permanently carries non-hardware evidence.
For qualification, every open-scoped provider resource transfers into the
Session and the entire Module—including its Adapter/bridge root—is consumed on
one absolute-deadline coordinator; only a joined coordinator plus clean
schema-versioned Session facts proves release.
_Avoid_: Viewer window as SDI output, Project-owned device ID, implicit device conversion, per-frame rounded audio, independently scheduled ANC, fake device when SDK is absent, simulated output presented as hardware qualification

**Broadcast QC Profile**:
The immutable versioned contract consumed by the Broadcast QC Module: source-document hash, encoded-signal identity, exact pre/post-Legalizer observation tap, active picture, ordered thresholds/severities, bounded evidence capacity, and explicit external obligations. Export scans contiguous real delivery pictures before encoder consumption and blocks publication on `Fail` or `Incomplete`; a finished-file re-decode, approved PSE/BT.1702 result, and broadcaster qualification remain separate evidence.
_Avoid_: Generic hard-coded broadcaster thresholds, flash candidates presented as certification, FFmpeg probe presented as content re-scan, Smart Render bypassing requested QC

**Ancillary Frame**:
One frame-addressed, canonical, collision-safe, bounded inventory of ST 291 Type 2 packets. The Broadcast Module alone owns ADF/parity/checksum and registered ATC/AFD/CDP transport lowering; Reference Output atomically schedules the inventory with the exact picture and Audio Program interval. Caption text authoring, CEA semantics, ST 436 carriage, and physical line/field readback remain explicit unqualified Adapters.
_Avoid_: Display timecode as ATC authority, Basic Title inferred as captions, raw caller words treated as validated packets, ANC scheduled outside the video/audio bundle

**Professional Scopes Controls**:
The versioned machine-local Viewer preference selecting Luma or RGB Parade, encoded IRE or transfer-decoded absolute-nits aggregation, exact Program Output or pre-ICC Monitor Output tap, skin-tone line, 75% color targets, and overview/grid/single-scope layout. Waveform mode, scale, and tap form the GPU analysis identity; guides and layout are presentation-only. A hidden Scopes panel performs no aggregation, and none of these controls enter `.mdp` author state or render-cache identity.
_Avoid_: Nits as relabelled code values, implicit tap changes on monitor movement, scopes after ICC calibration, layout invalidating analysis, hidden-panel GPU work

**Signal Compliance**:
The Core-owned normalized encoded-RGB contract shared by non-destructive Viewer false color, zebra, and gamut alarms and by explicit destructive Export legalization. Viewer warnings classify the selected Program Output or pre-ICC Monitor Output tap, color the monitor-adapted presentation in one fused CPU/GPU operation, preserve Alpha, and remain machine-local. Export `ClampRgb` is frozen in the immutable delivery preset, clamps only the final encoded-float root deliverable before its one integer/YUV quantization, and disables Smart Render. Delivery `VideoRange::Legal` controls code-value mapping and is not a pixel legalizer.
_Avoid_: Monitoring in Project/cache identity, warning colors in Program Output or exports, clipping working-linear pixels, legal range treated as clipping, legalization after quantization, nested legalization

**Program Color Context**:
The closed, immutable resolved Project engine plus one Sequence's working and Program Output semantics. Its fields are private and its output is exactly one of an encoded endpoint or a parent-working-space handoff. Root, nested, explicit Export, and rendering-View derivation are the only construction Seams; each validates engine/working/output/intent identity and derives tone-map state from whether the selected transform actually resolves a rendering View. A nested context always ends in its parent's working space, is colorimetric, and cannot be promoted into an encoded delivery context. It excludes per-media input tone mapping and machine-local monitor adaptation; the Sequence missing-metadata policy is carried only to derive explicit Media Input Color Contexts during nested Timeline traversal.
_Avoid_: Public field bag, caller-mutated output identity, nested context used as delivery authority, Viewer display target as source interpretation, export codec settings as Program Output, one tone-map boolean reused for input and output

**Media Input Color Context**:
The exact Project engine, effective working domain, Sequence missing-metadata policy, and per-contribution input-tone-map decision used to transform one media contribution into working pixels. It has no Program Output or monitor target.
_Avoid_: Display ICC profile in source cache identity, Program Output tone map reused as input tone map, parent Sequence replacing nested-child media interpretation

**Export Color Target**:
The admitted post-composite output intent for one deliverable: follow Sequence Program Output, direct colorimetric conversion to an explicit encoded space, or the Project engine's rendering View for an explicit display target. It is independent from codec, bit depth, range, and chroma.
_Avoid_: Making a Sequence Program Output illegal to request Camera Log, inferring a View from codec name, relabeling unchanged pixels

**Export Execution Service**:
The instance-owned offline Module that admits immutable Timeline Export Snapshots into a bounded dedicated queue, reserves final output identities, owns attempt generations and cancellation, consumes each heavy payload once, and exposes only bounded lightweight lifecycle/evidence snapshots. It shares cross-domain execution value semantics but not worker capacity with Preview, Thumbnail, Waveform, Proxy, or realtime audio.
_Avoid_: Process-global queue, universal media worker pool, UI-mutated job status, unbounded terminal Project retention, enqueue success after worker failure

**Professional Delivery Contract**:
One exact constrained IMF RDD 45, AS-11 X9, or SMPTE DCP product row frozen in
the ordinary Export Snapshot. The Export-owned deep Module alone lowers it to
wrapped essence, metadata, CPL/PKL/AssetMap where applicable, profile-specific
independent reimport, and identity-bound durable publication. Tool discovery
is profile-local and fail-closed; author metadata remains typed, while raster,
cadence, signal, audio, and color endpoints must exactly match the row.
_Avoid_: Generic “IMF-compatible” toggle, invented AS-11 metadata framework, placeholder RegXML, self-generated XML as its own conformance oracle, package files written directly to the final route, external-tool success treated as durable publication

**Export Deliverable Publication**:
The typed filesystem boundary for one exact admitted absolute output route and frozen namespace policy. Admission requires an existing canonical parent directory. `CreateNew` is the default and atomically preserves any route created during a long export; explicit `OverwriteExisting` is final-publication-time last-writer-wins authority, not admitted-identity compare-and-swap. Export reserves one unique direct sibling object through the shared Storage Adapter, releases only that identity-bound path to FFmpeg, reclaims and proves the same object before validation, then publishes under that frozen policy. `Completed`/`Published` require durable publication evidence; pre-namespace failure retains the validated partial, a target proven to name the new object without confirmed crash durability is `DurabilityUnconfirmed`, and an unprovable namespace postcondition retains `NamespaceIndeterminate` artifact evidence projected to queue state `OutcomeUnknown`.
_Avoid_: FFmpeg writing the final path directly, path-only partial ownership, deleting a validated partial after pre-namespace failure, private ReplaceFile/rename logic, validation success treated as publication success, late cancellation overriding typed publication evidence

**Playback Quality Policy**:
The allowed temporary preview resolution and user-selected proxy/original policy for a Playback Session.
_Avoid_: Quality flag

**Playback Evidence**:
Structured events and aggregates proving clock, scheduling, delivery, degradation, synchronization, and recovery behavior.
_Avoid_: Debug log

**Product Process Tree Memory Evidence**:
Bounded fixed-cadence observations over the Mondrian root process and every descendant process. A complete sample requires two matching OS inventories, stable PID plus process-start identity, every member query, and checked aggregation after bounded retries; PID reuse, exit/topology races, inaccessible members, and overflow fail the whole sample. The typed ownership metric is platform-native and non-interchangeable: Windows Private Commit, Linux anonymous resident memory (`RssAnon`), or macOS physical footprint. Current/peak resident values are diagnostics; macOS does not manufacture an unavailable per-process lifetime peak. Every result carries scope, Backend, metric, observed process count, inventory attempts, and completeness. Product resource policy may consume a complete platform-native footprint, while a versioned Windows qualification profile must require Windows Private Commit explicitly rather than relabel another metric.
_Avoid_: A `CurrentProcess` sample presented as whole-product memory, frame-cache totals presented as process memory, a global child-PID registry, one start/end sample, OS peak since process launch presented as gate-local growth, missing-child/query failure treated as zero

**Authoring Process Memory Evidence**:
The schema-10 authoring-matrix memory stream. It takes three valid Private Commit baselines before fixture construction, samples scalar process facts at an approximately 2 ms cadence through fixture construction, measured operations, and the History-depth probe, stops that sampler, and then takes one final sample. Enforced Windows runs fail closed unless this evidence is complete and the observed Private Commit peak satisfies both the scale/profile delta budget and the 4 GiB absolute peak limit. Working Set and OS process-lifetime peak resident bytes are diagnostics only; deterministic History logical charge remains a separate author-model budget.
_Avoid_: Starting after fixture construction, passing on a missing native probe, gating on Working Set, subtracting OS process-lifetime peak, presenting History charge as process memory

**Playback Memory Class**:
A versioned qualification of physically installed memory for one validation workload: `minimum-supported` (8 GiB correctness, boundedness, and explicit fallback), `standard-playback` (16 GiB M0 Video+Audio baseline), or `professional-large-project` (32 GiB large-project recommendation). OS-visible memory is separate evidence and cannot silently change the class.
_Avoid_: One universal RAM threshold, treating 8 GiB as native-4K performance proof, rejecting nominal memory because firmware reserved a small region, diagnostic override presented as baseline

**Professional Playback Acceptance**:
A versioned fail-closed contract that binds decoder-proven media identity to frame-local decode provenance, the exact Viewer candidate for a Frame Demand, and completed GPU presentation evidence.
_Avoid_: Filename-based fixture label, capability-only hardware claim

**Reference Playback Run**:
One immutable evidence bundle binding a versioned gate plan, source revision and cleanliness, an operator-assigned non-hardware machine identity, the required and observed Playback Memory Class plus validated machine report, corpus revision, generated-recipe identities, run-local artifact hashes, exact gate commands, and structured execution reports. A partial, dirty, or explicitly under-class run may diagnose behavior but cannot become a baseline.
_Avoid_: Loose log directory, filename-selected media, mutable “latest” fixture, machine serial capture, successful process exit without a passing execution report

**Reference Playback Gate Supervisor**:
The external validation Module that gives each gate a versioned wall-clock deadline, drains both child output pipes, terminates the complete descendant process tree on timeout, and preserves the log plus any independently flushed progress journal before classifying the gate. A timeout is terminal evidence and can never be represented as a passing or merely missing execution report.
_Avoid_: Test-internal timeout as the only process bound, synchronous `cargo` invocation, killing only the immediate child, absent report treated as an ordinary assertion failure

**Golden Acceptance Plan**:
The deterministic compiled ledger that maps every top-level Golden fixture, operation, content, and export obligation to declared executable slices, while identifying obligations that are unplanned or still lack a bound fixture. Structural plan completeness is never execution evidence.
_Avoid_: Inferring coverage from test names, unioning only passing reports, treating a declared slice as observed work

**Golden Project Run**:
One supervised execution against one Project identity that carries the complete top-level Golden workflow and typed postcondition evidence through authoring, playback, persistence, export, reimport, and recovery. Only a structurally complete plan plus all required observations in the same run can qualify; release requires the contract's consecutive passing-run count.
_Avoid_: Combining unrelated temporary Projects, promoting one passing slice to top-level success, counting retries within one run as consecutive runs

**Golden Product Workflow Driver**:
The UI-independent Headless owner of one production App composition, immutable persisted Project ID/path binding, and all Golden stage Sequences for one run. It drives ordinary product Interfaces, verifies the binding at every stage and reopen boundary, and cannot combine separately created Projects.
_Avoid_: Slice-local AppState presented as a complete run, report union as Project identity, test-only document mutation

**Preview Frame Store**:
The bounded runtime store and physical allocation ledger for decoded Preview payloads, including CPU pixels or opaque native decoder resources, UI-independent final Preview rasters, failure memory, and the explicitly pinned current/stale Preview raster. Each newly admitted Broker attempt owns one move-only work lease; repeated evaluation first performs an atomic same-key, same-reservation-scope bind and therefore cannot request a second charge for an already queued or in-flight producer. Successful decode consumes its lease into one cloneable frame-allocation lease carried by every external frame owner, while the cache payload itself remains lease-free. Optional cache residency is trim-responsive. Exact current-frame execution has a separate untrimmed machine-class per-demand grant through a bounded multi-key LRU overflow, while the component-wise maximum of optional and per-demand limits is the global aggregate hard grant across all physical owners; neither is an unbounded oversize exception. Demand-scoped protection leases count each allocation once for their typed demand and keep exact inputs alive through asynchronous visual/GPU candidate execution. The ledger aggregates every queued, in-flight, completed-but-unconsumed, Store-owned, protected, and externally retained allocation by entry, host-byte, and decoder-resource-unit charge. Decoded-media identity contains a complete source fingerprint, physical video stream, exact source time, geometry, and complete output/color contract; incomplete revision evidence cannot enter residency, and a new Authoring Session clears Store ownership before durable IDs can be reused. After stopped transport has a registered GPU Viewer output proved for its exact Preview generation and all work is quiescent, Store-owned media may be released independently of that output and failure memory; external/in-flight leases remain charged until their real owners drop. Widget payloads are constructed only by the final Window Presentation Adapter and are never cache state.
_Avoid_: Semantic-key counters as physical ownership, one shared lease for duplicate attempts, entry-count-only cache, treating zero-host-byte native surfaces as free, trim shrinking the current correctness grant, unbounded oversize pins, clearing external work by reconciliation, clearing media before final output is usable, retaining an idle hardware surface pool, retaining `ViewerFrameImage` in the Store

**Color Frame Contract**:
The typed per-frame identity of extent, render-graph domain, color or working-space identity, sample encoding, CPU/GPU residency, and RGB/coverage alpha association. Public working frames carry straight or opaque coverage; premultiplied frames exist only inside an explicitly typed spatial operation and may not cross OCIO, effect, composite, display, or export seams.
Timeline compositing validates every Media/Transition endpoint against the exact Sequence working-space contract before optimization or pixels. A source-only identity may share immutable storage only after Effect execution admission and only when background, extent, opacity, blend, transform, domain, encoding, residency, alpha, and storage extent are all exact; execution evidence, not layer shape, proves that zero-copy route.
_Avoid_: Bare RGBA buffer, texture format as color identity, shader-only premultiplied flag, alpha inferred from codec or pixel values, relabeling one working space as another, inferred passthrough without execution evidence

**Independent Colorimetric Oracle Corpus**:
The digest-pinned validation package of public-specification SDR/PQ/HLG transfer targets, legal/full-range and Alpha codes, 4:2:0/4:2:2 chroma sample geometry, and published CIEDE2000/Delta E ITP vectors. Its binary64 equations live only in the validation target and never call the production OCIO transform path. Production boundaries consume the fixed targets through distribution-aware Delta E and code-value reports; Mondrian-generated goldens and CPU/GPU parity remain regression evidence, not absolute colorimetric authority.
_Avoid_: Production CPU OCIO output used as an absolute GPU oracle, mutable screenshots without provenance, one maximum-delta assertion, Alpha folded into perceptual RGB error, a Mondrian golden promoted to an external reference

**Cross-Application Color Qualification**:
The Renderer-owned, fail-closed qualification of one digest-pinned analytic stimulus against exact-version Mondrian, Blender, DaVinci Resolve, and Adobe Premiere Pro artifacts. A compiled profile fixes the declared producer/case closure, color encoding, raster, Alpha, rational frame coordinate, metric, tolerance, and resource bounds. One non-spliceable clean-source run binds executable, native Project, actual render settings, acquisition Adapter, decoded metadata, operator attestation, payload, and machine-report hashes; the Module performs every available pairwise comparison and emits one deterministic self-verifying report. Missing evidence is `incomplete`, never pass/skip. Cross-application agreement remains parity evidence and never replaces the Independent Colorimetric Oracle Corpus or Viewer/reference-monitor hardware qualification.
_Avoid_: Self-labeling Mondrian output as an independent application, merging application versions or runs, screenshots as pixel evidence, decoder-applied hidden color conversion, sRGB math reused for Display P3, PQ math reused for HLG, native creative tone mappers required to match each other

**Platform / Driver / Display Qualification Matrix**:
The platform-core-owned, fail-closed qualification of an explicit finite set of Windows/DX12, macOS/Metal, and Linux/Vulkan physical-display rows. One row is an atomic serial run on one machine, exact clean source revision, exact target-specific package and actually executed runtime-image SHA/build provenance, active hardware adapter and platform-correct driver identity, compositor/display/ICC/HDR state, unique run ID, executable owner-verifier contracts for platform/GPU/Viewer receipts, bounded raw-evidence hashes, and identical before/after environment identity. The matrix may aggregate independently sealed rows from multiple machines only when canonical profile, source, release-candidate, and cross-target build-manifest identities match; each row retains its own Windows/macOS/Linux artifact SHA. Exact row closure and platform-level SDR sRGB, Display P3, PQ-program HDR, and managed ICC coverage are required; macOS proves PQ-program presentation through its linear EDR carrier rather than claiming a native PQ link. The final create-only bundle contains every admitted authority, row seal, package, runtime image, provenance, machine, normalized report, and raw-evidence file under a byte closure. Its independent verifier requires external source/policy/profile/release/build anchors plus a separately hashed capture-authority manifest, snapshots every closure byte, replays native ICC/EDID facts, exact measured GPU gates, the Core display-contract identity, every owner verifier, and the Matrix evaluator, then rehashes the original bundle before success. The authority approves exact capture bytes and operator identity outside the bundle; this remains an HITL provenance boundary. Missing evidence is `incomplete`, executed failure is `failed`, and neither hosted CI nor EDID capability can become physical qualification.
_Avoid_: Cross-machine evidence spliced inside one row, package hashes relabeled onto another running executable, bundle-owned trust anchors, Windows-style optional driver strings on macOS/Linux, native PQ facts invented for macOS EDR, WARP/llvmpipe qualification, DRM capability presented as active compositor HDR, display serials/raw ICC in public reports, green compilation promoted to hardware support

**Sealed GPU Color Qualification**:
The fail-closed Windows real-device gate bound to one clean source SHA, operator-assigned reference-machine identity, captured GPU/driver inventory, exact wgpu DX12 discrete-adapter fingerprint, and a versioned accuracy/performance gate set. Development tests may diagnose absent hardware, but the `sealed-required` policy converts unavailable adapters or timestamp capabilities into terminal failures. An external bounded supervisor proves exactly one executed test per gate, forbids skipped reports, hashes logs/reports, requires one adapter across all evidence, and adds the result to Commercial Engine release evidence.
_Avoid_: Green CI caused by `return` after adapter failure, an ignored performance test never invoked, WARP/software adapters used as qualification, mismatched GPU reports unioned into one run, unsealed local output promoted to release evidence

**Export Pipe Frame Contract**:
The Export-owned exact storage representation crossing an encoder frame Seam. Encoded RGBA8/16 UNORM and true Float16/Float32 master samples are closed, distinct variants carrying their actual FFmpeg pixel format, interleaved or planar layout, byte width, renderer boundary precision, clamp policy, and packing rules. Current 8/10/12-bit video codecs select only encoded UNORM staging; a renderer `Rgba16Float` intermediate never reclassifies `rgba64le` bytes as half float. UNORM serialization validates finite input, clamps once to `[0, 1]`, and quantizes; float serialization validates finite/range constraints, preserves extended-range RGB without clamp, requires normalized finite coverage Alpha, and uses FFmpeg's real `rgbaf16le` or planar G/B/R/A `gbrapf32le` layout. Native Float32 TIFF consumes the same planar contract through an exact lossless interleaving Adapter, never an integer inspection boundary.
_Avoid_: Naming UNORM16 transport Float16, deriving pipe scalar type from GPU texture format, interleaving a planar FFmpeg format, silently mapping NaN/Inf, clamping true float masters, expanding RGBA8 into a nominal high-bit pipe

**Image Sequence Master Module**:
The Export-owned deep Module that resolves one `ImageSequenceFormat` into the renderer frame contract, exact scalar/channel representation, extension, Alpha capability, encoder Adapter, output pixel format, independent decoder/header validation, durable per-frame content inventory, and whole-directory publication. PNG8/16, OpenEXR Half/Float32, DPX16, TIFF16, and TIFF Float32 are explicit product representations; generic `DeliveryBitDepth` remains the 8/10/12-bit video-codec setting and cannot stand in for half/float identity. FFmpeg image2 owns all formats except native Float32 TIFF. OpenEXR Half deliberately crosses the encoder Seam as planar Float32 and quantizes exactly once inside the EXR encoder, because the packaged FFmpeg's packed Float16 conversion clamps extended-range RGB; independent decode proves the published channels are truly Half and retain negative/greater-than-one values. Every frame is decoded and its true sample representation proved before manifest schema 2 is durably written and `OwnedPublicationDirectory` crosses the create-new namespace Seam.
_Avoid_: Extension inference as format authority, a nominal high-bit preset backed by RGBA8, treating decode success as Half/Float proof, DPX Alpha claims without a contract, float legalizer clamp, publishing a partially encoded directory, modeling an input folder as one StillImage

**Professional Mezzanine Codec Module**:
The Export-owned deep Module that resolves DNxHR, AVC-Intra, and uncompressed RGB/YUV authoring into one exact encoder, container, raster/cadence, depth, chroma, range, pixel format, coding structure, finished-output identity, and frontend default contract. DNxHR LB/SQ/HQ are 8-bit 4:2:2; HQX is 10-bit 4:2:2; 444 is 10-bit RGB. AVC-Intra Class 100/200 are software-only 1920x1080 square-pixel progressive 23.976–30 fps, with the built-in Class 100 preset fixed to video-only MXF 1080p25 because the current MXF Adapter does not preserve independently verifiable PCM channel-layout identity. MOV uncompressed 2vuy/v210/raw RGB/r210 are exact 8/10-bit representations. Runtime qualification checks encoder options/pixel formats, every row completes a real encode and independent re-probe, codec tags/levels are validated where the container exposes them, and the media Adapter reimports DNxHR and uncompressed identities exactly. XAVC and AVC-Intra Class 50 remain unrepresented: generic H.264-in-MXF is not an XAVC contract, and Class 50 needs a preset-owned anamorphic SAR model.
_Avoid_: Generic all-I H.264 labeled XAVC, encoder registry treated as conformance, hardware H.264 substituted for AVC-Intra, raw pipe transport confused with uncompressed output essence, arbitrary codec/profile/signal combinations, missing MXF audio layout guessed from channel count

**Float Coverage Algebra**:
The Effects-owned canonical Float32 policy used by source normalization, Effect joins and spatial operations, Timeline composition, Transitions, temporal interpolation, Viewer spatial filtering, and the GPU compositor. Normalized coverage and opacity have exactly one transparent identity: non-positive zero after clamping. Every positive Float32 value, including one 16-bit UNORM code, contributes continuously. Premultiplied-to-straight conversion and weighted interpolation canonicalize hidden RGB only when output coverage is exactly zero; scheduling and fast paths use the same predicate as pixel execution. WGSL mirrors this policy and is qualified against the independent CPU/f64 corpus on a real adapter.
_Avoid_: Epsilon used as semantic transparency, low opacity culled only by Preview planning, CPU/GPU threshold drift, hidden RGB retained at exact zero, scalar type used as an Alpha association

**GPU Working Float Policy**:
The Renderer-owned fail-closed Module that separates unbounded scene-linear project identity from transient RGBA16F/RGBA32F GPU storage. Viewer, Viewer spatial, the GPU Visual Frame Module, nested Export working transforms, and their active-resource estimates consume one immutable decision and verify actual working handles against it. RGBA16F requires an independently sealed end-to-end quality report, a same-device/same-suite report proving lower p95 GPU time and active bytes, and complete compositor, Effects, AlphaMask, heterogeneous, and Viewer/Export qualification; absent, mismatched, failed, or non-beneficial evidence selects RGBA32F. Current production remains RGBA32F because Effects GPU and heterogeneous execution are Float32-only. Program/monitor output carriers, encoded source textures, deliverable pipes, LUTs, DataTextures, AlphaMasks, and the lossless Render Cache retain separate typed formats.
_Avoid_: Memory pressure as permission to lower precision, Viewer and Export choosing independently, bytes-per-pixel constants detached from execution, encoded RGBA16F presented as working-path qualification, policy selection without actual-output verification

**Source Frame Preparation**:
The Renderer-owned Module that consumes one typed decoded CPU RGBA payload, its exact Alpha Interpretation, and one lowered preparation intent. It validates extent and encoded/linear/data identity, normalizes coverage once, and atomically produces either a color-managed source bound to one immutable input transform or a DataTexture working frame that preserves normalized numeric RGB(A) while executing zero OCIO stages. Preview and Export select the intent but cannot reinterpret the Media-owned payload contract. DataTexture requires proven CPU-addressable RGB sampling; YCbCr, native-surface, compact-YUV, and proxy paths fail closed until they can prove numeric-channel preservation.
_Avoid_: Preview and Export independently matching decoded float encoding, scalar type treated as proof of scene linearity, DataTexture treated as Rejected or assigned a color identity, YCbCr matrix conversion of technical channels, caller-selected source color replacing the decoded frame contract, Alpha normalization after OCIO

**Color Execution Modules**:
The Renderer-owned four-Module Interface separating source interpretation, working-identity conversion, Program Output, and monitor presentation. Product callers supply `MediaInputColorContext` or `ProgramColorContext`; they cannot construct generic stage graphs, mutate Program Output fields, or own shader/backend/resource-table caches. CPU execution reuses one owner-scoped color Session. GPU execution reuses one `GpuColorExecutionSession` and exact texture pool across all four semantic Modules; it is not four runtimes. Program Output and monitor remain distinct stages even when one command stream or owned Float32 raster eliminates an intermediate copy and performs only one final quantization. Low-level stage IR, planners, backend objects, frame identities, and materialization records are private Implementation; an explicitly hidden hardware-qualification Adapter is the only non-product exception.
_Avoid_: Generic color-stage IR as App/Export API, caller-filled Program Output field bag, one runtime/cache per semantic stage, Preview/Export reconstructing Timeline color contexts, monitor adaptation replacing Program Output, repeated float-to-integer quantization

**Camera RAW Development Contract**:
The Media-owned, versioned source-development identity for one probe-admitted DNG or single-frame CinemaDNG picture. It binds the proven Adapter, fixed-point exposure and white-balance intent, debayer quality, and exact algorithm revision into Preview, Thumbnail, and Export decode/cache identities. FFmpeg owns TIFF/DNG packet decompression and normalization; Mondrian owns typed CFA evidence, deterministic CPU demosaic, camera white balance/matrix development, and the declared scene-linear Rec.709 Float32 output consumed by the ordinary Renderer Source Frame Preparation seam. RAW always has full-range float semantics, bypasses generic RGB swscale, and cannot use proxy, compact-YUV, or native-surface routes in the current slice.
_Avoid_: Extension-only RAW admission, renderer-owned debayer, Preview/Export math forks, persisted float controls, generic FFmpeg RGB presented as controllable RAW development, asset range override applied to developed float samples, CinemaDNG folder sequence claims without sequence authoring semantics

**Playback Preview Pump**:
The UI-independent App Module coordinator that samples one pending Frame Demand and current transport activity, applies bounded preview-work completions and expirations, submits their exact terminal Frame Deliveries, then observes video preroll bound to a freshly captured exact active Frame Demand in that order.
_Avoid_: Window-owned result policy, Headless-only orchestration, separately sampled demand identities for completion and expiration

**Headless Realtime Playback Session**:

Startup failures retain the exact Preview and GPU inventory outside constructor/binder unwind boundaries until consuming shutdown. Partial GPU inventory records whether a Renderer was actually created; complete Adapters retain their whole lifecycle envelope. Endurance stores an owning failure before propagating its error and returns an independent Startup terminal receipt under the same absolute Preview → GPU → optional Waveform → App deadline. Failed startup cannot become qualified playback even when cleanup is clean. Waveform now prepares an unpublished complete owner before installing its SourceCache and analysis worker; its owning startup Interface retains the exact Prepared, SourceCache, or AnalysisWorker stage and reuses normal consuming shutdown. SourceCache prepares non-worker state before constructing its concrete decoder and never calls erased initialization hooks after that decoder returns. Normal Waveform qualification still requires one started/joined analysis worker; a partial startup receipt is not a normal-runtime receipt. Preview now prepares a complete unpublished Runtime and installs native owners in place; its owning failure Interface returns exact per-owner construction states and reuses the normal consuming shutdown under the caller's original deadline. Headless, Golden and performance callers retain partial and complete Preview owners exclusively. Partial schema-1 receipts bind individual worker outcomes to the unchanged normal schema-4 aggregate and are serialized separately from normal qualification receipts. Any in-progress native factory that unwinds without returning its inventory remains explicitly unverified; opaque panic payload abandonment also fails closure. Preview schema-4 shutdown requires the real visual-dependency observer join and complete callback-owner receipt in addition to existing workers and cache evidence.

The validation/test-only App composition that permanently pairs one production Preview Runtime with the exact Headless GPU Adapter/device generation supplying its completion waker and hardware-decode admission. Each declared realtime observation opens one fresh private driver/native-scheduling residency, executes the sole Clock advance, Preview pump, exact candidate reconciliation, bounded successor/lookahead, and revision/deadline wait ordering, then closes and resets that residency before settled work or owner shutdown; production A/V intervals additionally pump Audio before Clock, while video-only intervals do not fabricate an Audio stage. It distinguishes queue-visible publication from callback retirement and permits at most one same-turn reconciliation after a completion drain. Within admitted realtime intervals and terminal-opportunity closure, performance and endurance owners may observe facts but cannot reconstruct the driver state machine; settled/preflight helpers remain separate bounded phases over the shared candidate primitives.
_Avoid_: Perf-only playback loop, separately paired Preview/GPU generations, native playback priority outside an active coordinator window, callback completion treated as visibility, demand identity reduced to a frame number, or qualification counters mutating execution order

**Persistent Timeline Playback Phase**:
The validation-only phase owner that freezes one active Sequence identity, revision, Project Author Generation, exact 60/1 Evaluation Grid, and frame-zero stopped transport before starting the App's production Playback path. It pumps only the shared Headless Realtime Playback Session and the App-owned physical Audio output, accepting an interval only when it begins at the expected coordinate, advances exactly one frame in the same Playback Epoch, proves the departed exact picture ready, and remains governed by Audio Device Clock. The non-looping fixture must contain the complete required presented-frame extent plus one terminal guard frame; natural end, unavailable picture, skipped frame, clock fallback, author/transport drift, counter overflow, or coordinator failure permanently faults the phase. Cadence capture first leaves native realtime residency, and resume revalidates the unchanged binding and coordinate before opening a fresh residency. Its controlled Seek recovery similarly leaves scheduling, invokes the typed settled Timeline product action, proves a newer Epoch and exact target, resumes the same owners, closes the target's exact Ready demand under Audio Device Clock, and accepts exactly one new accurate-seek evidence observation before returning a sealed operation receipt. This owner proves the persistent product A/V transport path; it is not Reference Output clean-feed evidence.
_Avoid_: Looping a short fixture, counting the terminal frame without a successor boundary, sampling owners during native scheduling residency, recovering from an epoch/author/clock fault, reconstructing the Headless interval loop, caller-authored recovery facts, or relabeling Viewer pixels as canonical Program Output

**Endurance Recovery Operation Receipt**:
A bounded canonical schema-3 JSON receipt sealed inside the App by one real operation owner and embedded unchanged in the campaign ledger with the SHA-256 of its exact UTF-8 bytes. App publication reparses, canonically reserializes, validates the step-specific before/after relationships, exact cycle/step, and operation-ID uniqueness; the independent verifier separately rehashes, reparses, and replays the same fixed property sets and ordered four-step cycle. Seek, Surface/Device Reopen, Export Cancel/Retry, and Cache Pressure each have a production constructor only over facts returned by their completed product owner. Persistent Timeline Seek, frozen Export Cancel/Retry, and Cache Pressure preserve their existing exact operation contracts. Surface/Device Reopen runs only in the real winit Window owner after an actual external-texture Surface present. It revokes publication, consumes the old progress/runtime/lifecycle/native-owner envelope, requires both whole-queue wgpu quiescence and Adapter retirement with a clean joined worker, installs distinct nonzero Surface and Device generations, and seals only after the same Sequence/frame/Program+monitor color/display contract is GPU-composited and presented again. Its nested shutdown and reopened-picture canonical JSON are separately hashed and independently replayed, and the original-presentation picture digest must exactly equal the reopened-picture digest. A low-copy CPU upload may remain honestly `Degraded`; Headless execution, configure-only success, reused generation identity, or a CPU raster cannot qualify the Surface step. Every validation event-loop exit consumes Window Preview and Waveform inside one bounded UI deadline and returns the exact entering `AppState`; the narrow executable then consumes the full App endurance shutdown, while a campaign runtime may resume the same settled Timeline owner. The narrow executable proves this operation locally but does not replace four-step campaign orchestration.
_Avoid_: Caller-supplied digest or success boolean, arbitrary counters promoted to evidence, accepting non-canonical bytes, operation receipt replay, schema support presented as an executed operation, or Headless no-Surface execution presented as Surface reopen

**Endurance Surface Event-Loop Ownership**:
One non-`Send` winit EventLoop is created on the validation binary's main thread and reused on demand across every orthogonal Surface/Device recovery cycle. Each run must destroy all Window, Surface, Device, Queue, callback, and UI-runtime owners before returning; only the exact `AppState` and recovery pump may cross into the next run. The standalone batch runner proves a second same-process cycle does not hit winit's process-level recreation guard. macOS main-thread execution and Linux display-server availability remain native qualification gates.
_Avoid_: Constructing an EventLoop per recovery cycle, moving it to a worker thread, retaining a Window between on-demand runs, or treating a headless Linux display failure as a Continuous Export failure

**Endurance Phase Terminal Evidence**:
The owner-free complete raw Realtime or App-only shutdown receipt retained separately from a phase's optional final sample. The public Product campaign failure boundary transfers it once alongside the primary error and any cleanup failure, including when clean shutdown precedes failed sample/publication work. Next-phase preparation clears the prior error attachment before workload/capture loading without repairing a missing sample or granting admission. Endurance, Perf and Window share the exact GPU receipt and normal-qualification predicate: retirement request, healthy Renderer and worker closure, and no generation terminal are required. Lost/destroyed devices or joined upload panics can be physically safe to release while failing qualification. Partial constructor inventories and public raw success reporting remain separate work.

**Product Endurance Campaign Runtime**:
The validation-only concrete owner that composes each checked phase from one fresh `AppState`, one side-effect-free pre-start machine inventory, and strong Reference/Export/seek inputs. Pre-start facts describe declared reachable Timeline/Export bindings, device/provider/mode discovery, signal preflight, pinned tools, and prepared operation owners; they never claim an open Session, a live-App-validated fixture, or continuous lock. Missing declarations may yield typed `NotRun`; once the one-use token is issued, exact Project install and source/dependency preparation happen in the fresh App, and semantic failure is a hard setup failure rather than `NotRun`. The public campaign entrypoint accepts only a prepared machine-factory wrapper that installs and retains the exact FFmpeg closure before factory construction and revalidates it at every phase boundary. The runtime alone converts a complete inventory into an opaque phase/workload-bound one-use factory token and retains the prepared machine plan in one `Arc`; a Ready phase must return that exact Arc with a live-App-validated `PreparedEnduranceSourceInventory`, not a digest-equal clone. Project/source/preset leases, including any prepared before later setup failure, remain owned until consuming phase/App shutdown completes. After owner creation, Reference start must drain live provider status and prove the exact physical provider/device generation, positive external lock, zero lock loss, and Running state before `Started` can be returned; later samples re-prove continuous hardware/lock state. It retains partial startup owners for exactly-once cleanup, keeps realtime settled across coordinator snapshots, pumps Timeline A/V before Reference and Export, executes Seek → real Window Surface/Device reopen on the same returned App owner → Export cancel/retry completion → Cache Pressure, and drains/drops phase-local `Arc` owners before consuming Headless execution plus App under one deadline. Its terminal sample replaces live diagnostics only with owner-derived post-join Export, Reference, and Headless facts. A machine factory or profile cannot self-assert missing physical capabilities, and the future thin executable may call this runtime but cannot reinterpret it.
The validation-only Continuous Export machine factory is constructed only from the prepared FFmpeg receipt. Pre-start checks only direct declared Project/inventory/preset/QC paths plus the create-only output directory; build creates a fresh App, installs the exact Project, prepares its complete source authority, and projects only retained typed preset/QC plus machine-plan Sequence/range/route/verifier bounds into `FrozenRepeatedExportRequest`. It does not walk Track/Clip state or reopen contract files. Playback/Reference and Concurrent Recovery remain typed `NotRun` until a physical Audio/Reference/external-lock factory is composed.
The validation-only `mondrian-endurance` binary is the single strict high-level runner: one prepared request, exact FFmpeg-backed factory, process-monotonic clock, native product-process-tree memory probe, and coordinator-owned create-only manifest publication. Its `--self-test` ends after request/toolchain preflight and writes a create-only report with `qualifying: false`; it never creates an App or claims phase, physical-output, or duration evidence. With the current Continuous Export-only factory, formal runs necessarily retain typed `NotRun` for physical phases and cannot form a complete physical qualification.
_Avoid_: Creating App before `NotRun` admission, describing an unopened provider as open, promoting signal preflight to continuous lock, relabelling a dynamic start failure as `NotRun`, fabricating or retargeting the factory token, losing App on a failed Surface result, taking a snapshot while realtime scheduling is active, advancing to Cache Pressure before the Export retry receipt, renewing shutdown deadlines, caching pre-shutdown Export gauges as terminal, or reusing one consumed App across serial phases

**Persistent Reference Output Pump**:
The validation-only phase owner that freezes exact Sequence ID/revision, Project Author Generation, one exact-source Timeline Export snapshot, and one shared Timeline/physical cadence phase before device open. Its persistent visual Session reuses Export's production decoder, title, prepared Program, Effect, color, and composite owners to produce authored full-raster Float32 working composites under explicit Reference Output intent; its independent public Audio Program uses no audition or Standard channel remapping and strictly enters then continues exact rational 48 kHz windows. Picture, Audio, Program Output, carrier packing, and scheduling form one fail-closed bundle transaction. Any author drift or execution/provider/continuity failure permanently faults the generation; close cancels software work and transfers device retirement to the App lifecycle owner. This qualification-only CPU path proves canonical software semantics, not GPU residency, vendor hardware, wire output, external lock, or soak performance.
_Avoid_: Preview proxy/scale as clean feed, Viewer/monitor/ICC/Scopes/warnings in the signal, per-frame rounded Audio, independently advanced Timeline/device phases, partial A/V scheduling, retrying a faulted generation, simulated Adapter evidence presented as hardware qualification

**Preview Execution Coordinator**:
The UI-independent App Module that atomically binds complete Viewer intent to one media generation, pending state, executed presentation quality, monotonic candidate identity, and the currently registered output together with the generation that proved it exact. A retained output from an older generation remains stale until full output-key resolution or a new presentation proves it under the active generation. Window and Headless presentation are Adapters over its opaque GPU execution contract; neither may reconstruct candidate/cache lifecycle.
_Avoid_: Window-owned candidate counter, separate Headless output identity, UI-owned generation/pending booleans, treating retained stale output as current after generation rotation, cache hit inferred from texture presence alone

**Preview Output Unavailability**:
The typed terminal contract carried unchanged from Timeline and media resolution through CPU/GPU execution, final presentation, Window/Headless Adapters, and evidence. It separates expected `NoContent`, fail-closed dependency or correctness `Blocked`, and admitted execution `Failed`, and always names the owning production stage; diagnostic text is never classification authority.
_Avoid_: Unit `Unavailable`, error string parsing, missing dependency reported as empty Timeline, execution failure reported as loading, stale-frame reuse across a terminal unavailable result

**Preview Viewer Plan**:
The UI-independent resolved element representation and pure lowering Module that owns stable cache identity, aggregate presentation quality and decode provenance, deferred-composite detection, and renderer GPU-layer admission. Window and Headless Adapters consume the same result and blocker semantics.
_Avoid_: Window-owned plan hashing, Headless-specific lowering, cache identity derived from rendered pixels, presentation side effects during lowering

**Timeline Render Cache**
: The independently bounded persistent Module for complete post-Effect/post-composite working-linear frames. Its v2 content address accepts only one opaque **Resolved Visual Cache Identity** plus exact extent, physical lossless RGBA32F format, and straight-coverage alpha; no caller can partially assemble semantic digest fields. One worker owns bounded lookup/publication queues, lossless compression, checksummed validation, durable atomic publication and local disk LRU. Preview retains at most one verified hit and re-enters the ordinary Viewer output/monitor stages; Busy/Miss/failure renders normally. Export use is forbidden without an explicit immutable quality policy.

**Resolved Visual Cache Identity**
: The versioned, provider-independent identity of one fully materialized Prepared Visual frame. Renderer owns the semantic envelope (exact time and authored/execution raster, working space, engine, and render-semantics epoch); exhaustive source Adapters bind ordered resolved layers, transforms, opacity/blend, compiled Effect identity/frame seed, generated sources, recursive nested frames, and exact media revision/sample/interpretation. CPU RGBA, compact CPU YUV, and native decoder surfaces normalize to the same Full source semantics, while reduced/proxy quality remains distinct. Program Output, authored Preview codec, monitor/ICC, Scopes, warning overlays, scheduling generations, provider/backend, handles, diagnostics, and conservative author revisions are excluded because they do not shape cached working pixels.
_Avoid_: Optional fingerprint builders, Rust `Hash` bytes as a persistent protocol, Program Output in a pre-output artifact key, backend-specific cache misses, raw digest construction outside Store parsing

_Avoid_: UI raster as persistent cache, frame-number-only key, filesystem/compression on the playback thread, GPU readback on every realtime frame, global invalidation, cache hit as implicit Export substitution

**Preview CPU Execution**:
The UI-independent App Module that prepares source frames into working-linear inputs, composites one Preview Viewer Plan through renderer semantics, and applies the Program Output plus monitor adaptation for a final CPU raster. It returns pixels, complete color/composite facts, and stage durations; presentation Adapters only project those facts.
The presentation-only boundary retains Program Output descriptor/diagnostics but consumes its uniquely owned Float32 pixels through monitor adaptation, so the two exact OCIO stages use one float raster and quantize once. The retained Program Output API remains separate for scopes and other consumers that need those pixels.
_Avoid_: Calling Window diagnostics from execution, Headless-specific color math, discarded input-transform evidence, Widget raster types in the execution result, retaining unused Program Output pixels, flatten/repack copies between OCIO stages

**Preview Media Task**:
The UI-independent App Module that consumes admitted Preview media jobs, runs the concrete FFmpeg Preview Adapter, observes Broker cancellation at cooperative and blocking-I/O checkpoints, publishes one structured terminal result with the media Adapter's first-observation fact, and performs bounded worker shutdown. Window and Headless Adapters consume the same task semantics and may not reconstruct a second decode loop.
_Avoid_: Window-owned codec worker, Headless-only decoder, cancellation inferred after codec return, task success cached after cancellation

**Preview Media Source Resolution**:
The UI-independent App Module that turns one immutable asset record plus complete Viewer color, Alpha, proxy, geometry, and hardware-admission intent into exactly one canonical media key, one explicit color rejection, or one structured unavailable outcome. Proxy generation is returned as an intent; library lookup, dispatch, metrics, and presentation remain Adapter concerns.
_Avoid_: Window-built decode key, Headless-specific source interpretation, silent missing-file `None`, proxy side effects during semantic resolution, output scale encoded into a native-source decode key

**Preview Timeline Execution**:
The UI-independent App materialization Module that consumes the renderer's canonical Prepared Visual Frame Closure plus typed media/title outcomes, resolves only its typed node/binding addresses, composites child working outputs, and returns a ready Viewer plan with mandatory cache identity plus ordered execution facts. Its read-only media-demand collection iterates the same prepared nodes for prefetch, preroll, and input-color evidence. Nested time projection, cycle/depth, child canvas/color context, Transition side, temporal membership, and instance paths are renderer closure semantics rather than App decisions.
_Avoid_: App-owned recursive interpretation, Window-owned recursion, Headless-specific evaluator, scheduler-specific nested walker, child raster inherited from the parent, optional cache identity on a ready plan, diagnostics callbacks inside execution

**Preview Production Runtime**:
The UI-independent App composition root that binds the Frame Work Broker, Preview media workers, Frame Store, execution coordinator, Timeline/Viewer evaluation, result pump, evidence, and final application presentation arbitration. Its generic output payload is opaque: Window and Headless Adapters register their own usable GPU output while sharing generation, cache identity, pending state, stale scope, CPU raster, cancellation, and Frame Delivery semantics.
_Avoid_: `WindowPreviewAdapter` as the Headless composition root, Window-owned result pumping or cache policy, a second Headless scheduler, Widget payloads in production state, production decode residency hidden in thread-local state

**Prepared Viewer Successor**:
The capacity-one, ticketless result of evaluating the exact immediate running successor under one Playback Epoch, quality revision, frame coordinate, active Preview generation, and complete Viewer output identity. Transport intent and pixel identity are independent: identical adjacent pixels retain an already-visible alias proof, a different ordinary GPU output owns a separate physical lease, and an empty Program retains an explicit transparent proof. The prepared slot cannot become visible, consume a future Frame Demand, extend a deadline, or survive generation rotation; only an exact current-coordinate request may promote it. One UI-independent physical-publication Module owns the capacity-one current/prepared slots, exact submission identity and move-only leases; Window and Headless attach different artifacts but cannot reinterpret promotion or retirement. Device-generation failure, accepted non-GPU output, presentation-geometry change, or semantic/physical disagreement drains every affected physical owner and clears its exact semantic artifact before reevaluation. An already-visible alias may bind completion to the frame-boundary observation because no pixel replacement occurs; every distinct output is timestamped only at its real visibility commit.
_Avoid_: keying preparation by output hash alone, attaching the current demand to successor work, publishing the successor early, sharing one move-only lease between two physical slots, promoting after seek/quality change, calling decode/cache readiness a presentation, backdating a replacement-buffer commit

**Preview Work Watch**:
The UI-independent, payload-free monotonic revision and bounded-wait seam advanced after a Preview media, visual, title, or external-dependency result becomes pollable, or after an asynchronous coordination barrier changes from blocking to actionable. Worker terminal-health transitions use the same progress edge so a producer exit cannot strand Pending work. Window maps it to a coalesced native-loop wake; Headless waits on the same revision edge while retaining Clock Master tick and presentation-deadline bounds. Typed result channels, coordination state, health state, and the bounded Preview pump remain the sole payload, retry, freshness, and terminal authority; the watch itself proves none of them.
The watch owns bounded callback registration and retirement independently of payload workers. Native invocation stays synchronous outside ownership locks; one lazy worker owns final callback destruction, with eight total retained/in-flight/actively-retiring registrations. Rejected registration returns caller ownership. Caught opaque panic payloads and failed registrations are explicitly abandoned, never destroyed on an unwinding producer. Shutdown closes admission before joining producers, retains the original deadline and immutable terminal receipt, and rejects consuming reentry/concurrency without inventing closure. Preview schema 4 requires the raw schema-1 callback receipt, including explicit nullable fields, even when no callback was installed. Dependency observer termination stores unhealthy before publishing its terminal hint.
_Avoid_: EventBus as a completion queue, one native event per completed payload, notification as Frame Delivery evidence, blind fixed-interval polling, unbounded Headless wait, callback failure hidden by a clean worker join, per-callback retirement threads, omitted nullable receipt fields interpreted as clean defaults

**Viewer GPU Startup Owner**:
The App-owned partial generation guard installed when the sole GPU progress worker starts, before Window/Headless construction can fail. It retains device/queue and any constructed Renderer while UI resources stay on the main thread. Abandonment transfers the actual partial inventory to the existing progress retirement domain; activation moves the complete pair once and releases the guard's residual GPU handles. Window native decode authority is published only after complete session assembly. This ownership contract alone does not prove bounded cleanup or preserve a terminal receipt through a failed campaign return.
_Avoid_: guard creation after a caller's fallible setup, starting a dummy Renderer for same-device window replacement, publishing partial native decode authority, keeping device clones in an activated long-lived guard, equating asynchronous Drop with joined-worker qualification

**Preview Decode Session Context**:
The explicit worker-owned media execution context for packet-source/codec state, DPB, and hardware-surface pools. Playback has one continuous slot; Scrub and GPU-resident exact Still share one Interactive slot; CPU Still has one physically separate slot. Every native output carries a weakly observed lease through App and renderer clones. A slot cannot seek, flush, or change contract while its prior native output remains owned. After the final lease retires, a healthy slot may be reused only when its conservative source revision, isolated/direct packet-source execution family, and codec/output contract still match; incomplete source metadata never authorizes reuse. `IsolatedDemuxTermination` poisons the packet source and retires the complete paired Session without first flushing an unreusable codec context. Residency-family retirement is acknowledged only after every published native-output lease has retired. Idle and worker shutdown clear the complete context.
_Avoid_: hidden production TLS cache, unbounded or alternating native session pools, entering/seek-flushing the codec while its previous native output is still owned, flushing after isolated-demux termination, treating the last access mode as a terminal-state substitute, acknowledging residency retirement while a completion or renderer still owns its output

**Preview Demux Isolation Boundary**:
The media-internal recoverability seam that moves only `AVFormatContext` open, stream discovery, seek, and packet reads into one terminable packaged-helper process per source Session while retaining `AVCodecContext`, DPB, hardware device, and native GPU surfaces in the parent Preview worker. Its versioned bounded protocol validates native paths, launch identity, real FFmpeg library ABI, stream/codec identity, packet ownership, ordered non-zero command IDs, seek completion before codec flush, and pull-based packet responses. Playback, Scrub, and Exact use the same reusable `open/seek/read/close` Interface in production; direct in-process format ownership remains only an explicit unconfigured test/diagnostic Adapter.
_Avoid_: child-process RGBA as the native path, serializing FFmpeg pointers, `AV_PKT_FLAG_TRUSTED` across IPC, unbounded packet buffering, helper path discovery from `PATH`, callback cancellation presented as recoverability, claiming Playback isolation from an exact-Still-only vertical slice

**Isolated Media Probe**:
The media-owned one-shot Helper process that canonicalizes one native source path, captures complete conservative source-revision evidence, runs the bounded FFmpeg metadata/limited-frame probe, verifies the same source revision again, and publishes exactly one versioned bounded response. The parent owns cancellation and a monotonic deadline, kills and reaps the complete Helper on either, and the Asset Library transaction revalidates the returned fingerprint before mutation. Media Import, Relink, and Audio Component refresh/rebind share this physical Adapter while retaining independent queues, generations, commit rules, and terminal evidence.
_Avoid_: FFmpeg probe on a detachable App worker thread, Project or Asset Library handles in the Helper, unbounded stdout/stderr, helper discovery from `PATH`, string success without a typed snapshot, source fingerprint sampled only before probe, cancellation reported before child reap

**Preview Decode Execution Progress**:
A lock-free, single-writer evidence snapshot for one Preview media worker: the exact current FFmpeg/media stage, request/progress sequences, FFmpeg interrupt-callback poll/cancel sequences, the request that last observed callback cancellation, and cumulative process-isolated demux command/lifecycle facts. A helper launch owns one non-cloneable evidence lease; ready and cross-request reuse require completed protocol facts, while clean/canceled/failed/forced termination is published only after that child is reaped. It is observable by the UI-independent Preview Production Runtime and Headless acceptance but owns no timeout, cancellation, recovery, or scheduling decision.
_Avoid_: Debug log as liveness proof, helper executable configured as helper execution proof, read-count heuristics as cross-request reuse, process disappearance as a guessed terminal reason, coarse `codec` bucket, callback installed presented as callback polled, callback cancellation presented as call return, watchdog hidden in the media Adapter, progress sequence treated as completed work, UI-owned worker health

**Waveform Analysis Service**:
The UI-independent App Module that resolves asset/source revision, admits bounded background work, owns generation cancellation, decodes through a private bounded audio-source cache, retains revision-keyed envelopes and failures, and publishes bounded terminal evidence. The media Module owns only streaming PCM-to-envelope math; the Timeline receives a shallow nonblocking lookup Adapter.
_Avoid_: Widget-owned FFmpeg state, whole-file PCM, path-only or asset-only cache key, unbounded channel, global thread-local cache

**Window Preview Adapter**:
The shallow `app_ui::preview` Adapter that constructs a renderer-registered external-texture Widget payload, converts `PreviewRasterFrame` to `ViewerFrameImage` without copying pixels, and projects immutable diagnostics into panel models. It owns no worker, generation, scheduling, cache, output-selection, color, or transport policy.
_Avoid_: Redraw-local output priority, Widget types in `app::preview_runtime`, UI transport mutation, stale reuse across sequence/display/geometry identity

**Viewer GPU Preview Runtime**:
The device-scoped owner of native video import, working-linear compositing, spatial processing, display output, calibration, and current external-texture presentation resources for Viewer execution.
_Avoid_: Window-owned GPU grab bag, separate headless rendering semantics

**Decoded Video Surface Contract**:
The Media-owned physical descriptor for decoded CPU/native surfaces: RGB or YCbCr, chroma subsampling, plane/packing layout, numeric encoding and alignment, component bit depth, alpha, and native-payload eligibility. FFmpeg and platform Adapters lower their concrete formats into this vocabulary; Renderer validation, working-set estimation, and native YUV/RGB planning consume it without redefining the matrix. A modeled format is not support: one device-scoped Renderer generation must publish an exact `(native handle kind, surface format, ZeroCopy or GpuBridgeCopy)` route before App may request GPU residency. Subsampled YCbCr requires explicit chroma siting; 4:4:4 may leave it unspecified. Native encoded-RGB materialization uses the product RGBA32F decision before OCIO so 12/16-bit YUV, float RGB, alpha, undershoot, and overshoot never pass through RGBA16F.
_Avoid_: Cartesian product of separate handle/format lists, backend-independent preferred surface list, P010 relabeled as 12/16-bit, unspecified subsampled chroma, descriptor existence presented as platform execution, high-bit source staged through RGBA16F

**Renderer-Qualified Decoder Device Root**:
The immutable FFmpeg hardware-device root constructed over the exact native device owned by one Renderer generation. App installs it into the Preview worker-family pool before publishing GPU-resident decode admission; codec Sessions receive only FFmpeg reference-counted leases. Replacement advances pool and Preview generations, rejects reuse of old Sessions, cancels old decode bindings, and removes old decoder-resource cache entries without revoking in-flight native leases.
_Avoid_: Adapter index as exact device proof, separately-created decoder graphics device, Renderer handle in a decode request, old-device result admitted as cache-only, revoking a native lease before GPU completion

**Viewer GPU Output Health**:
The UI-independent policy that maps one typed presentation-attempt outcome plus renderer stage facts into the canonical Waiting, Blocked, Failed, Rejected, Degraded, or Ready status and cumulative counts. Window telemetry, Headless gates, performance smoke, and the budget CLI consume this single classifier and report schema.
_Avoid_: Window-local Ready rules, duplicated health enums, treating texture registration without presentation/native-boundary proof as Ready

**Platform / Driver / Display Qualification**:
The finite cross-machine campaign that binds one release/source/build identity to exact target packages, measured runtime images, physical adapters/drivers/displays, native probe backends, GPU color gates, and Viewer SDR/P3/PQ/ICC scenarios. Core owns strict row/scenario semantics and deterministic three-state resolution. Validation Adapters own bounded create-only files, before/after environment snapshots, and direct semantic replay of authority-challenged native producer transcripts, exact GPU test-process attestations, full Viewer Display Output Contracts, ICC/LUT replay, stimuli, and operator observations into normalized receipts. Each native probe returns the stable output identity selected by the same OS enumeration, and each mutually exclusive display state requires a fresh authority acknowledgement chained to the prior transcript. Final admission loads verifier scripts/contracts into memory from one link-free archive of the externally pinned Git revision, rechecks a private evidence snapshot, forbids runtime Cargo, and executes only separately hash-approved Core/Matrix replay binaries. The separately hashed capture-authority manifest binds each single-use challenge, producer, session transcript, and operator; content hashes prove closure, not physical provenance, so real hardware and approved operator authority remain HITL.
_Avoid_: Accepting a hand-written normalized receipt as source evidence, splicing runs/processes/adapters/displays, using EDID capability as active HDR proof, mutable executable-path hashing on Linux, treating hosted CI as a physical row

**Realtime Performance Matrix**:
The sealed, serial, reference-machine qualification that binds one clean unchanged source SHA to attested fixtures and exact 30-minute playback, a 120-minute Program-scale authoring workload (not 120 wall-clock minutes), real 4K60 dual-layer decode/publication, and Renderer 4K60/8K30 HDR-multilayer-multieffect-scopes evidence. The Renderer owns the visual workloads, timing budgets, report evaluator, and professional demand-driven Viewer texture grant; App supplies the real media and authoring producers without reinterpreting those facts. Missing hardware or fixtures is Unqualified, an executed failing gate is Failed, and neither may be presented as a skip or baseline pass.
_Avoid_: Promoting developer smokes or compilation to qualification, parallel Cargo/GPU gates, test-only unlimited resource grants, 8K claims without warm-path allocation evidence, mixing reports from different machines/source revisions, treating COL-010 physical HDR display qualification as covered

**Commercial Endurance Qualification**:
On Windows, exact-runtime admission also requires the strictly ordered exact
set of every DLL beside both FFmpeg tools. Before any App or worker exists, one
machine-plan-bound typed receipt copies the retained executables and DLLs into
a private capsule. It denies write/delete sharing on every declared file and
rename/delete sharing on the capsule directory, revalidates the exact capsule
entry set before every command, verifies fixed bounded outputs, the production
semantic baseline, and the process-loaded canonical-path closure, then installs that
exact CLI pair process-wide. A changed capsule or different later identity is
rejected through a typed command-admission error, never a fabricated executable
path or alternate packaged/PATH command. Audio failure caching and Preview/
Export acceleration fallback must preserve that rejection; it is not ordinary
decoder, hardware-codec or output-contract incompatibility. The machine-specific factory remains responsible for enforcing this
admission before constructing any App or worker.
This Windows evidence does not yet bind a previously mapped module image to the
later retained file object, and capsule revalidation is not atomic with process
spawn; those stronger guarantees remain explicit qualification work on an
otherwise access-controlled machine.
macOS/Linux remain fail-closed until descriptor or immutable-image execution
and loaded-library identity are qualified on the transfer machines.

The platform-core-owned deterministic replay of one exact-source, exact-runtime, COL-046-cell-bound 72-hour serial campaign: 24 hours Playback plus physical Reference Output, 24 hours repeated independently verified Export, and 24 hours concurrent playback/reference/export with controlled recovery. Before any phase owner may start, App admits one strict bounded schema-1 run request whose same-read byte hashes bind the raw profile, machine plan, capture authority, and ordered workloads to the complete identity; it also reserves an empty evidence directory and a distinct create-only manifest route. Only the resulting `PreparedEnduranceRunRequest` may enter the public product runtime. App then parses the exact phase-specific workload schema and fixed policies, closes duration/counter semantics against the profile, and hands the runtime only a `PreparedEnduranceWorkload`; a valid workload may become `NotRun` only through an exact pre-start capability inventory naming every absent fixture/provider/recovery driver. A separate bounded schema-2 machine plan binds the canonical Project and external-source inventory, active Sequence, exact Audio and physical Reference contracts including an explicitly present ANC policy, phase-specific Export inputs, recovery targets, pinned FFmpeg/FFprobe identities, and non-renewing timeouts. Campaign admission verifies the actual plan-file SHA against run identity, retains that already parsed typed plan inside the prepared request, then moves the same `PreparedCommercialEnduranceMachinePlan` into the runtime exactly once without reopening or reparsing its pathname; every factory preflight and phase build borrows that same typed plan, and all runtime deadlines are derived only from it, before any owner can exist. App projects owner diagnostics and one complete native product-process-tree sample into profile-bounded hash-chained chunks, bounds receipt/event inventories, generates typed producer evidence, publishes create-only, derives typed terminal closure, and seals phases in profile order; it does not reinterpret thresholds. A strict schema-2 externally pinned single-use capture authority binds the raw profile bytes, complete release/machine identity, machine plan, ordered phase workloads/producers, and challenge before capture construction succeeds; schema-2 manifest/report replay closes every chunk, owner report, and raw evidence file and independently derives artifact-verification/recovery summaries. Replay rejects per-domain stalls, gaps, latency, counter regression, non-closing queue/accounting/publication, backend/metric drift, unbounded memory peak/growth/integer slope, terminal resources, dead workers, or unreaped children. Missing hardware/execution is Incomplete, executed violation is Failed, and short or accelerated tests never qualify.

An **Endurance Owner Capture Envelope** is stamped by the supervisor immediately before the native process-tree probe and ends with the completion stamp. The snapshot path does not schedule, pump, or poll phase work, although an owner projection may refresh its bounded diagnostic cache; every domain remains responsible for an internally consistent projection and independent threads do not claim one global linearization instant. Export copies mirrored worker/shutdown lifecycle, counters, activity, job gauges, and job-scoped decoded-audio owner started/closed/failure/active facts under its sole Queue mutex. Dirty audio closure increments fatal errors and a live audio owner contributes queue depth. A settled Headless snapshot carries selected fixed-size Preview scheduler/worker, Frame Store/pin, prepared-program, GPU lease, complete App auxiliary-domain, Audio work/buffer, and monotonic failure gauges. Terminal zero requires consuming the complete AppState Playback owner plus clean Preview with typed render-cache startup/terminal evidence, the phase-bound product Waveform owner with its nested Audio Source receipt, complete-App, and GPU closure; otherwise resources and failure evidence remain nonzero. Preview, Waveform, and App close admission before any join and all spend from one caller-owned absolute deadline. A failed begin, pump, event, probe, or capture invokes consuming shutdown exactly once, and an incomplete `Ok` receipt or cleanup error remains paired with the primary failure.
Every snapshot privately seals its phase kind, so an Export-only zero-realtime projection cannot satisfy Playback or Recovery. When an unexpected Audio render-worker terminal replaces the move-only owner, App first carries its cumulative failure counters into a validation ledger; subsequent Headless samples combine retired history with the current owner and cannot regress.
For a Continuous Export interval, the validation-only **Frozen Repeated Export Phase** captures one ordinary `TimelineExportSnapshot` and complete delivery configuration exactly once, then clones that capture into a strictly serial sequence of unique create-only single-file artifacts. It exclusively owns a fresh empty Queue: while an attempt is active the Queue must retain exactly that one Job, and between attempts it must retain none. A Job advances only after `Completed`, exact `Published` plus path-matching `Durable` evidence, independent full-decode receipt creation, and removal of exactly one terminal history record. Closing stops new admission but permits the current irreversible publication and verification to finish; it never turns the Continuous Export policy into cancellation. Failure, cancellation, missing/mismatched Job or path, Queue contamination, verifier failure, cleanup mismatch, and counter overflow latch a terminal phase fault. The phase owner must be dropped before the complete App owner is consumed so its extra Queue reference cannot survive terminal closure.
_Avoid_: Passing a raw workload path to a runtime, treating malformed/mismatched workload bytes as `NotRun`, starting partial owners before capability admission closes, sharing the Continuous Export Queue with UI or recovery work, recapturing a live Timeline between repeated artifacts, cancelling the active Continuous Export attempt during close, retaining every 72-hour sample/event/receipt in an unbounded collection, calling a Program-duration scale test a soak, diagnostics activity hiding a stalled Export, aggregate progress hiding a stalled domain, raw hardware ticks without rate, clearing queued output without terminal accounting, durable Export publication relabeled as independent content verification, Viewer raster relabeled as Reference clean feed, arbitrary counter/hash files standing in for typed producer events, manifest-only self-attestation, simulated Reference Output promoted to hardware evidence

An **App Endurance Owner Closure** consumes the sole `AppState` after closing admission and cancellation across Project/persistence, Reference Output, Export, Audio playback and its shared source cache, persistent FFmpeg child/pump threads, paused-Audio warmup, Media Import, ordered asset mutation, visual tracking, Proxy generation, and the lazy native-memory observer. The upper product closure also owns phase-bound Preview, typed Timeline render-cache evidence, Waveform, and GPU retirement. All joins share one absolute deadline. Its typed receipts preserve startup, join, panic, timeout/detach, residual work/resource, cumulative-failure, Project lease, and App-owner facts; ordinary `Drop` is only best-effort cleanup and cannot prove closure. Runtime capture separately projects exact gauges and monotonic failures for five auxiliary domains plus an Infrastructure aggregate over persistence/native memory, then merges terminal evidence per domain so shutdown-time failures cannot disappear. `RenderQueue::begin_shutdown` provides the non-consuming signal used before the later consuming `RenderQueue::shutdown_until` receives the unchanged App-wide absolute deadline. Window validation similarly returns typed Preview/Waveform closure under the operation's original deadline instead of renewing an internal timeout; the returned App is either resumed by the campaign or consumed by the standalone outer lifecycle. This is a COL-047 software prerequisite, not proof of the 72-hour campaign or hardware HITL.

A **Performance Smoke Owner Closure** applies the same consuming rule to the four default App performance probes. Measured work, including panics and ordinary errors, is followed by simultaneous admission closure, Preview worker/decoder-native reclamation, GPU generation retirement, and App consumption, in that dependency order while every wait spends from one shared absolute deadline. The schema-1 JSON projection retains typed Preview/render-cache, GPU retirement, Project, Reference, Export terminal snapshot, Audio/Audio Source, and auxiliary-worker leaves; a shared suite/comparator validator reconstructs eligibility from the complete leaf set and fixed worker-domain inventory, so missing, default, contradictory, boolean-only, unequal three-row, timed-out, detached, or residual evidence is ineligible even if timing thresholds pass. Test Preview must install a real isolated Timeline Render Cache, and the decode/cache smoke must prove persistent miss → bounded CPU-fallback durable publication → prior-work terminal drain → in-memory reset → a request-local increase in both lookup and verified-hit counters. GPU-only presentation is not disk publication evidence; candidate scheduling and result pumping remain distinct from the presentation projection. Frame Store memory-pressure recovery and persistent disk-cache qualification remain distinct contracts.
Each Preview receipt has one unique ordered closure-local owner slot. Eligibility checks require actual JSON numeric/boolean types, reconcile startup and terminal inventories, and recompute timing aggregates over the exact default case set. The release suite builds the same-profile packaged product worker before execution; a test binary alone is not a worker prerequisite.
_Avoid_: Renewing a timeout for each owner, treating `Drop` or an empty queue as a join receipt, aggregating auxiliary failures before terminal merge, claiming live persistence/memory-observer gauges that are not instrumented, promoting simulated Reference Output closure to hardware qualification

**Independent Export Artifact Receipt**:
The Export-owned sealed result of copying one bounded direct regular file into a verifier-owned immutable snapshot, hashing its encoded bytes, deriving the typed container/stream probe, fully decoding every advertised video/audio stream to EOF in a separately supervised FFmpeg process, hashing the decoded stream bytes, and proving unchanged published artifact size and digest afterward. It carries the stable Export job/artifact identity, validator identity, terminal frame/duration progress, canonical report, and report SHA-256. App may project an endurance artifact event only from this receipt and rejects a repeated artifact identity within one phase. It proves stable complete decodability and content identity, not source-pixel or colorimetric parity.
_Avoid_: Publication counted as verification, opening-frame-only scans, caller-authored receipt hashes, unbounded decoder output, or TOCTOU-blind reports

**Viewer Execution Retirement**:
The consuming Renderer transition that closes CPU-YUV upload admission and
retains its actual worker JoinHandle, native-copy residency, and GPU resources
behind a poll-only owner. A cached terminal receipt proves the actual worker
join and native-copy release, not the App submission lifecycle or whole-device
queue barrier. Joined panic permits safe release but fails qualification. App
progress carries the receipt through envelope release and final worker join;
normal channel drain, unknown/timed-out retirement, and explicitly unconstructed
runtime inventory are distinct. Native-window replacement transfers this same
generation without creating a temporary worker.
_Avoid_: Queue idle as worker termination, bool-only retirement evidence,
detached upload worker, new generic reaper, UI-thread join

**Viewer GPU Output Residency**:
The UI-independent projection of declared Preview layers or completed renderer execution into typed decode, input-transform, working-residency, zero/low-copy, upload/readback, native bridge-copy count, and native-import evidence. Planned and executed residency are distinct; one immutable, device-scoped Renderer import-support snapshot—including exact handle/format routes and an explicit `ZeroCopy` or `GpuBridgeCopy` transfer mode—is shared with hardware-decode admission for the lifetime of a Renderer/Window Session. Media proves the exact decoded surface family and sampling facts; only the Renderer instance that owns the active Adapter/Device may prove import support. Zero-copy proves no CPU transfer and no decoder-surface pixel copy; it does not erase the deliberate YUV/RGB-to-encoded-RGBA32F or OCIO working-texture stages. Every direct Adapter retains the Media surface lease until a GPU completion terminal proves the final read.
_Avoid_: Planned zero-copy success, Cartesian inference from aggregate capabilities, OS-name or device-less capability probes as execution evidence, independently probing a second graphics device, per-frame capability re-probe, mixing capability generations inside one frame record, dropping the Media lease at queue submission

**Audio Playback**:
The realtime path that owns output-device lifecycle, PCM preroll and consumption, render generations, underrun recovery, and consumed-media-position evidence for a Playback Session.
_Avoid_: Audio clock, UI-owned output stream

**Audio Sample Position**:
One signed integer position on an explicitly identified sample-rate timeline, resolved once from rational timeline time using a declared rounding policy.
_Avoid_: Floating-point seconds passed between audio render stages, sample index without rate

**Decoded Audio Source Window**:
One exact interleaved PCM block for a fingerprinted physical stream selection on a prepared Audio Render Contract. Runtime hot windows and media LRU windows may differ in size but preserve the same integer sample coordinates.
_Avoid_: Whole-file PCM as the source Interface, per-sample decoder virtual call, path-only cache identity

**Asset Audio Component Catalog**:
The Asset-owned persisted mapping from stable `AudioSourceComponentId` values to conservative physical-stream signatures and the complete source-revision evidence captured by the same probe observation. Initial default disposition may choose `primary`; relink never retargets an existing identity by index alone.
_Avoid_: Timeline-owned stream index, `0:a:0`, language/title as identity, silently rebinding after source replacement, calling file metadata a content hash

**Audio Source Selection**:
A short-lived media Adapter value containing one absolute container stream index, its probed native layout, and the source fingerprint that authorized the binding. It is part of decoded-window and persistent-Session identity and is revalidated at open.
_Avoid_: Author state, path-only decode key, channel count presented as layout

**Audio Signal Layout**:
The validated semantic layout and canonical interleaving order of one audio signal. It is either layout-independent Mono, a canonical non-empty set of named speaker positions, or a bounded 1–64 channel Discrete bus with deliberately absent speaker meaning. Standard Stereo, 5.1(side), 5.1(back), and 7.1 are canonical named-speaker values rather than channel-count aliases; channel capacity is always derived from the layout.
_Avoid_: Bare channel count as signal meaning, unordered speaker vector, treating Discrete channels as speakers, device channel index, `5.1` without side/back meaning, guessed six-channel ordering, representation mistaken for Adapter execution support

**Timeline Time**:
A normalized exact rational offset interpreted within its owner's declared Authoring Time Domain, independent of frame, sample, or display grids.
_Avoid_: Frame number as universal time, floating-point seconds, fixed subframe ticks

**Sequence Author Revision**:
A persisted nonzero monotonic generation of one stable Sequence identity. Every committed Author Transaction that changes that Sequence, plus Undo or Redo that restores its content, advances it. Project-only transactions, file-save attempts, and playhead-only navigation do not rewrite it; Project-wide execution invalidation uses Author Generation plus the exact resolved Project semantics.
_Avoid_: Project document save revision, frame-demand sequence, content hash presented as a transaction generation

**Prepared Visual Schedule**:
An immutable execution index compiled from one exact Sequence Author Revision. It retains video Tracks in author order and indexes enabled Clip and visual Transition half-open placement intervals for exact Timeline Time queries. Preview and Export production Sessions reuse it by Sequence identity plus revision; dynamic Clip properties and effect graphs are still evaluated at the requested time after interval selection.
_Avoid_: Persisted placement authority, frame cache, stale-revision reuse, per-frame scan of every Clip, renderer-owned copy of Sequence semantics

**Prepared Visual Author Fingerprint**:
A versioned, domain-separated SHA-256 over the complete canonical author projection that can affect one Prepared Visual Program, including its Materialization Contract. It is a conservative equality guard paired with Sequence identity and revision: semantically irrelevant representation changes may invalidate reuse, but omitted pixel-, reachability-, canvas-, title-, Transition-, Effect-, or scheduling-affecting author state may never create a false hit. Copy-on-write allocation identity, cache residency, process generation, and runtime resources are excluded. Raw or deserialized snapshots and Program-cache misses recompute and compare it even when a caller reuses a Sequence ID/revision. A validated live Preview binding may reuse that proof only inside one exact Authoring Session and monotonic Author Generation, where each lookup still checks constant-time Sequence, revision, Effect Registry, Program, and residency identities. Frozen Export capture obtains the same conservative proof while stabilizing its selected range and Registry revision, then executes the frozen Program set without consulting a later live Registry. Serialization or fingerprint-version mismatch fails closed.
_Avoid_: Sequence revision alone as Program equality, truncated/diagnostic hash, pointer or allocation identity, fingerprint used as authoring transaction generation, optimistic partial-field hash

**Prepared Visual Program**:
The immutable per-Sequence production visual-execution capture for one exact Sequence Author Revision and Effect Definition Registry Revision. It composes one Prepared Visual Schedule with per-Clip resource-bound Prepared Effect Programs or typed blockers, prepared visual Transition definition snapshots/blockers, and one typed Materialization Contract containing the authored canvas, authored Preview raster scale, and Basic Title safe-area facts covered by the same conservative author fingerprint. Preview may continue across unrelated blocked intervals. Each requested Sequence instance first evaluates its dynamic Effect graphs and admits the complete per-Sequence plan against the concrete consumer. External-resource revalidation is an explicit low-frequency Seam, distinguishes retryable external change from author/definition repair, and never performs filesystem work on a frame lookup.
_Avoid_: Recursive Project closure hidden inside one Program, second Timeline interpretation, materializer reopening a Sequence by ID for canvas/title settings, readiness-only preflight presented as execution admission, per-frame definition/LUT resolution, unavailable Effect rendered as identity, one missing later Clip disabling the whole Viewer, resource polling in the hot cache path

**Prepared Visual Range Closure**:
The immutable renderer-owned recursive dependency capture for one inclusive root Sequence range. It pins exactly one Prepared Visual Program per selected Sequence under one Effect Definition Registry Revision, validates each Program's Sequence identity/revision and Prepared Visual Author Fingerprint, and accumulates only range-reachable Sequence, file-backed picture Asset, Transition, and Basic Title font identities. Reachability comes from Prepared Visual Schedule interval queries plus exact nesting/retime projection and conservative finite-or-unbounded temporal expansion; active-path cycles, depth overflow, duplicate Sequence identities, missing children, or mixed registry revisions fail closed. Export snapshot capture and Preview lookahead use this same closure. It owns no per-frame Render Plan, pixels, decoder/GPU state, audio semantics, presentation, or publication policy.
_Avoid_: Whole-Project dependency scan, frame-by-frame range scan, Export-private nested walker, unrelated Sequence prewarm, revision-only Program acceptance, range closure confused with per-frame execution closure

**Prepared Visual Frame Closure**:
The transient renderer-owned recursive execution capture for one exact root frame. It composes already evaluated per-Sequence Render Plans and consumer admission payloads, copies each node's typed Materialization Contract from its exact Program, projects every nested source time once on the child's Evaluation Grid, rejects active-path cycles/depth overflow/missing handles, fixes each child canvas from the child's exact Program contract plus consumer runtime divisor, fixes Program Color Context, and binds ordinary, Transition-left/right, and exact temporal samples to distinct placement-instance paths. Preview and Export consume this same closure; equal child Sequence/frame values reached through different placements remain distinct instances. After every binding is frozen, the consuming Module may atomically replace per-node payloads through `try_map_payload` while inspecting only this immutable closure; Preview uses that Seam to bind parent Nested Effect routes to the exact child execution raster rather than guessing the parent canvas. Their materialization contexts retain only closure nodes plus frozen media/color execution facts, never a raw Sequence collection or complete Timeline snapshot. The closure stores no media pixels, GPU resources, pending state, presentation, encoder, or publication policy. Before CPU materialization it exposes one checked conservative nested-output active-byte estimate admitted against the consumer's existing Timeline compositor grant.
_Avoid_: Preview/Export private nested walker, `(SequenceId, frame)` used as instance identity, temporal demand inferred by a current-frame walk, materialization-time Sequence lookup, caller-selected child rounding/color/canvas, pixels cached in the semantic closure, unaccounted child working frames

**Prepared Visual Execution Module**:
The renderer-owned iterative materialization driver that validates one Prepared Visual Frame Closure as a rooted instance tree, invokes the Preview or Export Adapter exactly once per reachable node in deterministic child-before-parent order, exposes only already-materialized direct-child outputs plus the exact inbound binding, and returns the root output without owning decode, generated pixels, compositing, pending state, presentation, encoding, or publication.
_Avoid_: Preview/Export recursive materializer, Adapter-selected traversal order, child output addressed outside its owning closure, renderer-owned media provider, generic application worker pool

**LUT Preparation Cache**:
The owner-scoped, entry-and-logical-byte-bounded resource-preparation cache directly owned by one Prepared Visual Program Cache. Preview Runtime and each Export attempt own different instances. Every low-frequency prepare reads and hashes the complete external `.cube` file before accepting a hit; a matching immutable `PreparedLut3D` may then be shared only within that owner. LRU eviction, scope rotation, clear, and online budget shrink retire cache residency synchronously, while already prepared Programs keep their own immutable `Arc`. An oversized LUT may be prepared and returned but is not retained. Its byte charge is a conservative scheduling/cache measure, not allocator, GPU-memory, Working Set, or RSS evidence.
_Avoid_: Process-global LUT cache, mtime/size or hot-time-window validity, filesystem polling during frame lookup, Preview eviction changing Export residency, hidden cache inside a convenience preparation helper, logical charge presented as process memory

**Effect Execution Contract**:
The Definition-owned declaration of exact admitted `(processing backend, sample representation)` modes, determinism, continuity state, past/future temporal input, ROI propagation, resource lifetime, and maximum graph topology for one visual Effect stage.
_Avoid_: Renderer inference from Effect type, Cartesian-product capability inferred from separate backend and precision sets, optimistic plugin defaults, ROI smaller than the admitted parameter range, capability booleans copied into parameters

**Effect Execution Envelope**:
The ordered per-stage Effect Execution Contracts plus their aggregate temporal/ROI/state/lifetime evidence and homogeneous exact-mode intersection. An empty exact-mode intersection denotes a valid heterogeneous chain whose execution needs explicit backend and/or representation transitions; it is not invalid author state.
_Avoid_: Inventing GPU-U8 from CPU-U8 plus GPU-F32, dropping an incompatible stage to force one mode, treating author validity as current executor availability, losing per-stage contracts after preparation

**Effect Execution Demand**:
The checked projection of one immutable Effect Execution Envelope, exact output Timeline Time, full frame extent, and requested output ROI into an exact or explicitly unbounded temporal window, clamped input ROI with evidence strength, and state/resource/exact-mode obligations. It is planning evidence, not a temporal frame provider, tile executor, or continuity Session.
_Avoid_: Hidden current-frame assumption, overflow-prone pixel arithmetic, treating unknown ROI as exact full-frame support, fetching temporal frames during graph compilation

**Prepared Temporal Value Program**:
The private time-expanded execution projection that references the unique `CompiledEffectGraph` IR by exact `(Clip time, graph value)` addresses. A `TemporalFrameBlend` samples its stable Definition-stage input at `time + sample_offset`; when earlier stages exist, the same `PreparedEffectProgram` is reevaluated at that exact time, so animated parameters, dynamic topology, frame seeds, and Masks retain their own semantics. Ordinary edges remain in one context and every cross-time edge strictly lowers the Definition stage index. Exact values/source times and use counts are de-duplicated; scalar dry-run and execution consume the same schedule. Stable stage contracts, declared temporal/spatial coverage, stateless CPU-F32 admission, 512-context and 65,536-value caps are mandatory. Internal same-stage temporal addresses, unbounded demand, or continuity state fail closed.
_Avoid_: Past-only offset type, one fetch per node for duplicate time, releasing a shared value before last use, separate Preview/Export temporal IR, output-time parameters applied to a sampled frame, treating internal plugin nodes as stable cross-time identities

**Prepared Mask Raster**:
The immutable, frame-extent-bound execution form of one evaluated Mask. Preparation validates finite geometry, enforces the 4,096-point author Path bound, flattens cubic Path segments once, and builds the nearest-segment index once. Direct and tiled consumers rasterize exact contained regions in full-canvas normalized coordinates, use bounded row scratch, and share cooperative cancellation and Float32 coverage semantics. Prepared geometry and row scratch are explicit Effect execution working-set obligations; they are neither author state nor a process-global cache.
_Avoid_: Rebuilding a Bezier polyline per pixel or tile, tile-local coordinates, hidden full-frame matte allocation, unbounded Path complexity, uncancellable raster loops, Preview-only Mask interpretation

**Mask Tracking Recipe**:
The persistent provenance for one atomically generated Mask shape-key range. It binds a stable Tracking ID to object-translation or planar-homography intent, direction, exact Clip-local anchor/range, bounded analysis settings, complete admitted source fingerprint, physical video stream and summary quality evidence. The App freezes exact author/media input and owns bounded decode/worker/cache/cancellation/publication; Effects owns only deterministic finite-frame analysis; Timeline owns the atomic key replacement. Manual shape authoring clears provenance, while decoded rasters, features, pyramids and caches never enter Project state.
_Avoid_: Viewer-current-frame analysis, render-time tracking, process-global result cache, persisted feature cloud, mtime-only reuse, partial key publication, stale result applied after a Sequence/source change, separate Qualifier spatial tracker

**Prepared Qualifier**:
The immutable Effects-owned interpretation of one HSL or 3D color selection plus bounded matte refinement. It consumes scene-linear working RGB and produces a Float32 `AlphaMask`; HSL uses circular hue and the exact working-space luminance basis, while 3D selection unions validated Include samples and subtracts validated Exclude samples in normalized RGB-cube space. Denoise, Gaussian feather, Clean Black/White, and inversion share one CPU/GPU semantic fingerprint and cooperative cancellation contract. Normal output consumes the matte through the canonical Mask node; Matte Preview is a separate typed `AlphaMask -> SceneLinearRgb` observation node. Preview and Export compile the same graph, and GPU private pass textures enter physical admission.
_Avoid_: Direct alpha mutation presented as a Qualifier, UI-owned key color, Preview-only matte math, untyped grayscale data, hidden full-frame scratch, excluding GPU refinement intermediates from admission, serializing Prepared Qualifier resources

**Effect Execution Session**:
The exclusive mutable execution lifetime owned by one Preview Runtime or one Export job. It owns one aggregate byte/entry grant partitioned across encoded, Float32, per-node, temporal Effect pixels, and dynamic graph-topology variants; an independent small entry/byte budget for GPU lowering plans and deterministic blockers; the active temporal/ROI execution limit covering retained source coverage, prepared Mask geometry, row scratch, live graph values, kernel scratch, final output, and tile work; and the currently bound scheduler generation. Prepared Effect/Visual Programs retain immutable zero-time topology and compiled graphs and never grow after cache admission. The canonical source-only identity graph is the only process-wide compiled graph; every non-identity graph is Program-owned, an uncached reference value, or bound through this Session. Production frame preparation binds the request generation once, then uses this same Session for ordinary Timeline graph evaluation, every exact-time temporal reevaluation, and later pixel execution; sampled dynamic topology therefore cannot escape into a caller-owned side cache. A valid dynamic topology that exceeds the Session grant is compiled for the current evaluation but not retained. Reconfigure and generation rotation synchronously retire topology alongside every pixel representation and GPU plan; Sessions never share a process-global frame, non-identity compiled graph, topology, or plan cache.
_Avoid_: Generic process-global Compiled Effect Graph LRU, mutable topology LRU hidden inside a Prepared Program, Session-free temporal preparation on a production path, caller stitching ordinary and sampled graph evaluation through different owners, Static Effect pixel/topology/plan cache, Preview eviction changing Export residency, per-frame Export Session creation, entry surviving a canceled/superseded generation, multiplying one declared budget per representation, over-budget topology treated as invalid semantics, GPU planning helper with hidden global ownership

**Effect Linear Stage Placement**:
A deterministic candidate placement for Definition-level Linear Chain stages over caller-owned lanes and explicitly declared adjacent backend/representation transfers. It is a shallow diagnostic projection retained for callers that need only stage feasibility; it is not executable route authority.
_Avoid_: Calling placement an executable data route, flattening a General DAG into topological order, implicit CPU/GPU copies, renderer-global capability guesses, placement evidence presented as executed pixels

**Effect Graph Value Execution Plan**:
A deterministic frame-local execution derivation over the unique `CompiledEffectGraph`. Every reachable Definition stage first binds its exact input value, output value, and emitted nodes; each node then retains the intersection of its real implementation modes and owning Definition contract. Planning assigns concrete lane/representation residency to every graph value, inserts only declared directed transfers, names producer completion tokens and waits, shares fan-out materializations, retires each non-output materialization immediately after its final consumer token completes, and checks host/device live-set, transfer, materialization, step, and arithmetic budgets. It is scheduling/resource evidence and never a parallel Effect semantic graph.
_Avoid_: Stage order treated as a data route, implicit upload/readback, whole-envelope capability copied onto every node, branch-local duplicate upload, release by scope exit instead of last use, unchecked frame-byte multiplication

**Prepared Heterogeneous Effect Route**:
The renderer-owned immutable CPU-Float32-prefix/GPU-Float32-suffix authority prepared from one Effect Graph Value Execution Plan before source pixels are materialized. It freezes compiled-graph identity, exact extent, graph-planning budget, a source-closed CPU dispatch DAG, an ordered frontier of one or more explicit CPU-to-GPU transfers, and an exact GPU dispatch/release schedule. Effects derives an opaque versioned shape identity over that schedule, its lane/backend choices, complete value formats and operation kinds while excluding extent and authored parameter values; Renderer exposes it and Export consumes it without rebuilding graph shape or reinterpreting domains. A linear point suffix within the fused-operation capacity remains one pass; longer linear suffixes split into exact admitted point dispatches. The bounded GPU DAG Implementation additionally executes point-operation fan-out followed by binary or ordered N-input canonical scene-linear BlendMode joins without introducing another semantic graph; Dissolve retains the complete frame seed and exact pixel identity. MultiInput with at least two inputs is a left fold over the shared compositor; its `N - 2` private intermediates remain Adapter resources, enter physical byte/texture admission and are never fabricated as semantic values. A one-input MultiInput retains a distinct graph-value lifetime through a bit-preserving GPU texture copy, not a shader or color reinterpretation. Preview and Export later bind a typed caller-owned working frame only when extent and resource authority still match; the worker executes the exact planned materializations without replanning. CPU fan-out clones only values with future consumers, joins release inputs at last use, retains every transfer boundary until atomic prefix completion, and admits Mask geometry, row scratch, kernel scratch plus the optimized live-frame peak against the Effect Session. The prepared route exposes the exact logical bytes retained by all frontier values; Renderer batch admission adds every one to the immutable input charge instead of assuming a single output. Synthetic Mask sources prepare Path/BVH and rasterize through the same generic checkpoint Seam as the owning heterogeneous attempt, preserving its exact cancellation/deadline error and never publishing partial pixels. Every transfer evidence carries its exact non-converting value format. The wgpu Adapter uploads scene-linear values as Working frames and `AlphaMask` values as `NonColorData`, validates every materialization, wait, signal and release before recording, and cleans earlier uploads if a later boundary cannot be admitted. Its GPU Mask dispatch consumes one Working input and one AlphaMask matte, applies the canonical Add/Subtract/Intersect/Difference plus inversion algebra, changes only straight alpha, and has real-device parity with the CPU Float32 reference. Because the one-command-buffer strategy cannot physically alias released textures, admission also freezes conservative physical recording bytes and texture count in addition to the abstract plan peak. Viewer admission consumes those same physical dimensions rather than reverting to semantic live-set evidence. The CPU completion carries every completed CPU node/token and each exact pending transfer/GPU continuation. Once accepted, a caller must submit or terminally fail the same attempt; the Seam never silently reinterprets the whole graph on CPU. A later backend transition or graph-internal readback, color-domain conversion beyond explicitly typed non-converting transfers, temporal input, and continuity state remain explicit blockers until their execution Adapters own the corresponding synchronization, cancellation, resource and parity contracts.
_Avoid_: Route choice after decode, CPU or GPU DAG flattened into a semantic chain, CPU work in Viewer/UI lowering, worker-side replanning, abstract live-set reported as physical wgpu residency, GPU capability probe presented as execution, changed extent or budget accepted after preparation, suffix failure followed by hidden whole-graph CPU fallback, completed upload/output token before Adapter evidence

The Effect Graph Value Execution Plan is dependency ordered rather than a
serialized backend clock. A CPU→GPU upload first needed by a later topological
consumer may therefore be listed after an independent GPU dispatch. Route
preparation accepts that layout only for an exact, non-converting
CPU-Float32→GPU-Float32 transfer with identical value format and explicit
CPU/GPU lanes, gathers every boundary value before GPU submission, and still
rejects GPU→CPU transfer or any CPU dispatch after GPU entry.

Product Power Windows are grade mattes rather than picture-alpha mutations.
Their unique compiled graph rasterizes each Window through `MaskSource`, reduces
the ordered stack through `MaskCombine`, and executes `MatteMix` as
`mix(base.rgb, graded.rgb, matte.a)` with `output.a = base.a`. Preview and Export
consume that same graph; CPU RGBA8, CPU Float32, temporal/ROI, and the admitted
real-wgpu suffix share the same Add/Subtract/Intersect/Difference semantics.

**Authoring Time Domain**:
The coordinate origin and mapping owned by a Sequence, Clip, Audio Component Edit, Audio Processing Scope, Transition, source, or other time-bearing author entity.
_Avoid_: Renderer frame grid, audio block, implicit clip-local flag

**Time Transform**:
A validated exact mapping between two Authoring Time Domains created by placement, trimming, speed mapping, or nesting.
_Avoid_: Rewriting a time base, floating-point seconds bridge, implicit cross-domain comparison

**Evaluation Grid**:
The consumer-specific frame, shutter-sample, audio-sample, or parameter-event instants at which authored semantics are evaluated.
_Avoid_: Persisted authoring time base, UI snap setting

**Picture Geometry Projection**:
The deterministic affine conversion from source and output authoring extents to
the concrete sampled extents used by one Preview or Export execution. Authored
Clip transforms never change when decode quality or delivery resolution changes.
One resolved picture contract applies encoded sample aspect ratio, typed scan
identity, and cardinal display orientation before the Clip transform, rejects
unsupported matrices, and keeps Sequence sample aspect ratio at the
Viewer/delivery boundary. Interlaced/unknown sources bypass progressive-only
proxy artifacts. Proxy generation otherwise preserves the original stored
orientation and resets only the proxy's physical SAR, so source and proxy share
the same interpretation.
_Avoid_: Applying authoring-space scale directly to a downsampled source,
delivery-size-dependent author state, decoding one cache entry at multiple
unstated extents

**Media Field Processing Session**:
The source-Session-owned, decode-keyed conversion from stable decoded scan
evidence into canonical full-height progressive field-time frames. It preserves
exact `tt`/`bb`/`tb`/`bt` transport evidence, uses motion-adaptive field-rate
processing for qualified TFF/BFF input, resets temporal state on seek/cancel,
and rejects mixed scan, changing dominance, residual interlace, or a required
GPU-native payload.
_Avoid_: Coded-first/display-first conflation, per-frame filter graph, metadata-
only deinterlace, proxy silently erasing scan identity

**Program Picture Sampling**:
The Renderer-owned projection of one encoded Program picture into either one
progressive exact evaluation or two display-ordered full-raster field-time
evaluations with explicit extraction parity. Effects, Transitions, nesting,
temporal demand, seeds, and caches consume those ordinary exact samples; only
the Export Interlaced Delivery Module vertically prefilters and weaves them.
_Avoid_: Woven frame entering Effects, same timestamp for both fields, encoder
field flag treated as pixel semantics, float master interlace

**Parameter Schema**:
One versioned definition-stable contract for a parameter's `ParameterId`, value type, definition default, automation capability, unit, numeric or enum constraints, admitted Hold/Linear/Bezier execution semantics, localization message identity, and cache impact. Editor presets such as Auto Bezier and Ease author Bezier handles and are not separate execution semantics. Processor execution capabilities remain on the Processor/Effect Definition and compiled graph.
_Avoid_: Instance property path as identity, UI-only min/max, duplicated CPU/GPU or color-domain claims

**Parameter Instance Address**:
A stable authoring and command-routing address for one parameter instance,
formed from its stable owner/animation-track identity and definition-stable
`ParameterId`. A property path is only a current lookup alias and may change
without changing this address.
_Avoid_: Property path as identity, ParameterId derived from display name, suffix matching during execution

**Clip Parameter Write**:
One non-empty, duplicate-free atomic gesture over persistent parameter instances owned directly by one Clip, addressed by canonical Clip ID plus Parameter Instance Address and evaluated at the current clamped Clip Visual Author Time. Track placement and lock are resolved at admission and again by the author transaction; Effect, Mask, synthetic Blend/Solid Color fields, and child-owned properties are outside this Interface. If animation is enabled, the write edits or creates the authoritative complete key and preserves existing key identity, handles, interpolation, and temporal flags. Any stale address, invalid value, duplicate target, or failed member rejects the complete gesture before an author candidate exists.
_Avoid_: Viewer- or Inspector-specific transform semantics, Track/range snapshot in the payload, property path as command identity, hidden static write beneath an active curve, partial multi-field commit

**Parameter Resource Reference**:
A recoverable typed parameter value representing unbound intent, a Project Asset, an external file, or a URI, with resource-level invalidation semantics.
_Avoid_: Free-form path text treated as a loaded resource, persisted resolved/available flag

**Display Timecode**:
A presentation contract for formatting timeline positions with a start offset, nominal rate, and drop/non-drop-frame rules.
_Avoid_: Timeline storage coordinate, arithmetic duration, Clock Master

**Audio Component Edit**:
One persistent, placement-local selection and edit of a media audio component or nested Sequence Output, owned by exactly one Timeline Clip. Its stable author address is `(TrackId, ClipId, AudioComponentEditId)`; logical source selection and all field mutations use that same address and one atomic candidate-validation Interface. It owns enable/Role, edit-local gain/pan/fades/automation, and a restricted binding into an Audio Processing Scope; Track and Sequence range are always derived from the owning Clip. Timeline validates source kind and aggregate coherence, while an application Adapter separately proves recoverable Asset-catalog or child-Output membership before commit.
_Avoid_: Inspector-specific source payload, duplicated Track/range placement, entire audiovisual Clip treated as one audio stream, routing Bus

**Audio Processing Scope**:
One Sequence-owned shareable audio processing definition containing input trim/automation and an ordered Processor Rack. Multiple Audio Component Edits may bind to it with only a Scope ID and exact `scope_in`; it never owns placement, speed, source selection, or output routing.
_Avoid_: Second Clip, arbitrary time transform, implicit copy-on-write

**Compiled Audio Contribution**:
One non-persistent PCM-bearing execution branch derived from an owning Track/Clip plus one Audio Component Edit and its Audio Processing Scope.
_Avoid_: Persisted placement authority, user-routable graph node

**Audio Program Execution Demand**:
Immutable conservative evidence derived from one compiled, selected Program Output Signal Closure. `ProvenSilent` authorizes a consumer to omit the Runtime and produce exact silence; `RequiresExecution` preserves canonical Runtime construction whenever a routed Contribution or active Processor could produce source signal, a tail, or generated signal.
_Avoid_: App Track/Clip scan, “has audio track” heuristic, media-probe audibility guess

**Audio Audition Overlay**:
Open-Session, non-persistent intent that restricts only a root Sequence's compiled Track program sources while preserving the downstream Signal Closure. It is bound to one Authoring Session and Sequence, never enters Undo/dirty/export state, and is never inherited by a nested Sequence Runtime.
_Avoid_: Persisted Track solo, muting author state to audition, parent Track IDs applied inside a child Sequence

**Audio Meter Bank**:
One Session-local, block-atomic observation of every executed Track, Bus, and Program Output post-mute target for the same public Sequence sample range. Measurement is staged before scratch reuse and published only after complete block success; missing targets mean unexecuted, not zero. Its sample peak/RMS facts are neither true peak nor standards loudness.
_Avoid_: Per-target publication epochs, hidden-preroll readings, partial failed-block publication, callback locks/allocation, zero fabricated for an absent target

**Prepared Audio Schedule**:
One immutable Render-Contract-bound lowering of compiled audio semantics into dense topological node slots, destination-contiguous Route and Contribution ranges, Transition bindings, exact sample spans, validated automation event spans, liveness-assigned scratch slots, admitted processor-private Session storage, and a selected processor kernel backend. It is execution data, never author data, and a Render Session may scan neither author collections nor routing maps after preparation.
_Avoid_: Compiled Audio Program, runtime graph wrapper

**Audio Program**:
The Sequence-owned persistent authoring model for mixer channels, mix buses, typed routes, processors, automation, and exposed outputs.
_Avoid_: Project-global PCM graph, Track as mixer and bus

**Audio Routing Node**:
A persistent user-addressable Track Mixer Channel, Mix Bus, or Program Output with type-specific identity, ports, and lifecycle in one Audio Program routing model.
_Avoid_: Generic property node, universal untyped ID, generated Clip DSP operator

**Audio Route**:
A persistent typed connection between stable signal endpoints in one Audio Program.
_Avoid_: Node-name connection, array-index connection, hidden fallback route

**Audio Processor Sidechain Route**:
A persistent typed auxiliary connection from one exact Track/Bus strip port to one stable Audio Processor Instance and definition-owned bus key. It participates in complete routing reachability and cycle validation but never sums into a Routing Node main input. Preparation must negotiate the key/layout against the realized processor contract and align the detector signal to the processor's main-input signal time.
_Avoid_: Soloing a Track to manufacture a stem, mixing detector PCM into the main Bus, plugin array-index bus address, unvalidated string lookup, callback allocation

**Audio Processor Instance**:
A persistent built-in or external audio effect instance identified by a stable definition. Each parameter captures the shared Parameter Schema plus one exact-time curve whose default is the unkeyed value, so unavailable external processors preserve editable author intent without a second static-value truth.
_Avoid_: Video EffectNode, plugin file path, registry index, opaque JSON effect

**Audio Processor Rack**:
An ordered collection of Audio Processor Instances at one explicit insertion point of an Audio Processing Scope, Track Mixer Channel, Mix Bus, or Program Output.
_Avoid_: Unordered effect set, format-specific VST chain, hidden master effect

**Generated Audio Processor Occurrence**:
One non-persistent prepared execution instance derived from an authored Audio Processor Instance plus its exact Contribution or Routing Node owner and insertion point. A shared Processing Scope may produce several occurrences; each owns independent mutable Session state.
_Avoid_: Sharing DSP state because author definitions match, flattening a Rack into aggregate gain, processor array index as identity

**Audio Processor Tail**:
The declared meaningful output extent after a Processor's input becomes silent: none, finite non-zero sample frames, or infinite. It excludes hidden algorithmic group delay/lookahead, which is a separate PDC fact.
_Avoid_: Latency alias, guessed reverb timeout, zero-valued finite sentinel

**Linked Sample-Peak Lookahead Limiter**:
The canonical built-in Processor whose fixed prepared lookahead observes the maximum absolute sample across every channel, delays the complete interleaved signal and its parameter time, applies one shared gain envelope, and reports that delay as algorithmic latency. It guarantees the authored sample ceiling, including a final rounding guard, but makes no inter-sample or true-peak claim.
_Avoid_: Per-channel image shifts, block-local lookahead, parameter automation moved by PDC, audible delay reported as tail, sample peak labelled true peak, callback allocation or window rescanning

**Audio Public Output Lookahead**:
The bounded internal sample-frame lead required to return one Program Output already aligned to its public Timeline coordinate. It is admission and capacity evidence, never public PCM delay or author-time offset.
_Avoid_: Leading silence exposed to consumers, nested latency propagated after normalization, device latency

**Audio Parameter Event Batch**:
A borrowed, Session-backed block of stable Parameter-ID lanes and exact sample-offset value events for one Generated Audio Processor Occurrence. Static values use one offset-zero event; varying curves are lowered sample-accurately without callback allocation.
_Avoid_: UI-rate parameter polling, per-block heap construction, plugin parameter index as semantic identity

**Audio Transition**:
An explicit Sequence-time-bounded relationship between two Audio Component Edits that applies a declared pair of sample-domain gain curves after their component/scope-local processing.
_Avoid_: Overlap inferred as crossfade, unary fade plugin, Track-wide dissolve

**Audio Role**:
A Sequence-owned single-valued semantic classification for an Audio Component Edit, optionally parented to another local Audio Role; “role” and “subrole” describe hierarchy position rather than different entity types.
_Avoid_: Project-global role object, routing node, display name as identity

**Standard Semantic Key**:
An optional namespaced classification value used for templates, matching, search, and presentation without replacing a Sequence-local Audio Role identity.
_Avoid_: Global Role ID, dynamic routing selector, mutable Project authority

**Sequence Semantic Catalog**:
The Sequence-root collection that owns Audio Roles and other authoring semantics shared by its Timeline, Audio Program, and public output interface.
_Avoid_: Project role registry, Mix Graph node collection

**Strong Entity Reference**:
A typed reference that must resolve inside its owning validation closure before a snapshot can exist. That closure is Sequence-local for local entities and the Project dependency closure for Project-contained nested Sequence/output references.
_Avoid_: Missing local node or Project-contained nested Sequence/output treated as runtime availability

**Recoverable Dependency Reference**:
A stable reference to media, plugin, font, or another real-world provider binding outside the closed Project author graph that retains its expected contract while the current binding remains derived.
An Asset Library record is a Project-contained strong entity; only the concrete source/component provider binding inside it is recoverable. A missing Project-owned Asset record is not an offline-media condition.
_Avoid_: Missing internal Route endpoint, missing Project-contained Asset or nested Sequence/output, persisted resolved flag

**Asset Library Membership**:
The user-visible inclusion of one strong Project Asset record in ordinary Library/bin listings. Retirement is one Asset Library transaction, not an Author Transaction: it changes only this membership, so the complete record, expected contract, recoverable provider binding, every Timeline placement, Project proxy intent, and Undo/Redo endpoint remain valid. Sequence/Project Undo and Redo neither hide nor restore Library membership; the next Undo/Redo still targets the preceding Author Transaction while the record remains retired. Reimporting the same concrete path is the current restoration operation and preserves the same record identity. Record-level physical purge is not a product operation until one authority can prove the Asset absent from the current Project and both History stacks.
_Avoid_: Deleting a strong record, recording Library retirement in Sequence/Project History, removing Clips as a Library side effect, missing row treated as offline, per-item partial batch

**Generated Audio Stage**:
A non-persistent IR operation deterministically lowered from Clip, Lane, Transition, Route, or processor authoring semantics.
_Avoid_: User-routable Clip node, anonymous diagnostic stage

**Audio Render Admission**:
A per-execution decision that requested logical audio outputs, exact artifacts, processors, decoders, state, and buffers are prepared.
_Avoid_: Audio-device availability, deadline guarantee

**Monitor Sink Admission**:
A per-listening-path decision that a user- or workspace-scoped monitor graph is bound and prepared to feed one physical device.
_Avoid_: Program Output compatibility, transport permission

**Signal Closure**:
The resolved typed audio dependency subgraph required to produce selected logical outputs under one processing contract.
_Avoid_: Consumer purpose, scheduler window, hash alone

**Audio State Domain**:
The mutable DSP state scope determined by a Signal Closure, continuity epoch, processor origins, nested instance path, time mapping, direction, and processing mode.
_Avoid_: Global plugin state, cache entry, fingerprint-equivalent graph

**Sequence Output Port**:
A stable typed audio output explicitly exposed by one Sequence for nesting, playback, analysis, or delivery.
_Avoid_: Child track reference, output array index, implicit stereo mixdown

**Delivery Mapping**:
A Project or export-job contract that packages selected Sequence outputs into files, containers, channels, and delivery metadata.
_Avoid_: Internal stem selection, physical device routing

**Monitor Path**:
A user-, workspace-, or session-scoped graph that maps logical program outputs to physical listening devices without changing the Audio Program.
_Avoid_: Program master processor, project-owned device ID

**Authoring Session**:
The sole mutable authority for one open Project lifetime: canonical Project Document, Asset Library authority, Sequence navigation, bounded project-wide history, author generation, and manual/autosave baselines.
_Avoid_: AppState fields mirroring Project data, mutable renderer snapshot, active-Sequence-only history

**Sequence Author Contract Certificate**:
Opaque, process-local evidence that one exact immutable Sequence snapshot satisfied the sole local author-contract validator in one exact Project Color Environment. Replacement preparation proves the exact baseline and a newer revision, always reruns global identity/link, Transition, and Audio Program rules, and may skip only unchanged local Track/Clip/Effect/Mask subtrees.
_Avoid_: Serialized proof, allocation ID as correctness evidence, dirty flags, partial validator that can accept a state rejected by the full path

**Sequence Dependency Certificate**:
Opaque, non-serialized cross-Sequence evidence containing private nesting/output facts and a strong reference to the exact canonical `AuthoringList<Sequence>` COW root that produced them. Active navigation is not anchored but must resolve. Full and incremental paths share one extractor/checker; changed fact inputs detach while the certificate lives, and any stale author baseline fails before fact reuse.
_Avoid_: Body-free index with allocation-keyed facts, persisted dependency graph, scanning unrelated Clips for one Sequence edit, revision-only freshness, second nesting interpretation

**Project Authoring Validation Certificate**:
The Authoring Session's sole opaque validation evidence, combining every Sequence Author Contract Certificate with one Sequence Dependency Certificate. It is non-serialized, non-cloneable, absent from History/execution, and installed only as the pair returned with its exact validated Project document.
_Avoid_: Separate Session dependency cache, author-state getter, certificate in Project JSON or Undo endpoints, document and certificate assembled from different outer COW roots

**Author Generation**:
A process-local monotonic identity assigned to every committed Project author state in one Authoring Session. It is neither a Sequence execution revision nor a successful-save revision.
_Avoid_: Document revision, Sequence revision, frame generation

**Author Transaction**:
A detached transformation of either one Sequence or the complete Project Document. Sequence-scoped validation consumes its candidate into one prepared document/certificate ticket before History preparation; History then reads the ticket's validated replacement and, after commit, the paired document/certificate installs without failure. The Project-scoped Interface accepts, normalizes, fully validates, and installs one complete detached document with a rebuilt Project Authoring Validation Certificate. Revision arithmetic, exact-before checks, semantic validation, and all fallible preparation precede the commit boundary.
_Avoid_: Mutate then record, best-effort rollback, UI-owned command side effect, treating a scoped History payload as the Project transaction Interface

**Project Restore Point**:
The typed before/after History endpoint for one structural Project transaction. It retains Project identity and Project-owned author fields, canonical Sequence order/default and the structural active fallback, plus exact body-or-absence state only for the transaction's affected Sequence identities. It excludes successful-publication evidence, materializes a complete candidate only after the current document matches the contiguous source endpoint, reuses current unaffected Sequence bodies, and preserves valid current navigation.
_Avoid_: Complete `ProjectDocument` History payload, sparse edit command, command replay, a second delta author model, rolling back document revision, save-updated metadata, or valid active-Sequence navigation

**Durable Project Publication**:
The typed filesystem boundary at which a flushed and validated Project archive or Recovery Manifest has both atomically created/replaced its target and completed the containing-directory durability barrier. Only confirmed durable publication evidence is success. `BeforeNamespace` proves the target did not acquire the new object; `DurabilityUnconfirmed` proves it did but cannot prove crash survival; `NamespaceIndeterminate` proves neither postcondition. The latter two may have changed the visible namespace but never advance the saved baseline, retire Recovery Authority, or authorize cleanup.
_Avoid_: ZIP writer close alone, temporary-file existence, enqueue success, rename success or target visibility as durability, boolean/string failure that erases namespace postcondition, rename-old-then-rename-new sequence

**Prepared Project Archive**:
A single-consumption open ticket over one retained `.mdp` file object. Preparation enforces the exact archive entry set and an explicit compressed/decompressed read budget, validates the Manifest and canonical Project exactly once, and exposes only the stable Project ID needed to acquire or verify the Runtime Lease before the same ticket extracts its CRC-checked Library into a fresh generation.
_Avoid_: Project preflight followed by a second Project deserialization, reopening the archive pathname after validation, allocating JSON before size admission, media-duration-derived archive limits, extracting SQLite before Project/runtime authority agrees

**Exact Endurance Project Fixture Install**:
The validation-only App lifecycle policy whose public input is one prepared machine plan. It pauses the previous Persistence Generation before filesystem access, resolves one canonical direct `.mdp`, retains one file object across bounded whole-file SHA-256, rewind, `PreparedProjectArchive` preparation and extraction, and installs it only when the active Sequence exactly matches the plan, owns video plus audio Tracks, and the source Project document plus Library already use current schemas. It creates a saved Author Generation 1 Session with empty History, never repairs author state, and resumes a failed candidate before retiring an existing Session. Success returns private-construction typed evidence binding machine plan, archive, Project/document, root Sequence/revision, Session/Author Generation, and live Library revision; later author or Library mutation invalidates it. Windows retains read-only sharing that denies writers and replacement; macOS/Linux fail closed until their native immutable-object Adapter is implemented and qualified.
_Avoid_: Raw path/hash public input, ordinary Project Open in the endurance factory, path hash followed by reopen, handoff after opening the source, `ensure_minimum_tracks`, runtime Library migration, copying the graph into a synthetic fixture, accepting a non-active planned Sequence, Windows receipt claimed as Unix filesystem authority

**Prepared Endurance External-Source Inventory**:
The validation-only strict schema-1 closure derived from an unchanged Exact Endurance Project receipt and the complete ordered profile topology sealed into the prepared machine plan; callers cannot substitute a phase slice. It recomputes phase Sequence/media reachability through the production Export dependency compiler: Playback is Entire Sequence plus primary Audio, Export uses its exact range and retained machine-plan preset, and Recovery is their union. Declared sources are the exact union, including reachable retired strong records and excluding reachable proxy-mode Assets. Preparation checks Session/Author/Library evidence before and after capture, retains the receipt, and must be revalidated at phase-owner admission. Windows non-follow-opens each direct source, hashes bytes and captures `MediaFileFingerprint` from that same read-only-sharing object, matches the admitted Asset probe, rewinds, and retains every source, inventory, preset, and optional semantically validated Broadcast QC profile handle through owner shutdown; a factory consumes the typed artifacts and never reopens their paths. macOS/Linux fail closed until descriptor-based immutable-source execution is qualified.
_Avoid_: App-owned Track walker, `list_assets`, hash then pathname reopen, unretained source handles, source extras, proxy execution against an original-file digest, stale Project/Library receipt, Unix advisory file descriptor claimed as immutable authority

**Project Archive Publication Evidence**:
The opaque, private-construction, non-cloneable point-in-time result of one confirmed durable Project archive publication. It is consumed by value and binds exact absolute path, `CreateNew`/`ReplaceExisting` mode, embedded Project ID/document revision, and whole-file length/SHA-256 proven through the retained construction object after entry/CRC validation. Recovery Manifest publication that admits a new point accepts only `CreateNew` evidence and derives its archive identity facts from this value; later discovery and recovery must rehash and reparse the current direct file object because the evidence grants no continuing path authority.
_Avoid_: Caller-constructed receipt, cloneable lease, path plus independently supplied hash/revision, reopening and deserializing the just-saved archive, replacement evidence admitted as an immutable recovery point, construction receipt treated as later pathname authority

**Manual Project Destination Binding**:
A Session-scoped monotonic identity pairing the latest successfully admitted manual-save intent with its exact canonical Project path. Save As advances the binding at queue admission; every following ordinary Save reuses it even before the first publication completes. Only a completion matching the current binding may change the canonical path, manual durable baseline, or Recovery Authority.
_Avoid_: `update_project_path` boolean, resampling `AuthoringSession::project_file` while Save As is in flight, request ID as path authority, obsolete destination completion clearing dirty or recovery state

**Recovery Manifest**:
The strict versioned Project-runtime index that binds one canonical Project ID/absolute path to immutable `CreateNew` autosave archives by exact absolute child path, timestamp, Author Generation, Asset Library revision, embedded document revision, and SHA-256. Each archive leaf contains wall-clock, Author Generation, and random UUID identity; new-point publication consumes exact Project Archive Publication Evidence only after the archive is durable.
_Avoid_: Directory scan as authority, unversioned file list, path-only recovery candidate, manifest published before its archive, overwriteable autosave leaf, filename timestamp as uniqueness or authority

**User State Directory**:
The narrow platform Interface that discovers one absolute, stable per-user state anchor without creating or modifying it. Its Windows, macOS, and Linux Adapters resolve Local App Data, Application Support, and XDG state/Home respectively. Every caller appends its own product namespace and remains responsible for creation, permissions, symlink/object identity, locking, and durability.
_Avoid_: Process TEMP/TMPDIR/XDG_RUNTIME_DIR, UI/App code calling native discovery directly, generic platform code creating Project directories or owning Recovery Authority

**Project Runtime Owner Manifest**:
The strict durable identity that binds one stable runtime root to the complete domain-separated SHA-256 of the normalized Project publication target that first allocated it and to exactly one Project ID. It authorizes root reuse and child deletion but owns no recovery point or mutable canonical Project path; Save As keeps the allocation identity and root stable.
_Avoid_: Lossy display path as authority, unresolved filesystem aliases, truncated hash, directory name alone, Recovery Manifest duplicate, deleting an unowned runtime tree

**Project Runtime Lease**:
The kernel-backed filesystem authority for one exact paired Project runtime root. It encapsulates a shareable in-process logical authority (one Project-ID lock plus retained publication-target locks in the stable per-user namespace) and one root-local `session.lock`; copied paths may own distinct paired leases sharing that logical authority only inside one coordinated App. The active Session and persistence/autosave/recovery work share the exact root lease, while a retired Project Library Generation may retain its old lease. Every mutation revalidates the applicable handles and admitted absolute publication routes; the OS releases authority on Drop/process death. It is not Authoring Session or Asset Library generation authority.
_Avoid_: Lock namespace derived from process TEMP/TMPDIR, lock-file presence as authority, PID liveness probe, path-string equality, parent-symlink retarget, Unix unlink-and-replace, two processes opening copies with one Project ID, two publishers targeting one filesystem alias, bare root-plus-Project-ID mutation seam, lease treated as Session-generation freshness

**Persistence Generation**:
The admission lifetime of durable work submitted by one exact Authoring Session. Open, Recovery, Close, and Project replacement first close admission and wait for a FIFO worker barrier; failure may resume only the matching pause token, while success retires it permanently. Barrier completion proves all earlier publication and request-owned Asset Library/runtime-lease references are gone.
_Avoid_: Runtime lease as freshness, pending-count polling, completion-time stale filtering after physical publication, close-and-hope

**Project Library Generation**:
A unique immutable runtime directory containing one live Project Asset Library database. Archive extraction completes before SQLite is opened; after open the directory is never renamed, replaced, or removed until weak lifetime evidence proves every strong `Arc<AssetLibrary>` has been released. Its retired record retains the exact runtime lease so another process cannot claim or sweep the tree during that interval.
_Avoid_: Mutable `runtime/library/`, renaming an open SQLite database, deleting a Unix inode still held by a connection, directory swap as Session commit

**Recovery Authority**:
The right of an exact validated autosave archive, while referenced by the canonical Recovery Manifest, to be offered and opened as a new dirty Authoring Session. Discovery emits a complete immutable candidate containing Project ID, exact paired root, absolute paths, revisions, timestamp, hash, and typed canonical-target evidence (`Missing` or same-Project `Present { document_revision }`); lower-than-canonical same-Project document revisions are stale, while equal revisions and missing canonical archives remain eligible. Product confirmation projects that evidence and never reads Project files itself. Selection preflight is evidence only: a canonical target whose presence or revision changed after discovery requires a fresh confirmation, and after live Project authority is acquired the same target evidence, unique snapshot paths, complete manifest entry, direct source and staging file objects, copied bytes, embedded identity/revision, canonical Project identity/revision, and final manifest membership must still agree. Only the exact verified staging file object may enter the archive loader. Only a current covering manual Project publication may first retire that authority durably and then remove the archive.
_Avoid_: Latest filename wins, UI-inferred target state, click-to-overwrite recovery, stale confirmation after target change, duplicate-path ambiguity, path-only preflight as admission, leaf symlink/reparse following, verify-then-reopen pathname race, copy after unchecked manifest change, delete-on-open, stale save completion clearing recovery, recovered Session treated as clean

**Clip Content**:
The one closed payload that identifies a Clip placement as Media, Adjustment Layer, Nested Sequence, Solid Color, or Basic Title and carries only that variant's external references, interpretation data, or generated-source author state.
_Avoid_: Parallel Clip kind/asset/nested/color/title fields, fake Asset ID for a nested Sequence or generated source

**File-backed Still Asset**:
An Asset Library picture identity whose probe proves exactly one physical
picture frame. Its Timeline placement remains ordinary Media content but
carries a zero-rate source Time Transform, so placement duration is editable
while every evaluation samples the same source instant.
_Avoid_: Fabricated video duration, a second still-only Clip payload, repeating
EOF frames as implicit hold semantics, classifying multi-frame or unknown-frame
image content as a still

**Picture Source Extent**:
One immutable admission fact for a concrete picture source: either an Asset
Library-classified `StillImage` that may be held for arbitrary before/after
handle demand, or one
exact non-empty half-open Timeline Time range. File-backed Export evidence stores
this fact beside the same selected video-stream identity and source fingerprint;
a nested Sequence supplies its zero-based exact output range from the same
frozen Sequence revision. The Timeline's pure selected-Transition validator
consumes these facts without live Asset Library, Project, filesystem, or FFmpeg
access.
_Avoid_: `None` meaning Still/generated/unresolved at once, fabricated still
duration, container duration substituted for video-stream duration, visually
static nested output classified as Still, `total_frames == 1` granting Still
authority to a Movie, live dependency lookup after snapshot capture

**Clip Visual Author Time**:
The stable exact Clip-local coordinate used by every Clip-owned visual property: Transform, Opacity, visual Effects, Masks, and generated visual content. Moving a Clip or changing its source selection/speed preserves this coordinate; trimming away the in edge, splitting, or creating a right-hand fragment advances the visible `clip_time_in` by the removed placement duration.
_Avoid_: Sequence time used directly for Clip properties, source time used as visual automation time, independently chosen time domains per visual subsystem

**Basic Title**:
A Sequence-local generated Clip content type whose closed definition-backed Property Bag is evaluated in Clip Visual Author Time and rasterized as tightly cropped straight-alpha working-linear picture before the ordinary Clip transform/effect/mask/composite path.
_Avoid_: Asset-library text generator, renderer-only TextLayer, text Effect, implicit system-font fallback

**Basic Title Font Dependency**:
The exact requested system font family/weight/style plus the resolved face bytes/index fingerprint used by one generation Session. Preview binds and revalidates the live catalog within its generation. Export resolves selected queries at queue admission, deduplicates and retains exact source bytes under a hard grant, then constructs a font database containing only those bytes; later system-font changes cannot alter the job. Missing families, inaccessible bytes, byte-budget overflow, or an undeclared fallback face are recoverable execution blockers and never authorize visually different substitution.
_Avoid_: Font display name as output fingerprint, platform default fallback, silent emoji/CJK fallback chain

**Clip Link Group**:
A Sequence-local set identity shared by two or more Clip placements whose ordinary editorial selection and structural edits are synchronized.
_Avoid_: Pair pointer, linked-list chain, singleton group, media ownership relation

**Timeline Selection Edit**:
A closed Product intent whose operands are the current open-Session Clip selection and, when required, the current playhead. Link, Unlink, Trim-to-playhead, Roll-to-playhead, and batch Enabled changes resolve stable Clip identities at dispatch, preserve primary-first selection order where an anchor is semantic, expand complete Link Groups where structural synchronization applies, and share read-only admission with execution before one Author Transaction.
_Avoid_: Widget-carried selection snapshots, UI-only lock/trim guesses, partial linked edits, repeated values committed as success, one external action name per enum branch

**Timeline Clip Gesture**:
A closed direct Move or edge-Trim Product intent addressed by stable Clip/Track identities plus one explicit frame-grid position. The App's deep gesture Module lowers that coordinate once, derives Track kind from current author state, expands complete Link Groups, validates every source/destination lock and boundary, and prepares the complete mutation before one Author Transaction. A linked Move preserves exact member-relative Timeline Time, including sample-accurate audio offset, plus same-kind Track offset; if any destination falls outside its Track set the whole gesture fails rather than clamping or partially moving. For edge-Trim, the first explicitly addressed member of each Link Group is its anchor: its requested exact edge delta is clamped once against the complete group's common legal interval, then applied identically to every member. Existing offset/J/L edges therefore remain offset; this synchronized gesture does not create or independently change that offset. Stale identities, invalid grids, negative time, locked members, and semantic no-ops fail closed.
_Avoid_: Bare frame integer, copied video/audio boolean, display Track index, linked audio quantized to video frames, clamped linked Track offset, unordered anchor selection, per-member Trim clamp, implicit J/L edge alignment, sequential partial trim, stale member skipped as success, Widget-owned link or lock policy

**Timeline Edit Targeting**:
Per-open-editor-Session policy keyed by stable Sequence and Track identity. Target decides whether a structural edit cuts Track content; Sync-Lock independently decides whether downstream placements follow a program-time ripple. Both default on, are compiled into explicit command scopes, and are not renderable Sequence author state.
_Avoid_: Persisted Track flags, selected Track as implicit target, hidden expansion of a command scope, Sync-Lock treated as content selection

**Track Product Action**:
The sole product-operation Interface for Timeline Track creation, stable relative ordering, persistent Visibility/Mute/Lock authoring, and transient Target/Sync-Lock policy. Existing Tracks are addressed only by `TrackId`; ordering uses a same-kind stable anchor in canonical author order. Persistent controls enter one Author Transaction, while edit policy changes only the open Session.
_Avoid_: Display/list index as author address, redundant video/audio boolean beside `TrackId`, audio Visibility, video Mute, Target/Sync-Lock in Project History, repeated value reported as success

**Timeline Asset Placement**:
A closed Timeline Product intent that addresses one Asset, one existing Track, and one explicit frame-grid position. The App placement Module converts that coordinate exactly once, re-resolves Track kind and lock state from current author state, and derives compatible media placement from current Asset evidence before entering one Author Transaction.
_Avoid_: Copied video/audio booleans, display Track index, bare frame integer, Widget-owned media compatibility, separate drag/drop author path

**Range Edit**:
One atomic Lift or Extract over an exact half-open Sequence-time range and explicit content/ripple Track sets. Lift removes intersecting targeted content without changing program time; Extract removes it and closes the interval on its admitted ripple closure.
The Product Action carries only the closed edit kind and resolves the latest authored In/Out plus open-Session Target/Sync-Lock policy into this complete request at dispatch. Ruler-authored In/Out input carries an explicit frame grid and lowers once to canonical author time.
_Avoid_: Repeated per-Track deletes, Widget-owned ripple rules, copied stale range/Track snapshots, bare frame integers, silent cutting of untargeted Sync-Locked content, local PushForward collision mode

**Precompose Action**:
One Project-scoped Author Transaction that resolves the current stable-ID Clip selection, expands complete link groups, prepares the complete identity/lock/time/Track plan without mutation, projects selected author content into a new Nested Composition Sequence, and replaces only the represented video/audio placement kinds in the parent. Candidate construction detaches only affected Track Clip lists.
_Avoid_: Widget-built child graph, stale or partially resolved Clip identity, invisible video placement for audio-only content, separately committed child and parent edits, whole-Project COW detachment

**Video Transition**:
A Sequence-owned, typed two-input visual author entity with strong adjacent Clip endpoints, an exact Sequence-time interval, stable instance identity, and validated source-handle demand.
_Avoid_: Clip opacity preset, Track-owned transition, inferred overlap, clamped source request

**Video Transition Product Action**:
The closed App action algebra for selecting, creating, exact-range editing, and removing a Video Transition. External intent carries only stable Clip/Transition identities, an exact Sequence-local Timeline Time range when applicable, and an explicit source-handle policy. The Timeline panel Adapter lowers a frame-grid resize proposal once; the App authoring Module re-derives endpoint Track membership, lock state, current range, and source extents before one transaction. A no-op range write creates no History entry.

**Video Transition Handle Diagnostics Snapshot**:
An immutable, process-local observation prepared by the App's Video Transition Module for one exact Authoring Session, Project Author Generation, active Sequence revision, and Asset Library instance/revision. Timeline projection consumes the complete map once and never resolves source extents per Track or per UI row. It is diagnostic evidence only, never author state: unavailable external dependencies remain distinct from invalid in-memory author structure, and a preparation spanning an Asset Library revision change is not published for reuse.
_Avoid_: Timeline namespace string interpretation, persisted Track identity, bare start/end frame authority, implicit handle shortening, UI-owned source admission

**Project Migration**:
An ordered, transactional transformation of one persisted archive, document, or SQLite schema version into the next supported version.
_Avoid_: Best-effort deserialization, ignored ALTER error

## Relationships

- A **Playback Session** has exactly one active **Clock Master**.
- A **Playback Session** has exactly one **Transport State** at a time.
- One user Play or Seek intent applies its Sequence identity, semantic revision, evaluation grid, content boundary, position, and resulting Transport State atomically and rotates exactly one playback epoch; an App Adapter cannot expose intermediate reset/seek/play states.
- A **Playback Session** produces zero or more **Frame Demands**.
- Each **Frame Demand** produces at most one terminal **Frame Delivery**.
- App presentation completion returns the accepted, ticket-classified **Frame Delivery** separately from whether the public Transport snapshot changed. Adapters must not use snapshot mutation or Evidence-counter deltas as a proxy for terminal acceptance; `Ready` and allowed `Degraded` are presentable, while `Late` is not current output.
- The current **Frame Demand** is the sole source of truth for terminal identity and target. Playback position changes refresh that demand atomically; the Engine cannot retain a parallel target field that can diverge under Audio Device Clock updates.
- Reissuing a running **Frame Demand** for the same target and a new quality revision or Clock Master handoff cannot extend its existing presentation deadline. A mid-frame or tardy observation receives only the active clock phase remaining to the target's next media-frame boundary; every terminal ticket remains immutable and cannot be revived by a timer or later Clock observation.
- A **Frame Request Binding** is resolved atomically at completion. If the same semantic frame key is rebound while work is in flight, a reusable result adopts the latest binding; a canceled or incompatible old execution leaves the newer binding pending.
- One Playback Session has one active **Frame Demand** identity for queued current work. All media keys in that demand's complete visual closure remain eligible, while a newer identity atomically supersedes every older unstarted `Current/Playback` binding even when the execution generation is unchanged. Superseded in-flight work may retain decoder locality but cannot publish a current result or failure.
- Every queued or executing frame request belongs to exactly one **Frame Work Broker** lifecycle. Admission and queue capacity cannot disagree, and only an execution lease or explicit synchronous Adapter completion may resolve its Frame Request Binding.
- **Execution Work Intent** is shared language, not shared scheduling ownership. Every consumer Module must define its own bounded admission, cancellation checkpoints, resource budget, publication rule, and domain evidence; Export may never contend in a realtime pool merely to reuse the Interface.
- The **Platform Execution Contract** is implemented throughout M1/M2 for Windows, macOS, and Linux; only physical qualification is staged. Shared Modules must compile and preserve the same project/time/color/alpha/audio semantics on all three platforms. Backend-specific graphics and media resources cross a Seam only as opaque typed capability, ownership, synchronization, and terminal evidence; absence of a correct native path produces an explicit correct fallback or blocker. Windows-only real-device evidence cannot certify Linux/macOS, while missing Linux/macOS hardware access cannot justify a platform-specific shared Implementation.
- Platform Adapters own OS services and OS facts; they do not independently certify a device-bound graphics capability. Native decoded-frame import support belongs to the concrete Renderer runtime created from the active wgpu Adapter/Device/Queue, and App admission combines that immutable support snapshot only with exact media-surface facts. A second D3D/Metal/Vulkan probe would violate device locality even when it reports the same API family.
- Every admitted background analysis attempt reaches exactly one **Execution Terminal Evidence** disposition. Rejected admission, dependency failure, cooperative cancellation, and stale publication are distinct and cannot collapse into a missing cache entry.
- `app::preview_access_mode` is the application scheduling Adapter over the **Frame Work Broker**. It owns media keys, access-mode admission, bounded job transport, and worker-lane mapping without depending on Widget or Window modules; `app_ui` consumes it and cannot host a second scheduler.
- `app::preview_scheduler_policy` is the sole application policy for Frame Demand deadline classification, executed decode quality, frame-local hardware recovery signals, bounded playback prefetch depth, scrub-locality/latency adaptation, and the consecutive-late decode-pressure guard. The production Adapter supplies one explicit monotonic observation instant per scrub request; UI code cannot own adaptation thresholds or sample time internally. These local guards may alter decode strategy or suppress speculative/duplicate work but cannot enter Playback Transport `Recovering`, change Clock Master, mutate authored quality, or accept an inexact settled frame. Preview UI Adapters consume these decisions and increment diagnostics but cannot reconstruct hardware fallback or redefine `Ready`, `Degraded`, or `Late`.
- The **Frame Work Broker** records the selected worker lane on the execution lease at dequeue and derives priority, class, lane residency, and cross-lane evidence from that same lifecycle state. Adapters may rename fields for their report schema but cannot maintain parallel activity counters.
- The **Frame Work Broker** derives each execution's cancellation disposition, request age, and execution-lease age from one Monotonic Runtime Clock sample under one lifecycle lock. Adapters may translate that evidence into domain-specific diagnostics, but cannot reconstruct it from separate freshness, competing-work, codec-entry, or timestamp queries.
- The first **Frame Work Broker** close records one immutable closure instant in its **Monotonic Runtime Clock**; every executing lease observes `BrokerClosed` with age from that instant. Repeated close cannot renew the cancellation budget, and an Adapter stop flag has no timing authority.
- The **Frame Work Broker** samples one **Monotonic Runtime Clock** Adapter exactly once for each atomic lifecycle operation that establishes or compares time. A regressing Adapter sample is clamped to the last observation and recorded as fail-closed evidence; it can never move an age backward or masquerade as valid timing proof.
- A **Frame Work Deadline** is lowered exactly once at Broker admission. Rebinding the same in-flight key replaces its deadline with the latest binding, while cancellation reports the earliest applicable deadline, invalidation, or preemption request.
- A worker records completion in the **Frame Work Broker** before crossing its result channel. Later UI polling resolves freshness against the latest binding but cannot change the recorded on-time/missed deadline result.
- A media **Adapter** contributes the Broker-owned execution-lease age at first checkpoint and return exactly once to **Frame Cancellation Evidence**. `app::preview_access_mode` exclusively maps the Broker's atomic disposition, access mode, and bounded speculative budget into Adapter cancellation reasons and Playback work classes; workers and UI reports cannot reconstruct timing from the later codec-function entry instant. The Playback Module alone aggregates causes and evaluates request-to-checkpoint plus class-specific checkpoint-to-return budgets.
- The concrete FFmpeg Adapter returns exactly one **Preview Decode Cancellation Fact** when cancellation becomes effective. `AVIOInterruptCB` records the first active in-process `input_open`, `stream_info`, `seek`, or `packet_read` checkpoint only when that call returns; ordinary cache, codec, frame-materialization, and external-process checks remain cooperative facts. Killing and reaping an isolated demux helper is a distinct `IsolatedDemuxTermination` fact and poisons that packet source; it must never be reported as callback interruption or reusable format state. Reused Sessions reset observation per request. App Runtime evidence may aggregate the fact but may not replace Broker cause/timing evidence with it.
- The **Playback Preview Pump** samples the still-pending Frame Demand identity once per runtime turn. A Preview Adapter reports execution facts against that sample but cannot apply Frame Deliveries or mutate Transport State; Window and Headless consumers receive the same pump outcome.
- The **Playback Preview Pump** applies terminal Frame Deliveries before observing video preroll. A delivery that ends or replaces a demand cannot be followed in the same turn by readiness attributed to that superseded demand. A presentable Priming delivery deliberately leaves its active demand available as the exact identity of the independent future-lookahead condition even though its Presentation Ticket has been consumed.
- If independent bounded completion sources report the same terminal authority in one pump turn, the first accepted fact consumes it and later losing facts are retired before Playback Evidence; an expected internal race is not a rejected terminal observation.
- Successful decode/cache completion is nonterminal readiness. A CPU or GPU **Presentation Adapter** must complete the exact **Frame Presentation Ticket** only after it has produced a usable Viewer output; the Playback Module compares the real completion timestamp with the ticket deadline and emits `Ready`, `Degraded`, or `Late`. Cancellation/failure paths may terminate earlier without presentation.
- A Viewer stale lifecycle state does not terminate a **Frame Demand**; only a deadline/policy decision may emit `StaleAvailable`, while an in-flight worker retains the chance to deliver `Ready`.
- A **Playback Quality Policy** constrains every **Frame Demand** in its Playback Session.
- An active Playback Quality Policy's temporary `Full`/`Half`/`Quarter` scale multiplies the user-authored preview scale at every Preview Adapter boundary; paused/stopped still-frame work returns to the authored scale. It changes output extent only and never changes proxy/original selection or color interpretation.
- Preview scale normalization has one application-layer owner shared by author mutation, Window execution, and Headless execution; UI formatting cannot introduce a parallel clamp or invalid-value fallback.
- Native-video playback admission has one application-layer snapshot for request, device selector, surface-format support, and blocker facts. Window, Headless, and Preview diagnostics project that snapshot and cannot maintain independent admission booleans or downgrade rules.
- A correct CPU frame produced after hardware decode was explicitly requested but did not execute is a presentable `Degraded` **Frame Delivery**. It contributes recovery pressure; observed hardware decode with CPU transfer and native GPU-resident decode remain `Ready` paths.
- Executed decode quality is stored on the decoded frame itself and survives prefetch and Preview Frame Store reuse; it is aggregated across the final composition before creating a **Frame Presentation Ticket**. Job identity is not a substitute for execution quality.
- **Playback Evidence** records state and clock transitions without owning them.
- The **Playback Engine** owns the only accepted-observation monotonic high-water mark. App wall-clock projection and Playback Evidence mirror it; neither can advance time independently, and a stale identity is rejected before its timestamp is accepted.
- **Playback Evidence** uses bounded versioned events and aggregates from real Frame Demand, Frame Delivery, Clock Master, seek, and Audio Playback observations; capability probes alone cannot satisfy execution gates.
- **Playback Evidence** distinguishes a one-frame Clock Master advance from a multi-frame observation and counts skipped intermediate frame targets without enqueueing obsolete current work.
- **Playback Evidence** detailed-event eviction is normal bounded retention, not observation loss. Metric population count and maximum cover the entire run exactly; percentile estimates use a declared fixed-capacity deterministic reservoir.
- **Product Process Tree Memory Evidence** is sampled by a platform Adapter over the same real-cadence observation window as professional playback. Acceptance compares fixed settled windows, enforces the profile's exact typed metric and cap, and samples again after the gate's declared terminal stress has reached quiescence; unsupported, incomplete, scope/metric-mismatched, topology-raced, or partially queried samples fail closed. The current Windows profile uses Private Commit. A video gate may define terminal stress as a latest-wins seek burst, while an audio gate may define it as completed long-run playback plus worker quiescence.
- **Professional Playback Acceptance** fails closed when observed **Frame Cancellation Evidence** has an unknown cause, lacks request/checkpoint attribution, contains impossible timestamp ordering, observes a request too late, or returns after its work-class budget.
- Every Clock Master observation or handoff that changes the authoritative timeline frame must publish a matching current **Frame Demand** in the same Engine transition; position, demand target, and delivery identity cannot temporarily diverge.
- **Professional Playback Acceptance** accepts HEVC Main10 only when FFmpeg proves the codec profile, dimensions, bit depth/pixel format, supported exact frame rate, primary-video stream duration, and any declared frame count. Container duration cannot substitute for primary-stream coverage; unknown probe values remain unknown and fail the contract.
- A versioned professional acceptance profile owns its cadence, duration, timeout, latency, readiness, visibility, and hardware-execution thresholds. Developer environment variables may tune manual smoke tests but cannot weaken that profile.
- `app::playback_acceptance` consumes only UI-independent execution evidence. A performance or Window Adapter may project its diagnostics into that Interface, but UI report schemas cannot become acceptance policy dependencies.
- A canonical **Reference Corpus** contains only self-owned, deterministically generated, public-domain, or explicitly licensed material. Media with unverified rights may be used only by ignored local diagnostics and can never satisfy release or professional acceptance.
- A generated Reference Corpus entry fixes its recipe identity and semantic probe contract, not an encoder-version-dependent artifact hash. Every **Reference Playback Run** regenerates or verifies an attested artifact, then pins its actual bytes in that run's evidence.
- A baseline-eligible **Reference Playback Run** contains the complete gate set, uses one qualified operator-assigned machine identity, begins and ends at the same clean Git revision, and carries passing asset, machine, and structured execution reports. Hardware serials are neither necessary nor permitted.
- A **Golden Acceptance Plan** can report structural completeness or blockers, but its `complete_golden_project` value is always false because planning does not observe execution.
- Every Golden execution slice reports `complete_golden_project: false`. Only the top-level **Golden Project Run** coordinator may report true, after one Project identity has satisfied every planned obligation and the required consecutive complete runs have passed.
- A short Golden operation slice proves only its declared product intent and typed postcondition. It cannot substitute for a professional performance, device, GPU, memory, cancellation, or long-duration synchronization profile that owns stronger evidence.
- One **Golden Project Run** has exactly one **Golden Product Workflow Driver** and one immutable persisted Project ID/path binding. Stages may create dedicated Sequences through Project author transactions, but may not create another Project or replace the bound App composition.
- A later Golden stage and its durable reopen must preserve every earlier stage Sequence and its authored content. Retaining only the active stage or unioning separately reopened reports is not one Golden Project Run.
- A Hero-assigned Proxy/Relink stage keeps the Hero Sequence as its primary identity and owns one dedicated video Track in an exact non-overlapping Timeline window. Relink advances only Asset Library state while retaining typed Track/Clip/Asset identities; changing the source path must select a distinct proxy artifact identity, and later recovery/reopen must preserve the Track-owned placement, relinked Asset record, and proxy author intent.
- A Hero-assigned Recovery/Nesting stage keeps the Hero Sequence as its primary identity. It may add one strongly referenced nested child, but it owns a dedicated parent Track and exact non-overlapping Timeline window; recovery must preserve the complete Hero parent snapshot and every earlier stage-owned anchor across the new Session and covering manual reopen.
- A Hero-assigned Color Media stage owns two directly adjacent video Tracks in the exact non-overlapping `350..375` frame window, with HLG below straight-Alpha. Both Clips retain one shared placement; HLG retains the exact source interval and the still retains an explicit zero-rate hold. Numeric color-reference execution must resolve the original source rather than a proxy, and later stages plus the final durable reopen must preserve the exact typed Track/Clip/Asset identities and their canonical hashes.
- Decode execution provenance lives on the decoded frame and survives the session-local playback ring, prefetch, and Preview Frame Store reuse. Acceptance coverage counts only provenance attached to Viewer candidates that complete through the headless GPU Presentation Adapter; aggregate prefetch diagnostics cannot satisfy it.
- A **Preview Frame Store** admits and evicts decoded payloads by entry count, host bytes, and opaque decoder-resource units. Every Broker submission attempt is independently charged by a move-only work lease, including duplicate semantic keys; successful measured admission preserves the same allocation identity as a cloneable frame lease. One typed current demand may not exceed its untrimmed exact working-set grant, multiple demands may not exceed the global aggregate hard grant, and protection or actual-charge rejection remains a resource blocker rather than terminal source-failure memory. Renderer import tables, bridges, output textures, and presentation resources remain outside it and require their own evidence.
- Playback prefetch and preroll use one canonical near-to-far future-media prefix planner and their frame-rate-derived window only as a temporal ceiling. Every media-bearing frame is one complete prepared visual dependency closure; blank frames do not break the prefix, while an invalid dependency, terminal failure, or capacity boundary forms a frontier that farther work cannot bypass. The Store projects typed speculative headroom from its authoritative physical ledger after accounting for outstanding/external ownership and the Store-exclusive LRU that may be released. While consuming that projection, the App retains short-lived allocation leases for every accepted nearer resident key, counts pending work once, and admits missing keys in deterministic order; therefore a farther Prefetch cannot evict a nearer accepted resident or masquerade as an available complete frame. One partial frontier may be warmed without advancing availability. The real Store admission remains authoritative and can never inflate policy to reach a fixed frame count.
- The future-media planner may reuse only a bounded per-frame canonical closure and fully lowered immutable media contract under one exact Authoring Session/Generation, root Sequence/revision, Effect Registry revision, runtime scale, target extent, Program Color Context, Asset Library revision, Proxy configuration, and hardware-admission identity. It caches no resident, pending, failure, headroom, lease, or Broker fact. Sequential traversal therefore evaluates at most the initial window plus each newly entering frame; a seek can hit only an exact frame under the same complete identity. A cached source/proxy key retains and revalidates full immutable file-revision evidence, has bounded reuse even without identity change, and fails closed on object drift, while current-frame source resolution remains uncached.
- A stopped **Preview Production Runtime** may release Store-owned media residency only when its Broker and execution intent are quiescent and a registered GPU Viewer output is exact for the active generation. Acceptance of that release additionally waits for every decode worker to publish `Idle`, zero active helpers, and launch-to-post-reap terminal accounting; a fixed sleep is never retirement evidence. The GPU output, any Viewer stale pin, and failure memory survive; external frame/work leases retire only through RAII. Active Playback, pending still work, or an old-generation output must fail closed. CPU raster presentation retains media until it gains an equivalent generation-proved reuse seam; it must not invoke the GPU-only release rule optimistically.
- Each production Preview worker owns exactly one **Preview Decode Execution Progress** observer for its lifetime. A non-cloneable, thread-safe bootstrap is consumed to construct the non-`Send` FFmpeg context on that worker; only read-only observer clones cross back to App diagnostics. Media call sites publish before entering potentially blocking FFmpeg operations, while the FFmpeg callback publishes every poll and whether it observed the current request canceled. The same observer records isolated-demux launch, validated-ready, acknowledged Seek, Packet/EOF Read, cross-request reuse, active/peak process count, and exactly one post-reap terminal class without creating a second Registry. Poll progress, callback cancellation, FFmpeg call return, helper process reaping, and Broker lease completion are distinct facts. The observer may identify a stalled call or an unpolled/ignored interrupt but can never authorize teardown, substitute for a returned execution lease, or claim that an in-process codec call is cancellable.
- The **Preview Demux Isolation Boundary** is the only recovery authority for a non-returning FFmpeg format call. One helper owns one source revision and serves strictly ordered, single-in-flight seek/read commands; it is never a multi-source pool and is not spawned per frame. The parent samples cancellation at most every 250 µs, disconnects bounded IPC, terminates and reaps exactly that helper, poisons the paired packet source, and reports the active Seek or PacketRead checkpoint before returning. That fact makes the complete packet-source/codec Session ineligible for recovery or reuse; the worker retires it directly rather than flushing hardware codec state that has no valid source to resume. Protocol buffers, packet payloads, side data, codec extradata, keyframe anchors, error text, stderr evidence, and response queue depth are independently capped; the helper may never place OS-native paths on its command line or transfer process-local FFmpeg ownership. EOF leaves the Session seekable, clean close is bounded, command/protocol/FFmpeg failures fail closed, and only a new process with a new nonce may reopen a changed or poisoned source.
- Exact Preview output reuse is bound at least to Sequence revision, Author Generation, playback epoch or stopped frame, output extent, seek intent, display contract generation, and the exact resolved Project engine plus Sequence program color context. A change in any of those facts requires re-evaluation before an old output can become current. Process-global OCIO reload generation is operational evidence, not semantic cache identity.
- One **Project Color Environment** privately owns one exact engine value. A Custom OCIO environment must bind every Program Output target used by the future-Sequence template and current Sequences before it can enter an author snapshot; it may retain additional explicit delivery bindings, but one missing or ambiguous required binding rejects the complete candidate rather than producing partial color semantics.
- `app::preview_media_frame` owns decoded Preview payload residency, lazy CPU working adaptation, frame-local quality/provenance, sampled versus logical geometry, and exact host/decoder reservations. Each frame contains exactly one closed residency payload—working CPU, source-domain CPU/GPU-capable, or native decoder surface—so empty and contradictory combinations are unrepresentable. `app::preview_media_task` constructs it from the concrete decode result; Widget and Window resource types cannot enter it.
- `app::preview_media_source` is the single Preview source-interpretation boundary. It owns source/proxy fingerprinting, color/range/Alpha interpretation, native-surface classification, proxy-generation intent, and canonical decode geometry. `PreviewProductionRuntime::media_adapter` owns Asset Library lookup and projects typed outcomes into execution evidence; the Proxy Generation Service owns same-key admission/deduplication. Window code owns neither decision.
- A **File-backed Still Asset** and a moving-image Asset both lower through `ClipContent::Media`; only the still constructor authors a zero-rate source hold. Preview, Thumbnail, Transition handle validation, Insert, trim, and Export consume that explicit time mapping instead of inferring stillness from extension at evaluation time.
- A **Media Decode Target** is one canonical nonnegative source-local **Timeline Time** retained unchanged by the Render Plan, Preview Frame Store key, Broker job, media request, and Export decode cache. Only an explicit media frame-rate override quantizes once onto its declared source Evaluation Grid; nested Sequence consumers project exact child-local time onto the child grid. The FFmpeg Adapter alone lowers the target to stream PTS with checked nearest rounding and the declared stream start PTS. Floating seconds and microsecond keys are not authority.
- A Timeline Export Snapshot freezes each reachable picture dependency's full source extent together with its source fingerprint. Delivery-sized decode is a sampled representation, not new author geometry: media, nested Sequence, generated source, and Transition endpoint transforms all pass through the same **Picture Geometry Projection**, and the sampled extent is part of decode-cache identity.
- Renderer `PreparedVisualFrameClosure` is the only recursive visual
  interpretation for Preview and Export. `app::preview_timeline_execution`
  owns Preview materialization for Window and Headless consumers: it supplies
  typed media/title outcomes to prepared nodes, composites child working
  outputs, exposes media-demand collection over those same nodes, and records
  execution facts. It cannot duplicate nested time/cycle/depth, child sizing
  or color-context binding, Transition/temporal membership, or instance-path
  identity; Adapters cannot duplicate either the closure or materializer.
- `renderer::BasicTitleRasterizer` is the shared Basic Title generation Interface for Preview and Export. Sequence resolution plus the persisted total title-safe margin define layout; exact face resolution/shaping and bounded raster caching remain Session-owned. Preview schedules this work on its bounded title worker, while Export reuses one raster Session for the complete job and nested closure.
- **Preview Output Unavailability** is exhaustive at every production output boundary: absence of an active output target is `NoContent`; unresolved dependencies and invalid display/color contracts are `Blocked`; an admitted decode, composite, color, or packaging execution error is `Failed`. Empty active root and nested Sequences contribute exact transparent Program pixels rather than becoming unavailable. Any terminal unavailability clears current and pinned stale output; only `Pending` may reuse a same-scope prior frame.
- `app::preview_frame_store::PreviewFrameStoreAdapter` is the sole Frame Store policy Adapter inside `PreviewProductionRuntime`, over the playback-owned generic Store. Window and Headless specializations share it; `app_ui` cannot own another cache or residency policy.
- `app::preview_raster_frame` owns the validated RGBA8 extent, encoded color identity, exact byte reservation, and stable resource key for a final CPU Preview raster. Cache and stale-reuse paths retain that application contract; the Window Presentation Adapter performs the only conversion to `ViewerFrameImage`, sharing the pixel allocation rather than copying it.
- The **Waveform Analysis Service** is the sole product owner of Timeline waveform execution. Its key includes asset identity and source revision; project rebinding rotates generation, relink/removal can evict one asset, canceled/stale results cannot populate the success or failure cache, and paint/layout code can only issue a nonblocking lookup.
- A Timeline **Waveform Lookup Adapter** is weak and cannot extend the Waveform Service lifetime. Product quit and qualification close Waveform in two phases: signal/cancel/close admission first, then join the analysis worker, drain publication ownership, and consume its private Audio Source Cache under one absolute deadline. Only the current typed Waveform receipt plus a clean nested Audio Source receipt proves closure; ordinary Drop and an empty UI demand are not evidence.
- The **Thumbnail Execution Service** is the sole product owner of asset-thumbnail work. Its exact key includes source path/fingerprint plus resolved input/range/working/output color contract; generation and pending ownership gate publication, raster bytes and entries are independently bounded, and the Window Adapter performs the only zero-copy conversion to a Widget image.
- The **Proxy Generation Service** is the sole App owner of derived proxy demand. Same-key Import work can be promoted by Playback recovery or explicit user intent without duplication; cache-root `concurrent_jobs` is acquired before a request becomes Running, project rebinding cancels queued and FFmpeg-active attempts, automatic exact failures are retained while an explicit user request may retry, and only current-generation completion publishes success.
- Relinking media changes the Asset Library's canonical source path while preserving `AssetId`, authored name, and every Clip reference; it is not a Sequence author transaction. Proxy artifact identity includes the canonical source path and exact source/color/encoding contract, so an old-path proxy can never become fresh for a replacement path merely because the bytes or file fingerprint match. Atomic proxy transcodes explicitly select their contract-owned muxer because the identity-bound opaque publication sibling has no extension-derived container identity.
- `app::preview_viewer_plan` owns the **Preview Viewer Plan** representation, stable semantic output identity, independent cross-call reuse admission, frame-local quality/provenance aggregation, and GPU lowering. It is semantically pure and UI-independent; its only mutable input is the caller-owned **Effect Execution Session** used for bounded lowering memoization. Window and Headless Adapters cannot rebuild these rules. Frame-seeded graphs include the seed in semantic identity; an `Uncacheable` graph still receives a mandatory per-attempt identity but bypasses cross-call cache/pending reuse and is bound to a fresh execution nonce.
- `app::preview_cpu_execution` owns **Preview CPU Execution** for both nested working-linear output and final CPU raster output. Its result retains input/output/monitor color facts, composite diagnostics, and execution durations; Window and Headless Adapters may record or assert those facts but cannot execute alternate color/composite rules.
- A **Viewer GPU Preview Runtime** owns GPU execution resources independently of a Window; production Window and headless validation must adapt the same execution lifetime and must not duplicate color or compositing interpretation.
- A renderer-owned **GPU Visual Frame Module** is the only homogeneous GPU compositor for resolved working media, typed RGB DataTextures, procedural sources, adjustments, Cross Dissolve, and GPU-resident nested outputs. Export selects it only after the complete prepared frame closure proves homogeneous GPU Effect plans and no temporal or heterogeneous payload. Child outputs remain `GpuColorFrameHandle` values in the shared frame table, and cross-working-space nesting executes one explicit stock-OCIO GPU transform. The generic root Program Output/encoder pipe reads back explicitly; a separately qualified Windows HEVC Main/Main10 route detaches the root texture, performs same-device D3D12 Video Processor RGBA-to-NV12/P010 conversion, and submits the FFmpeg-owned surface to in-process `hevc_d3d12va` with zero host readbacks/rawvideo writes/CPU uploads. That route is GPU-resident rather than literally zero-copy, is bounded by frozen source/surface grants and exact cross-queue barriers/fences, and rejects HLG, full-range PQ, static HDR metadata, active Legalizer/VBV, other codecs, and unsupported platforms. A pre-start blocker selects the complete generic route; any failure after the first GPU or resident submission is terminal and cannot trigger hidden whole-frame replay. The Module admits existing closure residency plus conservative upload/Effect/Transition/composite demand against an attempt-frozen byte-and-texture grant before each node records and returns high-water evidence. RGB DataTexture upload is exactly `NonColorData + DataTexture + Rgba32Float`, preserves extended numeric channels, and contributes zero input OCIO stages; Viewer and Export derive it only from the shared `PreparedSourceFrame` contract.
- The **GPU Working Float Policy** is also the physical storage authority consumed by the GPU Visual Frame Module, Viewer spatial, nested Export working transforms, and all scene-working resource estimates. Export retains the exact decision; Viewer and Export reject any mismatch between admitted bytes, policy format, and the actual working handle. DataTexture/AlphaMask, output carriers, LUTs, deliverable pipes, and Render Cache retain their separate typed formats.
- A renderer-owned **Composite Execution Plan** is the sole spatial execution authority for one ordered GPU working composite. It projects source domains and fused Crop through affine placement into conservative Layer damage, removes empty contributions, copies the unchanged accumulator complement for later bounded Layers, and records deterministic full-canvas-coordinate tiles inside one render pass. Preview and Export consume the same plan and measured shaded/copy/pass/fusion evidence. Layer damage is relative to the current intra-frame accumulator; it cannot be presented as cross-frame temporal damage without an exact prior-output identity and retained-resource proof.
- A renderer-owned **CPU Visual Execution Module** is the deep Implementation behind the shared Timeline compositor Interface. Each Preview fallback or Export Session owns one lazy bounded worker pool, runtime SIMD selection, and grant-bounded Transition scratch through its `TimelineCompositeScratch`; Rayon's global pool is never an execution authority. Full-frame Normal blends and Cross Dissolve gain parallel Leverage while scalar per-pixel semantics preserve exact Locality. Opaque Dissolve has a separately qualified runtime-vectorized path, partial Alpha retains the canonical scalar coverage path, and Adjustment passes transfer the canvas allocation across the Effects Seam for in-place base reuse. Diagnostics prove parallel dispatch, SIMD pixels, scratch hits, and owned-base reuse; worker count or machine capability alone is not execution evidence.
- Window UI refresh has two non-interchangeable domains. Author, project, preferences, and workspace changes may refresh complete panel models; Preview candidate, registration, clearing, and Frame Delivery changes update only the Viewer/transport presentation projection. Neither domain may replace persistent shell-chrome Widget identities during an active Window Session; title, menu availability, and shortcut models update in place so hover, press, focus, pointer capture, and overlays remain UI-owned transient state.
- An ordinary complete-GPU Viewer output becomes usable after Adapter registration and ordered submission to the same GPU queue used by its consumer; neither Window nor Headless claims fence completion at that publication seam. A heterogeneous CPU-prefix/GPU-tail output remains unpublished until its exact completion callback is observed and the completed batch is validated. Both complete the same **Frame Presentation Ticket**, and device capability alone is not execution evidence.
- A Window GPU artifact is usable only while its Preview metadata, submission-qualified renderer registration, move-only presentation lease, and Ready/Stale Widget projection agree on the same `(Preview Output Key, texture key)`. Exact timeout, cancellation, device-failure, geometry change, non-GPU replacement, or teardown revocation drains the shared physical-publication slots and removes every matching Widget projection before another redraw can sample an unregistered texture; a same-semantic replacement with a different texture key is never cleared.
- The Headless Viewer GPU lifecycle has one submitted-owner slot. A physical-completion timeout revokes publication authority and quarantines, rather than frees, that owner; only its exact late callback may retire the frame and media-protection leases. The submitted owner and the shared current/prepared physical-publication Module carry a process-local submission/resource identity in addition to the semantic **Preview Output Key**. Conditional clear always requires both identities, so a late callback can never erase a same-key replacement; device-generation retirement returns both slots for exact semantic cleanup rather than silently dropping a prepared owner.
- **Audio Playback** may offer an Audio Device Clock Master only after stream health, PCM preroll, and media phase satisfy Playback Policy.
- A healthy active audio stream with temporarily stale callback evidence is **Uncertain**, not lost: its last consumed-sample fact remains frozen while the predicted device phase continues at nominal rate and its conservative uncertainty grows with callback age. A newly active stream is also Uncertain while active callback consumption is smaller than the host-reported remaining playback delay, because no nonnegative effective device position is yet proven. That position is never clamped or invented. The current Audio Device Clock Master may coast only for one bounded grace interval. Explicit device/stream failure is **Unavailable** and selects Synthetic Clock Master immediately; grace expiry also hands off continuously to Synthetic.
- **Physical Audio Output Evidence** proves that a concrete CPAL or exact-mask Windows WASAPI output stream repeatedly consumed production PCM at a bounded callback cadence. It is not acoustic loopback evidence and cannot prove the waveform reached a physical connector, amplifier, or speaker.
- Every **Audio Sample Position** carries its sample rate. Positions at different rates cannot be compared or subtracted without an explicit resampling Adapter.
- An **AudioDecodedSource** fills exact interleaved **Decoded Audio Source Windows** and may fail the whole block; it cannot publish a partial shifted block. `mondrian-audio` owns only a small aligned hot window, while the media Adapter owns fingerprinting, decode, weighted LRU residency, and failure memory.
- Every Sequence, Audio Render Contract, PCM render request, decoded source cache, and PCM buffer carries one **Audio Signal Layout**. Capacity is derived from that layout. The DSP core can preserve named/custom and Discrete buses, but each media, plugin, device, and export Adapter must negotiate an exact lowering; current FFmpeg execution admits only explicit mono/stereo/5.1(side) matrices and may not infer semantics from a channel count.
- An **Audio Render Session** crosses its source Adapter Seam once per active Generated Audio Contribution block using ordered absolute source-frame coordinates; reverse, repeated, and non-contiguous mappings cannot force a per-sample Interface or move time-mapping authority into media decode.
- A **Prepared Audio Schedule** is the only graph representation interpreted by an **Audio Render Session**. Preparation assigns dense node/contribution/scope slots, contiguous incoming Route ranges, exact active sample spans, Transition bindings, scratch liveness, and the CPU kernel backend; author maps and general Route searches cannot enter block execution.
- Semantic compilation closes a muted Track's `PostMute` edges before dependency binding while retaining its `PreFader` and `PostFaderPreMute` sends. Audio Track visibility is presentation state and never gates PCM. Only the resulting selected Signal Closure may publish **Audio Program Execution Demand**; App and UI Modules may neither walk Tracks/Clips to infer audibility nor treat Track mute as closing a pre-mute send.
- A **Prepared Visual Program** is the production visual-evaluation representation shared by Preview and Export. Its hot cache key is exact Sequence identity, **Sequence Author Revision**, a versioned conservative **Visual Author Fingerprint**, and Effect Definition Registry Revision. Its **Prepared Visual Schedule** owns only interval selection and shared immutable Clip/Transition author snapshots, which must remain semantically identical to the direct Sequence reference Adapter at every half-open edge. Program preparation failure cannot replace a valid revision; Clip/Transition blockers never become identity output. A **Prepared Visual Range Closure** is the only recursive range-dependency interpretation: it pins the exact Program set and registry revision and owns nested range/cycle/depth, media, Transition, and Basic Title font-query reachability. Preview cold-start lookahead and Export capture consume this same closure instead of walking author Tracks/Clips. Export treats the selected root range, exact nested current-frame closure, and every nested frame in the admitted temporal-demand closure as one candidate capture, atomically publishes its job-local Program set only after every reachable Sequence binds the same stable Effect Definition Registry Revision, and never prepares an unrelated Sequence merely because the Project snapshot retains it. It rejects only blockers reachable from that closure and never consults the mutable registry during admitted frame rendering. A prepared Effect with external dependencies cannot be reused across Sequence revisions before explicit re-observation.
- Preview and Export dynamically evaluate every reachable **Prepared Visual Program** frame and complete concrete consumer execution admission before any media decode or generated-source work; Export does so before audio rendering and encoder creation. Graph identity may skip pixel operations only after admission and can never erase temporal, state, exact execution-mode, lifetime, or transfer obligations.
- A `CompiledEffectGraph` is immutable production IR: its graph, schedule, liveness counts, cache profiles, domain plan, signature, **Effect Execution Envelope**, exact Definition-stage value/node bindings, and per-node implementation/Definition mode intersections are private and exposed only through read-only accessors, so callers cannot mutate pixel or execution semantics without recompiling their evidence.
- An **Effect Execution Envelope** is immutable semantic evidence. **Effect Execution Demand** derives temporal, ROI, state, resource, and exact-mode obligations for one request. **Effect Linear Stage Placement** remains a non-executable Linear Chain diagnostic. An **Effect Graph Value Execution Plan** is the only heterogeneous route authority: General DAG fan-out/join, endpoint residency, explicit transfer, completion dependency, last-use release, and bounded live-set are derived from the unique graph values rather than stage order.
- **Prepared Heterogeneous Effect Route** executes a source-closed CPU-Float32 DAG prefix with exact materialization use counts, cooperative copy/kernel/Mask-raster checkpoints, synthetic `MaskSource`, Blend/MultiInput/Mask joins, and checked live-frame/Mask-geometry/row/kernel-scratch demand, followed by an ordered frontier of one or more explicit uploads and an exact GPU-Float32 dispatch/release schedule. It exposes the Effects-owned versioned shape identity and exact aggregate frontier retained bytes; Export compares the opaque identity, and Renderer charges each batch item for its immutable input plus all frontier values. Every transfer source stays live until atomic CPU completion; every upload retains its materialization, non-converting value format and wait/signal identity, and a later upload failure removes earlier unpublished resources. Linear point tails stay fused; shared uploaded values may fan out through point branches and rejoin through binary or ordered N-input canonical scene-linear BlendMode composition. MultiInput with at least two inputs left-folds the shared compositor; `N - 2` private intermediates enter the same physical recording byte/texture authority and are removed on terminal recording failure. One-input MultiInput is a distinct bit-preserving GPU texture copy with exact wait/signal/release and physical-resource accounting. CPU `MaskSource` raster output crosses the frontier only as `AlphaMask + NonColorData`; the GPU Mask dispatch consumes it beside a Working input and preserves RGB while evaluating every MaskOp and inversion state. The original Gaussian-to-Basic-Correction/Grain path, a CPU fan-out/Blend join, generated CPU Mask/Mask into a GPU point tail, two independent CPU spatial branches uploaded into one GPU join, CPU MaskSource→GPU Mask, GPU fan-out/Screen join, three-input SoftLight GPU join, and one-input identity copy are scalar-reference gates; the last five also run on a real wgpu Adapter with exact token/live-set/resource validation, and the copy requires bitwise Float32 parity. The shared compositor has real-device parity against the CPU Float32 reference for all canonical BlendModes, straight alpha, fractional opacity, threshold/color-tie inputs, every MaskOp/inversion pair, and the complete 64-bit Dissolve frame seed. The wgpu one-submission Adapter separately admits its conservative non-aliasing recording bytes and physical texture count instead of presenting the abstract plan peak as physical ownership; Viewer active-resource admission consumes the identical requirements. The checkpoint Seam preserves the owning attempt's exact cancellation or deadline reason through Path/BVH preparation and raster execution; stopped work returns no pixels or completion token. `HeterogeneousCpuPrefixSource` is the single renderer-owned source contract for both existing CPU working frames and procedural Solid Color. A Solid retains only exact extent plus authored straight-alpha working color during planning; batch/source bytes are validated first and the serial executor allocates the raster under the owning attempt, so Preview and Export do not fabricate a media frame or duplicate source interpretation. Preview prepares a per-Sequence placement ledger before any media Adapter call: the root may prove a complete CPU or complete Viewer route, while every nested child materialization remains complete-CPU-only. Once the canonical closure has frozen all child bindings, the parent Nested Clip may use that already-materialized CPU working frame as its heterogeneous prefix input; the route extent is the child's exact execution raster, never the parent canvas. Media, Basic Title, and Solid Color use the current node raster. Media demand obtains CPU-working residency from that ledger, while procedural Solid bypasses media/title Adapters; the dedicated visual worker only binds or materializes the typed source and executes the frozen route under its move-only completion lease. Export consumes the same renderer-owned prepared route/source contract for Media, Basic Title, same-raster Nested Sequence, and Solid Color placements; its attempt-local `ExportVisualRenderSession` retains placement preflight, generated-source byte admission, uploads, exact GPU dispatch, wait, readback, and terminal policy but does not replan or reinterpret typed value domains. Adjustment accumulator readback and Transition-input heterogeneous placement retain a complete CPU route or fail closed. Both consumers retain generation, cancellation, and terminal Adapter evidence. This vertical slice does not prove a later backend transition or graph-internal readback, arbitrary placements, color-domain conversion beyond explicitly typed non-converting transfers, external lanes, or temporal-plus-heterogeneous execution. Viewer lowering may not synchronously execute the CPU prefix outside its worker, and a started heterogeneous attempt may not fall back implicitly to whole-graph CPU execution.
- Every Effect pixel, dynamic-topology variant, and GPU-plan cache belongs to one **Effect Execution Session**. Preview binds the sole Preview generation authority; Export binds the queue's exact attempt generation. The production Timeline-frame preparation Interface performs ordinary graph evaluation and every temporal root/sample evaluation through that same Session before the same owner executes pixels. Rotation retires every encoded, Float32, node, temporal, topology, GPU-plan, and deterministic-blocker entry together, and no resource decision or cancellation in one consumer may mutate another consumer's residency. Session-free preparation remains only a scalar reference/test path.
- Finite Temporal/ROI scalar execution freezes the root `CompiledEffectGraph`, exact request, **Prepared Temporal Value Program**, and raw source demands into one `PreparedEffectTemporalExecution`. The request carries Clip visual-domain **Timeline Time**, the ordinary Render Plan's output-frame seed, complete frame coordinates, generation, cancellation, ROI evidence, and Float32 precision. Signed past/future taps address stable Definition-stage inputs; earlier Effect stages are reevaluated from the same Prepared Effect Program at each exact sample time. Exact duplicate graph/source values are frozen once; shared dry-run/runtime expanded use counts keep each value resident and charged until its last edge. Mask geometry is prepared once per reachable exact-time graph context and frame extent, uses exact global pixel centers for every region, and charges retained geometry plus maximum row scratch to the same hard grant. Stage-contract drift, internal same-stage temporal input, unbounded input, stateful continuity, unresolved domains, and unsupported operations fail closed. A demand batch reports exact Float32 source-coverage bytes before materialization. Preview and Export admit that coverage against their CPU working-set grant, freeze it under generation cancellation, and expose only bounded subregion copies with no decode, conversion, nested evaluation, or allocation at the Effect provider Seam. The Effect Session includes retained coverage, prepared Mask resources, expanded live values, kernel scratch, one final output and tile work in its hard grant, executes directly when possible, or derives a deterministic non-overlapping 2D schedule capped at 4,096 tiles. The final `Vec` transfers into shared cache/output ownership without a full-frame pixel copy; internal tiles are attempt-local and cannot read or publish output-cache entries, so cancellation or any planning/ledger failure publishes neither a partial frame nor partial cache state. Export uses its exact job-local closure provider. Preview submits all media temporal demands through its asynchronous Scheduler, binds CPU-working representation into request/cache identity, returns typed Temporal Pending while any Frame Store value is absent, and never blocks for decode or substitutes the displayed/current frame. Direct and tiled Preview/Export production execution share this contract; GPU temporal tiling, internal same-stage cross-time addressing, and continuity execution remain unclaimed.
- CPU scalar reference and runtime-selected SIMD implementations consume the same **Prepared Audio Schedule** and must produce identical PCM for the supported processor set. SIMD is an execution choice, never a second semantic compiler.
- Concurrent misses for the same complete **Decoded Audio Source Window** have one decode leader. Persistent media decode Sessions are bounded by source revision plus output contract; sequential reuse, cold open, and random restart are distinct evidence classes and cannot share one latency claim.
- Persisted timeline positions, ranges, automation keys, and temporal handles use **Timeline Time** in an explicit **Authoring Time Domain**; video frames and audio samples are derived **Evaluation Grids**, not competing author time systems.
- A **Sequence Author Revision** is the conservative invalidation generation for every author entity owned by that Sequence. Project document revision records successful file saves and can never substitute for it in Playback, Preview, audio compilation, or render-cache identity.
- One open Project has exactly one **Authoring Session**. Production UI and execution code receive read-only references or immutable snapshots and cannot borrow the canonical Project Document mutably.
- An **Author Transaction** edits a detached candidate. A Project transaction still enters through one complete detached `ProjectDocument`; the smaller **Project Restore Point** is only the History representation derived after exact scope validation. Failure leaves the canonical document, **Project Authoring Validation Certificate**, **Author Generation**, Sequence revisions, dirty state, and Undo/Redo stacks unchanged; success advances them as one commit.
- An ordinary Sequence-scoped **Author Transaction**, Undo, or Redo prepares and replaces only that Sequence's authored body. It verifies the exact **Sequence Author Contract Certificate** baseline/color context and newer revision; global identity/link, Transition, Audio Program, and **Sequence Dependency Certificate** rules still execute. One prepared ticket owns the next complete document and certificate on the same outer COW root. Only after ticket validation may History prepare; History commit is followed by infallible paired installation with revision high-water state and **Author Generation**. Every Project transaction or Project Undo/Redo validates the complete detached document before publication. When the exact ordered Sequence bodies, default Sequence, and Project color environment are unchanged, the new outer Project certificate reuses their already-anchored Sequence/dependency evidence; a structural, Sequence-body, default, or color change rebuilds the complete certificate.
- Bounded History is one contiguous sequence of typed state endpoints. Sequence commands retain before/after `AuthoringSnapshot<Sequence>` roots; structural Project commands retain scoped `ProjectRestorePoint` roots whose affected Sequence set exactly matches the body/presence difference. Undo/Redo validates the current opposite endpoint, restores only the captured author state, reuses unaffected Sequence allocations, and never rolls back publication evidence or valid active navigation.
- History charges the deduplicated union of immutable COW allocations once across Undo and Redo under the 200-entry/256 MiB contract. An unretained commit is an explicit correctness barrier that clears older Undo and branch Redo; deterministic History charge and native process-memory evidence remain independent fail-closed gates.
- Large-Project qualification uses the current source-attested schema-10 protocol-v1 matrix and the normative timing, locality, reversibility, and Private Commit budgets in [Timeline Model](docs/architecture/timeline-model.md). A recorded run never transfers authority to a later source revision.
- Sequence navigation is explicit Session state. `EnterNested` appends one validated parent return edge; `ReplaceRoot` selects a direct target and clears the complete prior return path. Project restoration keeps the current active Sequence while it remains a valid member; only then may it adopt the restore point's validated structural fallback.
- A product **Action** that performs no operation cannot report success. A Window Adapter may suppress a disabled Undo/Redo gesture by consulting current availability before dispatch; direct, Headless, scripted, and automation callers receive a typed **Action Not Executed** failure when the requested history direction has no entry.
- A Project-scoped **Author Transaction** advances Author Generation exactly once and may change the active Sequence; a Sequence-scoped transaction additionally keeps the active Sequence identity stable and advances that Sequence revision exactly once. Evidence cannot apply the Sequence rule to Project navigation or creation.
- Durable reopen changes the process-local Authoring Session identity but preserves persisted Project ID/path, active Sequence identity, and its saved revision. Reusing a Session or changing Project identity is a failed Golden lifecycle boundary.
- A **Complete Golden Project Run** is one Rust-owned Headless execution over one Project identity. It executes the exact declared slice set, treats each slice report as partial-only evidence, owns every stage-created Sequence explicitly, waits for background work to quiesce, and may claim completion only after a final durable reopen preserves the complete author and asset state.
- Heavy Golden media/GPU work owns a dedicated process main lifetime rather than a libtest worker. A terminal typed report is the semantic completion signal; an external supervisor may enforce deadlines, validate schemas, repeat distinct runs, and clean up a child blocked in third-party DLL detach, but it cannot combine slice reports or infer success from process exit.
- A persistence request binds one **Persistence Generation**, **Author Generation**, Asset Library revision, request identity, exact runtime-lease identity, and a typed durable destination. Manual Save additionally binds the current **Manual Project Destination Binding**: a superseded destination, retired Session, or prior Persistence Generation completion may not change canonical path, clear dirty state, replace newer metadata, or reconcile **Recovery Authority**. A later ordinary Save admitted behind Save As reuses that destination rather than resampling the old Session path. A Session handoff closes admission and waits for a FIFO barrier before a new Session/library binding is installed or prior runtime authority is released; completions retain only scalar evidence, not library/lease Arcs.
- Manual Project publication validates its direct destination parent inside the persistence worker before allocating the SQLite snapshot. A missing Save As/Headless directory suffix is established from the nearest existing direct ancestor through the durable direct-child chain; UI-thread `create_dir_all`, pathname presence, or an unconfirmed/indeterminate directory outcome never admits archive construction.
- An Asset Library freezes its created root to one ordinary canonical native path. Persisted file-backed Asset identity is an absolute lossless UTF-8 ordinary path; relative, traversal, device, non-file verbatim, or unrepresentable paths fail closed at the SQLite row boundary even when media is offline. Windows extended-length spelling exists only at the SQLite VFS boundary for the live database and sidecars and is never persisted as author identity. Persistence validates the captured Library revision before allocating one opaque identity-bound sibling, performs SQLite online backup without persistent sidecars, reclaims and flushes that exact object, and streams it into the Project archive through a retained handle rather than reopening its pathname. The archive target is only the sibling-directory anchor; no parallel service/request snapshot filename is author or ownership identity.
- A concrete Project runtime directory is selected by the pair of the complete domain-separated SHA-256 of the normalized Project publication target and `ProjectId`; the digest is never truncated and lossy Unicode text is diagnostic only. The longest existing parent-directory prefix is canonicalized; the leaf remains the atomically replaced directory entry rather than a symlink referent. Unresolved Unix components retain exact native bytes, while unresolved Windows components use case-insensitive valid-Unicode identity, preserve unpaired UTF-16 units, and conservatively remove Win32-ignored trailing spaces/dots. Its recovery-bearing parent is the single product-owned `mondrian-project-runtime-v4` child of stable per-user state—Local App Data on Windows, Application Support on macOS, and `XDG_STATE_HOME`/`~/.local/state` on other Unix—and never process `TEMP`, `TMPDIR`, or `XDG_RUNTIME_DIR`. From that platform-selected trusted anchor, only a missing suffix is durably published one direct child at a time; the final parent owns one exact versioned durable marker whose initial publication is create-only, and a markerless, unknown, or raced existing parent remains inert. Creation then durably publishes the root direct child and one schema-v3 **Project Runtime Owner Manifest** before admitting a library or autosave payload. `BeforeNamespace`, `DurabilityUnconfirmed`, and `NamespaceIndeterminate` remain distinct until admission: only confirmed durability grants payload authority; unknown state leaves an inert root, disables broad cleanup, and must be revalidated and republished on retry. Production exposes no path-only root lookup: ordinary Open derives the exact pair and Recovery carries the exact root in its candidate. Reuse, library-generation creation, retention, and retirement validate the complete allocation identity before deletion; a missing, malformed, or mismatched owner fails closed.
- A **Project Runtime Owner Manifest** is immutable across Save As and proves only filesystem ownership of the stable live runtime root. The **Recovery Manifest** remains the sole authority for its current canonical Project path and exact recovery snapshots, so owner validation cannot make an unreferenced archive recoverable.
- Every Project runtime or manual-publication mutation requires the exact live **Project Runtime Lease**. Its stable per-user logical authority is independent of process `TEMP`/`TMPDIR` and retains one Project-ID lock plus every normalized publication-target lock admitted during the Session; each paired runtime lease separately retains its root-local `session.lock`. Distinct copied-path roots may share that logical authority only inside one coordinated App, while another process remains excluded. Windows uses no-share handles; Unix uses nonblocking exclusive locks in an effective-UID-owned mode-`0700` directory. Every mutation revalidates handle-to-namespace object identity and re-resolves every admitted absolute publication route, so unlink-and-replace and parent-symlink retarget both fail closed. Presence, timestamps, and PID diagnostics never authorize mutation, and abnormal process exit releases authority through the kernel. The active App Session and in-flight persistence/recovery work share one exact root lease Arc; retired library records may retain additional exact old-Project lease Arcs until their weak library evidence reaches zero. Read-only discovery may validate durable owner metadata, but recovery open uses the candidate's exact root and acquires the complete lease before staging. The lease does not establish current **Persistence Generation** or **Project Library Generation**.
- Stable runtime children are created only through the owner-validated Module Seam. First autosave durably establishes `autosave/` before any SQLite or archive file create; an existing non-directory entry is never replaced, and unconfirmed/indeterminate directory publication admits no archive. Each `project-<unix-ms>-g<author-generation>-<random-uuid>.autosave.mdp` leaf stays `CreateNew` through atomic publication and is immutable afterward. Opening an archive prepares a unique, explicitly ephemeral `library-generation-<uuid>/`, validates extraction before opening SQLite, and installs the new Session without touching the previous generation. Retired generations remain named until their final library Arc is gone; a later exclusive claim may sweep abandoned process-local generations.
- Archive construction verifies the exact entry set, lengths, and CRC and computes whole-file length/SHA-256 through one retained filesystem object. Only confirmed durable publication returns by-value **Project Archive Publication Evidence**; production Recovery Manifest new-entry publication consumes it without reopening or deserializing the just-created archive and rejects any mode other than `CreateNew`.
- A **Recovery Manifest** is published only after its new archive validates and becomes durable. Its canonical Project path and every snapshot path are absolute. Manifest publication retains typed pre-namespace, durability-unconfirmed, and namespace-indeterminate outcomes; only confirmed durability permits retention cleanup. Retention publishes the replacement manifest before removing entries it no longer references, so a pre-namespace failure leaves the previous Manifest and every archive it authorizes intact, while an unconfirmed/indeterminate namespace suppresses deletion rather than pretending the old namespace is proven. The new unreferenced create-only archive may remain for conservative cleanup. Publication evidence is construction fact only: discovery, selection, and recovery still rehash and reparse the current direct file object.
- Every recovery entry accepts one complete candidate—Project ID, exact paired root, canonical/autosave paths, author/Library/document revisions, timestamp, and hash—rather than a path pair. Deterministic discovery hides a snapshot only when an existing same-Project canonical archive has a greater document revision; equal revisions and a missing canonical archive remain eligible. Selection preflight must exactly match one unambiguous manifest entry but grants no mutation authority. Manifest, source, canonical Project, and staging leaves are direct regular files; leaf symlinks/reparse points fail closed. After lease acquisition, recovery rechecks canonical freshness, rejects duplicate snapshot paths, rereads and exactly matches the complete entry, hashes and copies through the retained source object, exclusively creates and flushes a staging object, verifies that same object, and then matches the manifest entry again under the recovery mutation guard. The archive loader borrows the exact verified staging handle from an RAII cleanup guard rather than reopening its path. Lease-scoped crash cleanup removes only direct stale staging leaves and snapshots proven unreferenced by a valid manifest; absent/invalid manifests, unknown entries, directories, links, and sibling roots are preserved. Only exact object evidence may create a new dirty Authoring Session; opening does not consume the source.
- A manual save may retire **Recovery Authority** only when its completion covers current author and Asset Library state. It publishes an empty canonical Recovery Manifest before deleting archives; stale completion and cleanup failure cannot fabricate successful retirement.
- Save As keeps the live Session runtime root stable. An author-generation-stale completion inside the still-current **Persistence Generation** and **Manual Project Destination Binding** may rebind retained **Recovery Authority** to the newly published canonical path without deleting it; a completion for a retired Session, prior Persistence Generation, or superseded destination has no such authority. Exact recovery selection reuses the manifest's actual runtime authority rather than deriving one from the changed path.
- A **Precompose Action** commits parent replacement and child Sequence creation once. Video-only or audio-only input produces only that placement kind; linked A/V input produces one linked pair targeting the same nested Sequence.
- A **Timeline Asset Placement** carries no Track-kind snapshot. Availability and execution use the same authoritative placement preparation, and a position not exactly representable on the active Sequence grid is rejected rather than silently moved.
- Manual save and autosave share **Durable Project Publication**, but only manual save advances the durable baseline; autosave advances recovery coverage and never makes an unsaved Project appear saved.
- Every Clip has exactly one **Clip Content** variant. Unknown or legacy parallel content fields fail current-schema loading rather than being ignored.
- Every non-null **Clip Link Group** has at least two members. Selection and commands expand a selected member to the complete set, validate all locked Tracks and zero-boundary constraints before mutation, and compact broken/singleton membership before commit. Explicit Link retains the sole existing group identity when adding unlinked members, creates a fresh identity when merging multiple groups, and never commits a no-op; Unlink clears every expanded group atomically.
- A **Video Transition** derives Track membership from its strong Clip endpoints. It persists no parallel Track identity, requires one exact shared cut, and projects its full interval into both source domains without clamping; insufficient source handles fail closed.
- A **Video Transition Product Action** is the sole product-operation Interface for Video Transition selection and authoring. Widget frame proposals become exact Sequence-local author time once at the App panel Adapter; Product dispatch never reconstructs the range from an implicit grid, and Create/SetRange/Remove revalidate current strong endpoints without cloning the complete Sequence candidate before the Author Transaction.
- A professional **Insert Edit** is one atomic Sequence edit over explicit target Tracks and Sync-Lock/ripple scope. Track Targeting and Sync-Lock are editor policy resolved into the request, not renderable Sequence author state. The edit opens one exact interval, splits crossing material, preserves coherent link groups, respects every lock, safely shifts downstream Transitions, explicitly rejects/removes intersected Transitions, and applies an explicit Sequence-time automation/navigation policy. The unrelated local collision mode is named `PushForward` and can never satisfy Insert semantics.
- One **Range Edit** receives **Timeline Edit Targeting** only as explicit content and ripple Track sets. Lift never ripples. Extract requires every content Track in its ripple set, may shift an untargeted Sync-Locked Track only when the removed interval contains no content there, and otherwise rejects without mutation. Clip/source/local-time fragmentation, complete link-group transforms, Transition disposition, Sequence-time automation ownership closure, playhead/In/Out collapse, author validation, and one Undo receipt are one transaction.
- A **Track Product Action** never transports a display index or media-kind snapshot for an existing Track. Move resolves a stable same-kind Before/After anchor against current author order; already-satisfied relations and repeated author/session controls are non-actions. Visibility is video-only, Mute is audio-only, Lock is shared, and Target/Sync-Lock never advance Author Generation or History.
- **Automation Follows Insert** is ownership-based: a rippled Track and Routes sourced from it follow; a Bus or Program Output follows only when every authored input follows. Clip/Scope-local automation remains in its local domain. Partial aggregate ownership never guesses.
- A visual Transition author type, definition, or Property Bag is not execution evidence. Product support requires the shared Preview/Export render projection and verified backend path.
- Undo and Redo restore author content but always advance the **Sequence Author Revision**; restored content can therefore never masquerade as the older live snapshot from which it originated.
- **Timeline Time** equality, ordering, arithmetic, and hashing use checked canonical rational semantics; serialized numerator/denominator field order can never define chronology.
- Timeline Times from different **Authoring Time Domains** cannot be compared or combined until an explicit **Time Transform** maps one domain into the other.
- Video automation and audio automation share the same exact curve and stable parameter-identity foundation; their Evaluation Grids, supported value types, and delivery cadence remain domain-specific.
- Animation selection, clipboard state, and incremental editing address a property by **Parameter Instance Address** and a key by `KeyframeId`; point index, key time, display name, and property-path alias are snapshot-local lookup data and never author identity.
- Moving one key's time and value is one atomic author mutation that preserves its `KeyframeId`, interpolation handles, and temporal flags. Collision, ambiguity, stale identity, or invalid schema state fails before an **Author Transaction** and cannot expose a remove-then-insert intermediate.
- One committed curve gesture produces at most one **Author Transaction**. Pointer motion is widget-local preview state; a Curve Editor emits one incremental Insert/Move/Delete intent on commit and never replaces an entire author curve.
- Every renderer-stage exchange uses a **Color Frame Contract**. OCIO transforms process RGB only and require straight/opaque coverage; spatial filtering may materialize a typed premultiplied internal frame but must restore the declared public association before the next Module.
- **Display Timecode** formats a Timeline Time but never owns it; changing drop-frame display or start timecode cannot move authored media.
- **Timeline Interchange** is an app-independent bounded translation Module, not a second Timeline. Native OTIO/CMX/FCP XML Adapters and the isolated qualified AAF helper lower into one private exact representation; only the common materializer may construct a detached canonical Sequence after every foreign media key has a product-owned strong Asset binding.
- Every successful interchange inspection or preparation returns one versioned **Interchange Conformance Report** with pinned profile/direction, source/semantic/capability digests, stable owner-addressed findings, aggregate dispositions, and outcome. Blockers always fail; an allow-with-report policy can admit only non-blocking declared loss and never fabricates support.
- **Editorial Source Identity** is optional bounded media-interpretation metadata: reel name, exact SMPTE source reference, and foreign item identity. It supports conform and round trip but never replaces Asset identity, Clip placement, or canonical Timeline Time.
- The OTIO Mondrian color extension may preserve explicit input color identity and only an exact static `ASC CDL -> LUT3D` Clip Grade. A LUT requires URI, processing-space identity, finite intensity, and SHA-256 content binding; other graphs or formats produce explicit loss/blocker evidence rather than alternate grade semantics.
- A qualified **AAF Toolchain** is an isolated process Adapter identified by exact helper, bridge, and engine versions. Its bounded bridge and Compound File Binary gate are containment and format evidence, not proof of compatibility with any external editing application; real-application qualification belongs to the cross-application matrix.
- One Sequence persists exactly one position-display setting and resolves it with its video Evaluation Grid into one validated display contract shared by Viewer and Timeline ruler. Frames ignore the retained timecode origin; drop-frame is valid only for its exact supported rational rates, and neither negative positions nor SMPTE 24-hour label wrapping alter Timeline Time.
- Each Sequence exclusively owns one **Audio Program**; ordinary PCM routes cannot cross Sequence ownership.
- An **Audio Program** presents one typed routing graph of **Audio Routing Nodes** without forcing Track, Bus, and Output to share an untyped identity or lifecycle; a Track Mixer Channel uses its owning audio Track identity.
- An **Audio Route** connects stable typed endpoints and can never target a **Generated Audio Stage**.
- Every independently processable **Audio Processing Scope** and every **Audio Routing Node** may own explicitly placed **Audio Processor Racks** using the same **Audio Processor Instance** author model for built-ins, VST3, CLAP, and future host adapters.
- An **Audio Processor Instance** addresses automation by stable instance and parameter identity; display names, property-path suffixes, plugin file paths, and parameter indexes are never authoritative.
- Audio Processor Instance IDs are unique across every Scope/Track/Bus/Output Rack in one Sequence. Current preparation preserves Rack order and creates one **Generated Audio Processor Occurrence** per execution owner; a shared Audio Processing Scope never by itself shares mutable DSP state. Any coalescing requires proof of the complete Audio State Domain and must preserve identical execution semantics.
- Each Audio Processor parameter persists one validated **Parameter Schema** snapshot and one exact curve keyed by the same `ParameterId`; built-in compilation additionally requires an exact match to its canonical definition schema, while unavailable external dependencies retain the snapshot and opaque state but fail execution closed unless bypassed.
- Every generated processor receives an allocation-free **Audio Parameter Event Batch** whose lanes retain stable `ParameterId` order and whose sample offsets are block-local. ABI normalization and capability reduction belong only to the concrete processor Adapter and must fail closed when exact delivery is unavailable.
- An intentional audible delay is Processor signal semantics, not PDC-compensable algorithmic latency. A Processor may declare nonzero algorithmic latency only when the Host must align hidden lookahead/group delay; meaningful output after silence is instead an explicit **Audio Processor Tail**. Equal sample-history lengths do not make these meanings interchangeable.
- The built-in **Linked Sample-Peak Lookahead Limiter** is the first production nonzero-latency Processor: Lookahead is a non-automatable millisecond author parameter rounded upward once onto the prepared sample grid, while Ceiling and Release retain their signal-time automation through the Processor's own delay. One fixed-capacity monotonic peak window gives amortized constant work per frame, every channel shares the same gain, and a fresh Audio State Domain clears delay, window, and envelope history. This contract proves only sample-peak limiting; true-peak requires a separately qualified oversampled detector.
- Audio plan preparation checked-sums every realized occurrence's processor-private Session bytes, aggregate PDC delay-line bytes, and **Audio Public Output Lookahead** against explicit **Audio Render Contract** budgets. Session construction must reproduce the admitted totals; overflow, excess, or Factory contract drift fails before callback execution.
- Component automation uses Audio Component Edit-local rational time, Scope automation uses Audio Processing Scope-local rational time, Track/Bus/Output automation uses Sequence-local rational time, and an **Audio Transition** interval is Sequence-local; compilation maps each domain once to exact sample offsets. Execution subtracts each prepared stage's input-signal delay before evaluating its automation or Processor parameter batch, so PDC cannot shift author time.
- An **Audio Transition** names exactly two Audio Component Edits and does not affect other overlapping material; overlap without a Transition remains ordinary summing.
- Every parallel input to a sum or Transition is delay-compensated from declared processor and nested latency; internal floating-point mixing neither normalizes, soft-clips, nor limits without an explicit authored processor.
- Each **Audio Role** belongs to exactly one **Sequence Semantic Catalog**; its optional parent is a **Strong Entity Reference** in the same catalog, and the resulting hierarchy must be acyclic.
- Each separable Audio Component Edit has zero or one authoritative **Audio Role**; ancestors are implied by hierarchy, while independent tags and routing duplication cannot masquerade as additional Role assignments.
- A Track may materialize a default **Audio Role** when authoring an otherwise unclassified Audio Component Edit, but moving that edit does not silently reclassify it; a mixed Bus has no authoritative single Role.
- A **Standard Semantic Key** may suggest matches between Audio Roles in different Sequences but never establishes a reference or changes existing authoring semantics.
- Audio Component Edits, semantic projections, and output-family selectors use local **Audio Role** identities; a Project may derive indexes and coordinate atomic edits across Sequence snapshots but owns no live role graph referenced by them.
- Every **Strong Entity Reference** resolves before an authoring transaction commits; only a **Recoverable Dependency Reference** can produce a runtime resolution issue.
- Product Asset removal retires **Asset Library Membership** in one preflighted SQLite transaction and never edits a Sequence or Project proxy set. `get_asset` continues resolving retired records while ordinary listing hides them; a genuinely missing row fails closed as a broken strong reference.
- Every **Generated Audio Stage** retains a deterministic origin reference for diagnostics, caching, and per-instance DSP state allocation.
- Preview, playback, audition, analysis, and export compile the same author semantics into generated operations; they may schedule differently but cannot reinterpret processor order, automation, transitions, latency, or nesting.
- An **Audio Audition Overlay** is transient root-consumer intent: only the selected root Sequence compiler receives it. Recursive child public outputs compile their canonical Program, and a failed Runtime replacement restores the prior open-Session intent.
- An **Audio Meter Bank** publishes all executed Channel Strip targets under one successful-block serial. Hidden priming and incomplete execution are never observable, and target absence cannot be presented as measured silence.
- A **Prepared Audio Schedule** propagates algorithmic latency, **Audio Processor Tail**, stage input-signal delay, and state-entry obligations through every Contribution, port-specific Route, and processor rack. The selected Program Output normalizes its total latency into bounded **Audio Public Output Lookahead**; a normalized nested public output enters its parent with zero algorithmic latency while retaining its independent state-entry obligation.
- A stateful **Audio Render Session** begins unentered, accepts a fresh continuity epoch plus exact first sample, and thereafter admits only exact contiguous blocks. Reusing an epoch, skipping coordinates, or continuing after an execution error is invalid; a fresh entry resets Session history. A stateful Contribution occurrence remains pending until its exact causal execution interval begins, then receives contiguous local callbacks through its declared tail; it is never advanced across pre-Clip silence. Discontinuity causes remain coordinator evidence and are not processor-owned rule keys.
- Each stateful nested Sequence instance owns a private continuity epoch stream. For a nondecreasing parent time map, the nested Runtime enters lazily at the first demanded child sample and evaluates every skipped child sample in order, so speed mapping cannot skip processor history. Generic stateful reverse evaluation is invalid until a processor-specific reverse contract, checkpoint replay, or materialized child output proves block-partition-invariant semantics; stateless nested output remains freely indexable in either direction.
- **Audio Playback** attaches one explicit Enter operation to the first PCM window of each render generation and Continue to every later window. The Timeline PCM Adapter maps that generation to the Runtime continuity epoch and rejects duplicate entry, missing entry, generation mismatch, or a non-contiguous sample coordinate; it never infers entry from a changed start sample.
- A PCM renderer declares either independent windows or generation-owned state. Only independent-window failure may become exact-duration silence; failure or PCM-contract violation after generation-owned state has advanced invalidates the whole generation, clears its output, hands Clock Master to Synthetic, and re-enters a fresh generation at Playback's authoritative position. Fresh-generation retries are bounded; exhaustion enters explicit RenderBlocked state until a deliberate reprime boundary.
- **Audio Execution Owner Closure** is a captured two-phase protocol. Playback freezes the PCM continuity model before trait erasure, signals only prebuilt atomic/cancellation seams at begin, and consumes its render, renderer-retirement, output/device, and Audio Source owners against one absolute deadline. Opaque renderer errors and FFmpeg child/pipe owners are retired only by their tracked background owners; safe completion values may cross back to Playback. Native decoder construction uses an independent bounded startup lane. Its move-only completion retains a teardown-producer lease through install or retirement, so read cancellation never waits for OS spawn and queue closure cannot strand a late child. Physical decoder capacity counts active, queued, partial, unclaimed, terminating, and EOF-finalizing Sessions until child and pump retirement. Default, stale, timed-out, detached, abandoned, or residual-owner receipts always fail closed.
- CPU scalar, CPU SIMD, isolated plugin, and optional GPU execution are prepared processor backends for the same compiled semantics, not alternate Audio Programs. A GPU backend must declare and account for batch/transfer latency, PDC, state ordering, cancellation, bounded in-flight storage, and device loss; the audio callback never waits for GPU work.

**Isolated Audio Processor Worker**:
The exclusive supervised child process that owns one mutable external processor instance. Parent and Worker revalidate one exact execution/render/auxiliary-bus/parameter contract, exchange PCM and sample-accurate events through Session-preallocated fixed shared storage, and use a versioned bounded loopback control protocol. The parent owns per-operation deadlines, poisoning, termination, and reaping; a crash, access violation, hang, disconnect, or protocol failure cannot resume that instance. This is failure containment, not a hostile-code security sandbox or evidence that a particular VST3/CLAP ABI Adapter is available.
_Avoid_: loading external plugin code in the Mondrian process, process-per-block execution, unbounded pipe PCM, plugin-created alternate mixing, treating panic isolation as crash isolation, or advertising native plugin availability from the generic Worker boundary alone
- Equal **Signal Closures** may share immutable plans, but mutable processors share an **Audio State Domain** only when all continuity and evaluation identities also match; fingerprints alone never authorize state sharing.
- An **Audio Program** exposes one or more stable **Sequence Output Ports** and never binds physical listening devices.
- A **Parameter Schema** has one stable identity shared by all instances; each instance has its own stable **Parameter Instance Address** and author value.
- Effect execution selects parameters by Parameter Schema identity, never by address suffix; UI and commands route through the stable Parameter Instance Address and resolve its current property-path alias only at the author boundary.
- Gamut Compression and Highlight Recovery are ordinary stable-schema Effect Definitions. Their CPU/GPU implementations consume the same compiled grade, preserve alpha, allocate no scratch frame, and cannot be reinterpreted by Viewer or Export.
- Color/Alpha domain, CPU/GPU support, determinism, temporal extent, and ROI have one owner on the Processor/Effect Definition and compiled graph; Parameter Schema cache impact only determines what must be re-resolved.
- A nested Sequence is one instanced composite audio source in its parent; it consumes selected **Sequence Output Ports** and owns independent mutable DSP execution state.
- A parent binds nested PCM through the child's stable public output identities and records any semantic assignment separately against parent-local **Audio Roles**; it never references child-internal Audio Role identities.
- A **Delivery Mapping** packages Sequence outputs but cannot address private child tracks, buses, or routes.
- A **Monitor Path** consumes logical Sequence outputs and cannot alter program or exported samples.
- A **Reference Output Module** consumes one public Audio Program plus the canonical full-raster Program Output; it cannot consume Monitor Path processing or Viewer presentation pixels.
- **Audio Render Admission** and **Monitor Sink Admission** are independent; a missing Monitor Sink never invalidates an Audio Program or stops Synthetic Clock transport.
- During `Priming`, **Audio Playback** may render and queue PCM but must keep device consumption inactive. Only `Playing` or `Recovering` grants consumption permission; a late/missing video frame cannot revoke it or rotate the audio render generation.
- **Audio Playback** records isolated underruns without changing Clock Master; sustained missing-sample evidence enters recovery through a continuous Synthetic handoff and fresh preroll.
- Each persisted archive, document, and SQLite library has an independent version and **Project Migration** chain.
- A **Project Migration** operates on an in-memory value or runtime copy; opening never rewrites the source archive.
- A **Dynamic HDR Program** is Sequence-owned final-Program author state, never a Clip property or export-preset payload. It contains bounded, exact-time, format-specific analyzed Shots plus immutable Adapter/schema, visual-author-fingerprint, and canonical-payload provenance. `Omit`, byte-identical whole-source-file `PreserveSourceExact`, and qualified `Remake` are distinct delivery intents; preservation is not remux or Smart Render and can never fall back to rendered pixels. Remake requires a licensed/adopter-qualified generator, independent technical validation, and human HDR/SDR QC. ST 2094-40 Application #4 detection is not an HDR10+ certification claim, and Dolby CM version, metadata levels, bitstream profile/level, licensing, and delivery qualification remain distinct evidence.

## Example dialogue

> **Dev:** “The Viewer missed frame 240. Should it set playback buffering?”
> **Domain expert:** “No. It reports a late **Frame Delivery**. The **Playback Session** decides whether its **Transport State** keeps playing, primes, or recovers according to the active **Clock Master** and quality policy.”

> **Dev:** “Can Reel 1 route directly into a Project-wide dialogue bus?”
> **Domain expert:** “No. Reel 1 exposes a **Sequence Output Port**; a parent Sequence combines it inside its own **Audio Program**, while the Project only applies a **Delivery Mapping**.”

## Flagged ambiguities

- “Buffering” previously meant startup priming, per-frame decode waiting, and sustained recovery. These are distinct Transport States or Frame Delivery outcomes.
- “Audio clock” previously advanced from `Instant` even when it was not proven to represent consumed device samples. Device sample position and synthetic monotonic time are distinct Clock Masters.
