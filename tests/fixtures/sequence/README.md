# Sequence Fixtures

Put nested-sequence and interpretation samples here.

Use this folder for:
- nested sequence clips
- PAR / field order / frame-rate override cases
- alpha interpretation cases
- timeline diagnostic samples

Recommended structure:
- `source/` for input project media
- `golden/` for expected render or diagnostic output
- `manifests/` for sequence metadata and expected interpretation notes
