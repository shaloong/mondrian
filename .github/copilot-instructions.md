# Mondrian Copilot Instructions

本文件为 Copilot 标准入口指令文件。

- 规范主文件：`/copilot-instrution.md`
- 当本文件与主文件存在差异时，以 `copilot-instrution.md` 为准。
- 所有 AI 代码改动必须遵守该主文件中的 MUST/必须/强制条款。

为避免歧义，提交前请重点核对：

- Conventional Commits 格式是否正确。
- `cargo fmt` 与 `cargo clippy --workspace` 是否通过。
- 是否补齐测试与受影响架构文档更新。
